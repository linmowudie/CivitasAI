# 数据模型与持久化设计

> 本文档定义 Civitas-AI 的 SQLite 数据库 Schema、数据生命周期和存储策略。
>
> **本文档 §2–§4 与 §8.2 是全部 DDL 的唯一事实源（Single Source of Truth）**。其他设计文档（含 Docs/12、Docs/13）**禁止内联 `CREATE TABLE`**，只能引用本文件的表名与列名；
> 表结构变更一律先改本文件，再回写引用方。开发速查入口见 `Docs/15-参数总典与接口契约/参数总典.md`。
>
> **可执行事实源**：本文件为**设计权威**，`Src/Infra/Db/migrations.ts` 为**可执行注册点**（唯一建表入口），两者必须逐表同步；不一致时以迁移代码为准并回写本文件（2026-09-22 校准）。

---

## 1. 数据库概览

| 数据库 | 文件位置 | 用途 |
|--------|---------|------|
| `civitas_main.db` | `Data/db/civitas_main.db` | 主业务数据库（任务、Agent、Token、会话、持久执行、审批）**+ 共享记忆 `global_workspace` + 长期记忆 `long_term_memory` + `effect_journal`** |
| `civitas_events.db` | `Data/db/civitas_events.db` | 事件溯源数据库（仅追加，不作恢复入口）：`events` + `domain_events` + `action_fingerprints` |
| `civitas_memory.db` | `Data/db/civitas_memory.db` | **当前为空壳**：仅建立连接与 `_migrations` 版本表，**无任何域表**（共享记忆/长期记忆实际落在 `civitas_main.db`） |

> **2026-09-22 校准**（依据 `Src/Infra/Db/migrations.ts` 逐条核对）：
> - `effect_journal` 注册于 **main** 库（`migrations.ts:167`，`database: 'main'`），**不是** events；
> - `action_fingerprints` 注册于 **events** 库（`migrations.ts:245`）；
> - `global_workspace`（`:295`）/ `long_term_memory`（`:315`）注册于 **main** 库，memory 库目前**没有**任何 `registerMigration(..., database: 'memory')` 登记项；
> - memory 库仅在 `initMigrations()` 中创建 `_migrations` 表（`:359`）。三库分离仍是**目标态**，把记忆域表迁至 `civitas_memory.db` 列为待办迁移。

迁移登记现状：main 库 19 项（v1–v19）、events 库 3 项（`events` v1 / `action_fingerprints` v2 / `domain_events` v3）、memory 库 0 项。

### 1.1 DDL 全局约定（强制 · v2.1 新增）

| 约定 | 规则 | 反例（已修正） |
|------|------|---------------|
| **时间列类型** | 所有 `*_at` 列一律 `INTEGER`，存 **epoch 毫秒**；禁止 `TEXT` ISO-8601 与 `INTEGER` 混用 | v1 表 `created_at TEXT` / v2 表 `created_at INTEGER` 双标 |
| **时长列** | `*_ms` 后缀 + `INTEGER`；审批超时长用 `timeout_sec` + `INTEGER`（秒） | `time_limit_ms` 与 `timeout_ms` 两名并存 → 统一为 **`timeout_ms`** |
| **比例/评分列** | `REAL`，取值域 **0.0–1.0**，与 `Configs/**` 标度一致 | `quality_score INTEGER`（0–100）→ **`quality_score REAL`** |
| **金额列** | `REAL` + `_usd` 或明确语义名（`calculated_cost`/`tax_amount`/`net_deduction`） | — |
| **枚举列** | `TEXT`，存值与 TS 联合类型**完全同形**（不做大写化/小写化转换），取值写在行内注释中 | `phase` 存 `'awaiting_approval'` 而 TS 写 `AwaitingApproval` |
| **JSON 列** | 列名以 `_json` 结尾（数组/对象皆然）；TS 侧为已解析的结构体 | `decided_by TEXT` → **`decided_by_json TEXT`** |
| **布尔列** | `INTEGER` 0/1，命名 `is_*` 或 `*_enabled` | — |
| **外键** | 子表必须声明 `FOREIGN KEY ... ON DELETE CASCADE`；`PRAGMA foreign_keys = ON` 在连接初始化时开启 | `verifier_results`/`failure_feedbacks` 已有；`action_fingerprints` 原缺 → **仍未补**（2026-09-22 校准：`migrations.ts:245-255` 该表无任何 `FOREIGN KEY`，且其在 **events** 库、`loops` 在 **main** 库，跨库外键 SQLite 不支持 → 改为应用层校验，列为待办） |
| **机械映射** | TS `camelCase` ↔ DB `snake_case` 由 repository 统一转换，**禁止人为改名** | `timeoutMs` ↔ `timeout_ms` |

---

## 2. 主数据库 Schema

### 2.1 任务表（tasks）

```sql
CREATE TABLE tasks (
    task_id TEXT PRIMARY KEY,              -- UUID
    session_key TEXT NOT NULL,             -- 会话标识
    trace_id TEXT NOT NULL,                -- 运行追踪 ID
    
    -- 任务信息
    user_request TEXT NOT NULL,            -- 用户原始需求
    route_mode TEXT NOT NULL,              -- 路由模式：DIRECT/DELEGATION/CONSORTIUM/ASSEMBLY_LINE
    status TEXT NOT NULL DEFAULT 'pending',-- pending/running/completed/failed/cancelled
    
    -- 复杂度评估
    estimated_tokens INTEGER,
    required_domains TEXT,                 -- JSON 数组
    coupling_score REAL,
    
    -- 结果
    final_result TEXT,
    quality_score REAL,                      -- 0.0–1.0；禁止 0–100 百分制
    
    -- 时间
    created_at INTEGER NOT NULL,
    started_at INTEGER,
    completed_at INTEGER,
    
    FOREIGN KEY (session_key) REFERENCES sessions(session_key)
);

CREATE INDEX idx_tasks_status ON tasks(status);
CREATE INDEX idx_tasks_trace ON tasks(trace_id);
CREATE INDEX idx_tasks_session ON tasks(session_key);   --（迁移未建，待办：migrations.ts:88-89 仅建前两个）
```

### 2.2 子任务表（subtasks）

```sql
CREATE TABLE subtasks (
    subtask_id TEXT PRIMARY KEY,           -- UUID (assignmentId)
    task_id TEXT NOT NULL,
    trace_id TEXT NOT NULL,
    
    -- 任务定义
    description TEXT NOT NULL,
    input_context TEXT,                    -- JSON
    output_schema TEXT,                    -- JSON Schema
    assigned_agent_id TEXT,
    
    -- 约束
    max_iterations INTEGER NOT NULL DEFAULT 30,   -- 同上：仅作 fallback，实际由 loopConfig 注入
    timeout_ms INTEGER,
    token_budget INTEGER,
    
    -- 状态
    status TEXT NOT NULL DEFAULT 'pending',-- pending/assigned/running/completed/failed
    result TEXT,                           -- JSON
    quality_score REAL,                      -- 0.0–1.0；禁止 0–100 百分制
    failure_count INTEGER DEFAULT 0,
    failure_reason TEXT,
    
    -- 依赖
    depends_on TEXT,                       -- JSON 数组
    required_tools TEXT,                   -- JSON 数组
    
    -- 时间
    created_at INTEGER NOT NULL,
    started_at INTEGER,
    completed_at INTEGER,
    
    FOREIGN KEY (task_id) REFERENCES tasks(task_id)
);

CREATE INDEX idx_subtasks_task ON subtasks(task_id);
CREATE INDEX idx_subtasks_agent ON subtasks(assigned_agent_id);   --（迁移未建，待办：migrations.ts:103 仅建 idx_subtasks_task）
```

### 2.3 Agent 表（agents）

```sql
CREATE TABLE agents (
    agent_id TEXT PRIMARY KEY,             -- 格式：{role}-{uuid8}
    trace_id TEXT NOT NULL,
    parent_agent_id TEXT,                  -- 招募者 ID
    
    -- 角色配置
    role TEXT NOT NULL,                    -- prime_director/partner/worker/reviewer/assembly_node/arbitrator/auditor/regulator（与 loopConfig.roleOverrides 键一一对应，Docs/11 §2.8）
    system_prompt TEXT NOT NULL,
    task_prompt TEXT,
    
    -- 工具配置
    allowed_tools TEXT,                    -- JSON 数组
    denied_tools TEXT,                     -- JSON 数组
    
    -- 资源限制
    token_budget INTEGER,
    max_iterations INTEGER DEFAULT 30,     -- 仅作 fallback；实际值由 `loopConfig.roleOverrides[role].max_iterations` 在创建时写入
    timeout_ms INTEGER,
    
    -- 安全配置
    trust_level TEXT NOT NULL DEFAULT 'L1',-- L0/L1/L2
    sandbox_enabled INTEGER DEFAULT 1,
    
    -- 状态
    status TEXT NOT NULL DEFAULT 'creating',-- creating/ready/running/suspended/expelled/destroyed
    failure_count INTEGER DEFAULT 0,
    
    -- 时间
    created_at INTEGER NOT NULL,
    destroyed_at INTEGER,
    
    FOREIGN KEY (parent_agent_id) REFERENCES agents(agent_id)
);

CREATE INDEX idx_agents_trace ON agents(trace_id);
CREATE INDEX idx_agents_status ON agents(status);
CREATE INDEX idx_agents_role ON agents(role);   --（迁移未建，待办：migrations.ts:74-75 仅建前两个）
```

### 2.4 Token 钱包表（token_wallets）

```sql
CREATE TABLE token_wallets (
    wallet_id TEXT PRIMARY KEY,            -- UUID
    agent_id TEXT NOT NULL,
    
    -- 余额
    balance REAL NOT NULL DEFAULT 0,
    total_earned REAL NOT NULL DEFAULT 0,
    total_spent REAL NOT NULL DEFAULT 0,
    total_tax_paid REAL NOT NULL DEFAULT 0,
    total_frozen REAL NOT NULL DEFAULT 0,
    
    -- 状态
    status TEXT NOT NULL DEFAULT 'active', -- active/frozen/closed
    
    -- 时间
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL,
    
    FOREIGN KEY (agent_id) REFERENCES agents(agent_id)
);

CREATE INDEX idx_wallets_agent ON token_wallets(agent_id);
```

### 2.5 Token 交易表（token_transactions）

```sql
CREATE TABLE token_transactions (
    transaction_id TEXT PRIMARY KEY,       -- UUID
    wallet_id TEXT NOT NULL,
    trace_id TEXT,
    operation_id TEXT,
    
    -- 交易信息
    type TEXT NOT NULL,                    -- 见 TransactionType 枚举
    amount REAL NOT NULL,                  -- 正=收入，负=支出
    balance_after REAL NOT NULL,
    
    -- 详情
    description TEXT,
    metadata TEXT,                         -- JSON
    
    created_at INTEGER NOT NULL,
    
    FOREIGN KEY (wallet_id) REFERENCES token_wallets(wallet_id)
);

CREATE INDEX idx_tx_wallet ON token_transactions(wallet_id);
CREATE INDEX idx_tx_trace ON token_transactions(trace_id);   --（迁移未建，待办）
CREATE INDEX idx_tx_type ON token_transactions(type);
CREATE INDEX idx_tx_created ON token_transactions(created_at);   --（迁移未建，待办）
-- migrations.ts:126-127 仅建 idx_tx_wallet 与 idx_tx_type
```

### 2.6 消耗记录表（consumption_records）

> ⚠️ **未落库（当前内存态实现），列为待办迁移（2026-09-22 校准）**：`Src/Infra/Db/migrations.ts` 中**不存在** `consumption_records` 的 `CREATE TABLE`；实际消耗记录由 `Src/Services/TokenEconomy/consumptionRecorder.ts:16` 的进程内数组 `const records: ConsumptionRecord[] = []` 持有，**重启即丢失**。以下 DDL 为**目标态设计**，落库前不得当作既有能力引用。

```sql
CREATE TABLE consumption_records (
    record_id TEXT PRIMARY KEY,
    trace_id TEXT NOT NULL,
    agent_id TEXT NOT NULL,
    operation_id TEXT NOT NULL,
    
    -- 模型信息
    provider TEXT NOT NULL,
    model TEXT NOT NULL,
    
    -- Token 计量
    prompt_tokens INTEGER NOT NULL,
    completion_tokens INTEGER NOT NULL,
    total_tokens INTEGER NOT NULL,
    
    -- 成本
    unit_cost_per_1k REAL NOT NULL,
    calculated_cost REAL NOT NULL,
    tax_amount REAL NOT NULL,
    net_deduction REAL NOT NULL,
    
    -- 时间
    started_at INTEGER NOT NULL,
    completed_at INTEGER NOT NULL
);

CREATE INDEX idx_consumption_agent ON consumption_records(agent_id);
CREATE INDEX idx_consumption_trace ON consumption_records(trace_id);
```

### 2.7 仲裁案件表（arbitration_cases）

> ⚠️ **未落库（当前内存态实现），列为待办迁移（2026-09-22 校准）**：`migrations.ts` 中**不存在** `arbitration_cases` 的 `CREATE TABLE`；案件实际由 `Src/Services/Arbitration/tribunal.ts:20` 的进程内 `Map`（`const cases: Map<string, ArbitrationCase> = new Map()`）与 `:21` 的 `restorationPlans` 持有，**重启即丢失**。以下 DDL 为**目标态设计**（另见本文 §8.3 关于 `loop_ids` 的同批待办）。

```sql
CREATE TABLE arbitration_cases (
    case_id TEXT PRIMARY KEY,
    conflict_id TEXT NOT NULL,
    trace_id TEXT,
    
    -- 涉事方
    parties TEXT NOT NULL,                 -- JSON 数组
    capsule TEXT,                          -- JSON (ContextCapsule)
    
    -- 裁决
    arbitrator_verdicts TEXT,              -- JSON 数组
    final_verdict TEXT,                    -- JSON
    is_deadlock INTEGER DEFAULT 0,
    is_regulatory_intervention INTEGER DEFAULT 0,
    
    -- 恢复
    restoration_status TEXT DEFAULT 'pending',
    restorer_id TEXT,
    compensation_actions TEXT,             -- JSON 数组
    
    -- 状态
    status TEXT NOT NULL DEFAULT 'filed',  -- filed/arbitrating/verdict_reached/restoring/closed/deadlocked
    
    -- 时间
    filed_at INTEGER NOT NULL,
    verdict_at INTEGER,
    restored_at INTEGER,
    closed_at INTEGER
);

CREATE INDEX idx_cases_status ON arbitration_cases(status);
CREATE INDEX idx_cases_trace ON arbitration_cases(trace_id);
```

### 2.8 会话表（sessions）

```sql
CREATE TABLE sessions (
    session_key TEXT PRIMARY KEY,          -- SHA-256 前 16 位
    trace_id TEXT,                         -- 当前活跃 trace
    
    -- 会话信息
    user_id TEXT,
    description TEXT,
    
    -- 状态
    status TEXT NOT NULL DEFAULT 'active', -- active/archived
    
    -- 时间
    created_at INTEGER NOT NULL,
    last_active_at INTEGER NOT NULL,
    archived_at INTEGER
);
```

### 2.9 操作历史表（operation_history）

> ⚠️ **未落库（当前内存态实现），列为待办迁移（2026-09-22 校准）**：`migrations.ts` 中**不存在** `operation_history` 的 `CREATE TABLE`。代码侧仅有 `OperationRecord` 类型（`Src/Services/Arbitration/types.ts:59`）与**尚未接线**的 `getAgentOps()` 端口（`Src/Services/Arbitration/capsuleAssembler.ts:18`），既无表也无集中式内存存储，属**纯设计态**——操作历史目前只能从 `events` / `effect_journal` 间接还原。

```sql
CREATE TABLE operation_history (
    operation_id TEXT PRIMARY KEY,         -- {timestamp_ms}-{4位hex}
    trace_id TEXT NOT NULL,
    agent_id TEXT NOT NULL,
    
    -- 操作信息
    operation_type TEXT NOT NULL,          -- llm_call/tool_execute/file_write/...
    description TEXT,
    input_data TEXT,                       -- JSON
    output_data TEXT,                      -- JSON
    status TEXT NOT NULL,                  -- success/failed/timeout
    
    -- 耗时
    duration_ms INTEGER,
    
    created_at INTEGER NOT NULL
);

CREATE INDEX idx_ops_trace ON operation_history(trace_id);
CREATE INDEX idx_ops_agent ON operation_history(agent_id);
CREATE INDEX idx_ops_created ON operation_history(created_at);
```

### 2.10 行为准则表（behavior_rules）

> ⚠️ **未落库（当前内存态实现），列为待办迁移（2026-09-22 校准）**：`migrations.ts` 中**不存在** `behavior_rules` 的 `CREATE TABLE`；准则实际由 `Src/Services/Regulation/behaviorCode.ts:39` 的进程内变量 `currentCode` 与 `:40` 的 `history: BehaviorCode[]` 持有（默认规则内置于 `DEFAULT_RULES`），**重启即回到内置默认**。
> 形状差异（落库迁移时须裁决）：代码是**整部准则对象嵌套 `rules` 数组**，非按行存储；字段名 `condition`（本表为 `condition_expr`），且 `BehaviorRule` 无 `effective_at` / `created_at` 单列，版本与生效时间在 `BehaviorCode` 顶层（`version` / `effectiveAt` / `publishedAt`）。

```sql
CREATE TABLE behavior_rules (
    rule_id TEXT PRIMARY KEY,
    version TEXT NOT NULL,
    
    category TEXT NOT NULL,                -- safety/communication/resource/cooperation
    description TEXT NOT NULL,
    condition_expr TEXT NOT NULL,
    action TEXT NOT NULL,                  -- warn/restrict/forbid/report
    severity TEXT NOT NULL,                -- low/medium/high/critical
    enforceable INTEGER NOT NULL DEFAULT 0,
    
    effective_at INTEGER NOT NULL,
    created_at INTEGER NOT NULL
);
```

### 2.11 系统配置表（system_config）

```sql
CREATE TABLE system_config (
    config_key TEXT PRIMARY KEY,
    config_value TEXT NOT NULL,            -- JSON
    description TEXT,
    updated_at INTEGER NOT NULL,
    updated_by TEXT                        -- agent_id 或 'system'
);
```

---

## 3. 事件数据库 Schema

> events 库（`civitas_events.db`）实际含 **3 张表**：`events`（v1）、`action_fingerprints`（v2，DDL 见本文 §8.2 第 8 项）、`domain_events`（v3）。
>
> **接线状态（2026-09-22 校准）**：`events` 与 `domain_events` 虽由迁移建表，但 `Src/` 中**无任何写入方**（无 `INSERT INTO events` / `domain_events`，亦无 eventRepository）；事件总线为纯进程内 pub/sub（`Src/Services/EventBus/eventBus.ts:21`）。两表**已建但当前恒为空**，"仅追加事实日志"能力尚未落地。

### 3.1 事件表（events）

*迁移登记：`migrations.ts:258-267`（`version: 1`, `database: 'events'`）。*

```sql
CREATE TABLE events (
    event_id TEXT PRIMARY KEY,
    event_type TEXT NOT NULL,
    trace_id TEXT,
    source TEXT NOT NULL,
    
    payload TEXT NOT NULL,                 -- JSON
    priority TEXT NOT NULL DEFAULT 'normal',
    
    created_at INTEGER NOT NULL
);

CREATE INDEX idx_events_type ON events(event_type);
CREATE INDEX idx_events_trace ON events(trace_id);
CREATE INDEX idx_events_created ON events(created_at);   --（迁移未建，待办：migrations.ts:264-265 仅建前两个）
```

### 3.2 领域事件表（domain_events）

*迁移登记：`migrations.ts:332-344`（`version: 3`, `database: 'events'`）。2026-09-22 校准补登记——本表此前未出现在本文档中。*

与 `events` 的分工：`events` 为**基础设施级仅追加事实日志**（含 `created_at`，仅审计、不作恢复入口）；`domain_events` 为**事件总线发布/订阅的领域事件**，多一个 `loop_id` 维度用于按 Loop 回溯，时间列名为 **`published_at`**（发布时刻）而非 `created_at`。

```sql
CREATE TABLE domain_events (
    event_id TEXT PRIMARY KEY,
    event_type TEXT NOT NULL,
    trace_id TEXT,
    loop_id TEXT,                          -- 领域事件所属 Loop（events 表无此列）
    source TEXT NOT NULL,
    payload TEXT NOT NULL,                 -- JSON
    priority TEXT NOT NULL DEFAULT 'normal',
    published_at INTEGER NOT NULL          -- epoch ms；本表无 created_at
);

CREATE INDEX idx_de_type ON domain_events(event_type);
CREATE INDEX idx_de_trace ON domain_events(trace_id);
CREATE INDEX idx_de_loop ON domain_events(loop_id);
```

> §1.1 时间列约定对本表的 `published_at` 同样适用（`INTEGER` epoch ms），仅命名不同；命名豁免清单另见 Docs/15。

---

## 4. 记忆域 Schema（实际注册于 `civitas_main.db`）

> **2026-09-22 校准**：本节两张表**不在** `civitas_memory.db`，而是注册在 **main** 库（`migrations.ts:295` / `:315`，均 `database: 'main'`）；表名与列名以迁移代码为准逐字更正。迁往 `civitas_memory.db` 属目标态，列为待办迁移（见 §1）。
>
> **接线状态**：两张**表已建**，但服务层 `Src/Services/SharedMemory/globalWorkspace.ts:19`（`entries` Map）与 `longTermMemory.ts:36`（`memories` Map）**仍为进程内内存态**，整个 `SharedMemory` 目录**无一处 `Infra/Db` 导入**，即当前**没有 repository 读写这两张表**，重启后记忆丢失。落表接线列为待办迁移。

### 4.1 全局工作区表（global_workspace）

*迁移登记：`migrations.ts:295-314`（`version: 16`, `database: 'main'`）。原名 `workspace_entries` 与列结构（`content_type` 单列语义、`semantic_vector`）均已按代码更正。*

```sql
CREATE TABLE global_workspace (
    entry_id TEXT PRIMARY KEY,
    key TEXT NOT NULL,                     -- 工作区键（原设计以 entry_id 兼作键，代码改为独立 key 列）
    trace_id TEXT NOT NULL,
    agent_id TEXT NOT NULL,
    task_id TEXT,

    content TEXT NOT NULL,
    content_type TEXT NOT NULL,            -- fact/decision/verdict/contract/status
    assertion TEXT NOT NULL DEFAULT 'observed',   -- observed/inferred/assumed（AssertionLevel，eventTypes.ts:152）

    metadata_json TEXT NOT NULL DEFAULT '{}',     -- JSON（原列名 metadata → metadata_json，对齐 §1.1）
    version INTEGER NOT NULL DEFAULT 1,           -- 乐观锁版本
    last_modified_by TEXT NOT NULL,               -- 最近写入者 agentId
    conflict_strategy TEXT NOT NULL DEFAULT 'lww',-- lww/authority/arbitrate（versionedEntry.ts:14）
    causal_tokens_json TEXT NOT NULL DEFAULT '[]',-- JSON 数组，因果向量时钟 token

    status TEXT NOT NULL DEFAULT 'active', -- active/superseded/discarded
    superseded_by TEXT,

    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL
);

CREATE INDEX idx_gw_key ON global_workspace(key);
CREATE INDEX idx_gw_trace ON global_workspace(trace_id);
CREATE INDEX idx_gw_agent ON global_workspace(agent_id);
```

- ❌ **`semantic_vector` 列不存在**：代码未持久化任何向量（`Src/Services/SharedMemory/**` 与 `Src/Services/Retrieval/**` 均无 embedding 引用），检索当前为**关键词打分**实现（`Src/Services/Retrieval/reranker.ts:35`），向量能力尚未落地。向量持久化列为待办。
- 本文 §8.3 旧条"新增 `version / last_modified_by / causal_tokens`"三列**已落表**（`version` / `last_modified_by` / `causal_tokens_json`），无需重复新增。

### 4.2 长期记忆表（long_term_memory）

*迁移登记：`migrations.ts:315-331`（`version: 17`, `database: 'main'`）。表名为**单数** `long_term_memory`，非 `long_term_memories`。*

```sql
CREATE TABLE long_term_memory (
    memory_id TEXT PRIMARY KEY,

    title TEXT NOT NULL,
    content TEXT NOT NULL,
    category TEXT NOT NULL,                -- rule/fact/decision/pattern

    -- 来源追溯（JSON 列以 _json 结尾，对齐 §1.1）
    source_trace_ids_json TEXT NOT NULL DEFAULT '[]',
    source_arbitration_ids_json TEXT,      -- JSON 数组；代码中可空

    assertion TEXT NOT NULL DEFAULT 'observed',

    -- 有效性
    status TEXT NOT NULL DEFAULT 'active', -- active/deprecated/contradicted
    contradicted_by TEXT,

    -- 元数据
    created_at INTEGER NOT NULL,
    last_accessed_at INTEGER NOT NULL,
    access_count INTEGER NOT NULL DEFAULT 0
);

CREATE INDEX idx_ltm_category ON long_term_memory(category);
CREATE INDEX idx_ltm_status ON long_term_memory(status);
```

- ❌ **`semantic_vector` 列不存在**：同 §4.1，向量未持久化。
- 注：`source_trace_ids_json` 为 `NOT NULL DEFAULT '[]'`，而 `source_arbitration_ids_json` 允许 `NULL`——两列可空性不一致（代码现状，非笔误），统一与否列为待办裁决。

---

## 5. 数据生命周期

### 5.1 保留策略

> 下表的天数为**默认值**，实际生效值由 `Configs/session.json → retention.*` 提供（键名清单见本文 §8.4 的"配置键"列与 Docs/11 §2.10）；本表与 §8.4 冲突时以 §8.4 为准。

| 数据类型 | 保留策略 | 清理方式 |
|---------|---------|---------|
| 活跃任务数据 | 任务完成后保留 30 天 | 归档到 `Data/archive/` |
| Agent 数据 | Agent 销毁后保留 90 天 | 归档 |
| Token 交易记录 | 永久保留（审计要求） | 不清理，按月分文件 |
| 操作历史 | 保留 180 天 | 归档 |
| 事件记录 | 保留 90 天 | 按月压缩归档 |
| 隔离层数据 | Agent 销毁即清理 | 直接删除 |
| 长期记忆 | 永久保留 | 标记 deprecated 但不删除 |

### 5.2 归档策略

```
归档触发：每日定时任务
归档流程：
[1] 扫描超过保留期限的数据
[2] 导出为 JSONL 文件 → Data/archive/{type}/{year-month}.jsonl
[3] 从主数据库删除已归档数据
[4] 记录归档操作到归档日志
```

### 5.3 数据库维护

| 维护操作 | 频率 | 说明 |
|---------|------|------|
| VACUUM | 每周 | 回收已删除数据占用的空间 |
| 完整性检查 | 每日 | PRAGMA integrity_check |
| 索引重建 | 每月 | REINDEX |
| 备份 | 每日 | 复制 .db 文件到 Data/backup/ |

---

## 6. 迁移策略

> **2026-09-22 校准**：本节原写"单 `version: 1` 全量建表 + 失败自动回滚"，与 `Src/Infra/Db/migrations.ts` 实际契约不符，现按代码更正。

**契约（`migrations.ts:20-26`）：**

```typescript
export interface Migration {
  readonly version: number;
  readonly name: string;                   // 如 'create_sessions'（非 description）
  readonly up: string;                     // SQL 语句（可多语句：建表 + 建索引）
  readonly down: string;                   // 回滚 SQL —— 必填，非可选
  readonly database?: 'main' | 'events' | 'memory';  // 省略时默认 main（:451-452）
}
```

**登记方式：按表逐版本注册**，不存在"一次性全量建表"的 version 1：

```typescript
registerMigration({ version: 1, name: 'create_sessions', database: 'main',
  up: `CREATE TABLE IF NOT EXISTS sessions (...); CREATE INDEX ...;`,
  down: `DROP TABLE IF EXISTS sessions;` });
// 每张表一条：main v1–v19、events v1–v3；registerMigration 入栈后按 version 升序排序（:42-45）
```

- `registerDefaultMigrations()` 幂等：内部 `defaultsRegistered` 标志保证重复调用只注册一次（`:50-52`）。
- `initMigrations()` 在 **main / events / memory 三库各建一份 `_migrations`**（`version INTEGER PRIMARY KEY, name TEXT, applied_at TEXT`，`:350-359`）。
  → **版本号按库独立**：`main` 的 v1 是 `sessions`，`events` 的 v1 是 `events`，两者互不冲突。
- `migrateUp()`：遍历全部迁移 → 按 `database` 解析目标连接 → `isApplied(db, version)` 未应用则 `db.exec(up)` 并写入该库 `_migrations`，返回本次应用条数（`:370-388`）。
- `migrateDown()`：仅从 **main** 库 `_migrations` 取 `MAX(version)` 那一条回滚（`:393-408`）→ 目标库执行 `down` → 删除该库版本记录。
  ⚠️ 因 `migrations.find(m => m.version === last.version)` 不区分库，当 events 侧存在同号版本时可能命中错误迁移，跨库回滚列为待办加固项。
- 查询 API：`getAppliedMigrations(db)` / `getCurrentVersion(db)`（默认 `'main'`）；测试用 `clearMigrations()` 重置内部数组与幂等标志。

**迁移执行：**
- 系统启动 ⑥ 步后调用 `initMigrations()` → `migrateUp()`（`Src/main.ts:104-107`）；
- 失败 → 抛错**终止启动**（`main.ts:105/108`），返回 `err(..., 'FATAL')`；
- **不会自动回滚**：`up` 未包裹事务，失败时已执行的前序语句保留原样，需人工介入或显式调用 `migrateDown()`（原"迁移失败 → 回滚"表述作废）。

---

## 7. 依赖关系

| 依赖组件 | 来源 | 用途 |
|---------|------|------|
| `better-sqlite3` | npm 包 | SQLite 驱动（同步 API，性能优） |
| `Logger` | Infra | 迁移日志、维护日志 |
| `ConfigLoader` | Infra | 读取保留策略配置 |

---

## 8. v2 补丁：新增表（对齐 Loop Control 与 Durable Execution）

> **修订依据**：`Docs/99-审查记录` §5.13；语义定义见 Docs/12（循环控制）与 Docs/13（持久执行）。本文档**仅列 schema**，其他文档不得复制以下 DDL。

### 8.1 表清单补充（v2）

| 表 | 库 | 迁移登记 | 作用 | 写入频率 |
|----|----|---------|------|---------|
| `loops` | civitas_main | v8 | LoopState 持久化 | 每轮末尾 |
| `loop_checkpoints` | civitas_main | v9 | 快照 | 每轮 + 副作用后 |
| `pending_approvals` | civitas_main | v15 | 人工审批队列 | 命中审批门时 |
| `verifier_results` | civitas_main | v12 | L1/L2/L3/L4 结果 | 每次验收 |
| `failure_feedbacks` | civitas_main | v13 | 结构化失败反馈 | 失败时 |
| `effect_journal` | **civitas_main** | v10 | 副作用意图日志 | **写前 + 写后两次** |
| `idempotency_cache` | civitas_main | v11 | 幂等结果缓存 | 幂等工具首次 |
| `action_fingerprints` | civitas_events | v2 | 已执行工具指纹 | 每轮 |
| `strategies_ledger` | civitas_main | v14 | 已试策略账本 | 每轮 |

> **2026-09-22 校准**：`effect_journal` 原列 `civitas_events`，实际注册于 **main** 库（`migrations.ts:167` 显式 `database: 'main'`，见 §1）；其余 8 表库归属与迁移登记均与代码一致。版本号按库独立计数（见 §6）。

### 8.2 完整 DDL

```sql
-- 1) 循环总控
CREATE TABLE loops (
  loop_id TEXT PRIMARY KEY,
  trace_id TEXT NOT NULL,
  session_key TEXT NOT NULL,
  agent_id TEXT NOT NULL,
  task_id TEXT NOT NULL,
  parent_agent_id TEXT,
  goal_json TEXT NOT NULL,             -- 不可变
  state_json TEXT NOT NULL,            -- 可变，每次 checkpoint 覆盖
  iteration INTEGER NOT NULL DEFAULT 0,
  phase TEXT NOT NULL,                 -- planning|acting|verifying|awaiting_approval|completed|failed
  budget_used_tokens INTEGER NOT NULL DEFAULT 0,
  budget_used_usd REAL NOT NULL DEFAULT 0,
  last_checkpoint_id TEXT,
  stopped_reason TEXT,                 -- success|limits|budget_soft|budget_hard|no_progress|risk|human_abort（与 Docs/12 §2 五类退出同名）
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
);
CREATE INDEX idx_loops_active ON loops(phase, updated_at)
  WHERE phase NOT IN ('completed','failed');
CREATE INDEX idx_loops_trace ON loops(trace_id);

-- 2) Checkpoint
CREATE TABLE loop_checkpoints (
  checkpoint_id TEXT PRIMARY KEY,
  loop_id TEXT NOT NULL,
  iteration INTEGER NOT NULL,
  state_snapshot_json TEXT NOT NULL,
  artifact_manifest_json TEXT NOT NULL,   -- [{path,hash,sizeBytes}]
  pending_effects_json TEXT NOT NULL,     -- 写时未完成的副作用 id
  next_step_hint TEXT,                    -- Agent 自评下一步（辅助恢复；Docs/13 §5.2，v2.1 补列）
  created_at INTEGER NOT NULL,
  FOREIGN KEY (loop_id) REFERENCES loops(loop_id) ON DELETE CASCADE
);
CREATE INDEX idx_checkpoints_loop_iter ON loop_checkpoints(loop_id, iteration DESC);

-- 3) 审批
CREATE TABLE pending_approvals (
  approval_id TEXT PRIMARY KEY,
  loop_id TEXT NOT NULL,
  trace_id TEXT NOT NULL,
  iteration INTEGER NOT NULL,            -- 发起时的轮次（v2.1 补，对齐 Docs/12 §6.2）
  requested_by TEXT NOT NULL,
  kind TEXT NOT NULL,                 -- tool_execute|budget_expand|irreversible_action|judge_override
  payload_json TEXT NOT NULL,
  risk_level TEXT NOT NULL,           -- LOW|MEDIUM|HIGH|CRITICAL
  requested_at INTEGER NOT NULL,
  timeout_sec INTEGER NOT NULL,       -- 生效超时快照（L4 覆盖值或 supervision.approvalTimeoutSec），防配置热更新突变存量审批
  expires_at INTEGER NOT NULL,        -- = requested_at + timeout_sec * 1000
  status TEXT NOT NULL DEFAULT 'PENDING',   -- PENDING|APPROVED|REJECTED|TIMEOUT
  deciders_json TEXT NOT NULL,        -- [{role,weight}]
  decision_policy TEXT NOT NULL DEFAULT 'majority',  -- majority|unanimous（CRITICAL 强制 unanimous）
  decided_by_json TEXT,               -- [role 或 agentId]，v2.1 由 decided_by 改名（JSON 列必以 _json 结尾）
  decided_at INTEGER,
  decision_reason TEXT,
  default_on_timeout TEXT NOT NULL DEFAULT 'reject',  -- reject|abort_loop；禁止默认通过
  FOREIGN KEY (loop_id) REFERENCES loops(loop_id) ON DELETE CASCADE
);
CREATE INDEX idx_approvals_pending ON pending_approvals(status, expires_at);

-- 4) Verifier
CREATE TABLE verifier_results (
  result_id TEXT PRIMARY KEY,
  loop_id TEXT NOT NULL,
  iteration INTEGER NOT NULL,
  level TEXT NOT NULL,                -- L1|L2|L3|L4
  verifier_kind TEXT NOT NULL,        -- test|schema|numeric|rule|golden|llm_judge|human_gate
  pass INTEGER NOT NULL,
  evidence_json TEXT NOT NULL,
  defect_category TEXT,               -- spec_missing|logic_error|quality_low|risk_violation
  cost_tokens INTEGER,
  duration_ms INTEGER,
  judge_model TEXT,                   -- L3 时记录哪个模型判定，供校准
  created_at INTEGER NOT NULL,
  FOREIGN KEY (loop_id) REFERENCES loops(loop_id) ON DELETE CASCADE
);
CREATE INDEX idx_verifier_loop ON verifier_results(loop_id, iteration);

-- 5) FailureFeedback
CREATE TABLE failure_feedbacks (
  feedback_id TEXT PRIMARY KEY,
  loop_id TEXT NOT NULL,
  iteration INTEGER NOT NULL,
  category TEXT NOT NULL,             -- schema|logic|external|timeout|risk|no_progress|budget_soft|budget_hard
  evidence_json TEXT NOT NULL,
  compared_with_last_json TEXT,
  tried_strategies_json TEXT NOT NULL,
  remaining_budget_json TEXT NOT NULL,
  recommended_next_action TEXT,
  created_at INTEGER NOT NULL,
  FOREIGN KEY (loop_id) REFERENCES loops(loop_id) ON DELETE CASCADE
);

-- 6) Effect Journal（强一致，写前写入，写后更新）—— main 库 v10，migrations.ts:167
--    唯一被服务层实际读写的持久执行表：Src/Infra/DurableExecution/effectJournal.ts 经 getMainDb() 读写
CREATE TABLE effect_journal (
  effect_id TEXT PRIMARY KEY,           -- = operation_id
  loop_id TEXT NOT NULL,
  iteration INTEGER NOT NULL,
  kind TEXT NOT NULL,                   -- tool_execute|file_write|http_call|db_write|agent_message_send|token_debit|external_api
  idempotency_key TEXT NOT NULL,
  payload_hash TEXT NOT NULL,
  status TEXT NOT NULL,                 -- INTENT|EXECUTING|SUCCEEDED|FAILED|UNKNOWN
  result_json TEXT,
  error_class TEXT,                     -- retryable|non_retryable|unknown
  started_at INTEGER NOT NULL,
  timeout_ms INTEGER NOT NULL,          -- 生效超时快照；默认 durable.effect.defaultTimeoutMs（Docs/13 §4.1，v2.1 补列）
  completed_at INTEGER,
  UNIQUE(loop_id, idempotency_key)
);
CREATE INDEX idx_effect_status ON effect_journal(status, started_at);
CREATE INDEX idx_effect_loop ON effect_journal(loop_id, iteration);

-- 7) 幂等缓存
CREATE TABLE idempotency_cache (
  idem_key TEXT PRIMARY KEY,
  loop_id TEXT NOT NULL,
  result_json TEXT NOT NULL,
  result_hash TEXT NOT NULL,
  created_at INTEGER NOT NULL,
  expires_at INTEGER NOT NULL
);
CREATE INDEX idx_idem_expiry ON idempotency_cache(expires_at);

-- 8) 已执行行为指纹（建在 **events** 库，migrations.ts:245-255）
CREATE TABLE action_fingerprints (
  fingerprint TEXT NOT NULL,          -- SHA-256(toolName + canonicalJson(args))
  loop_id TEXT NOT NULL,
  iteration INTEGER NOT NULL,
  tool_name TEXT NOT NULL,
  args_hash TEXT NOT NULL,
  count_in_iteration INTEGER NOT NULL DEFAULT 1,
  recorded_at INTEGER NOT NULL,       -- epoch ms；与 LoopState.actionFingerprints[].recordedAt 同名
  PRIMARY KEY (fingerprint, loop_id, iteration)
  --（迁移未建 FK，待办：migrations.ts:245-255 无 FOREIGN KEY 子句。
  --   本表在 events 库、loops 在 main 库，跨库外键 SQLite 不支持 → 须改应用层校验，不能照本行原样补建）
);
CREATE INDEX idx_fp_loop ON action_fingerprints(loop_id, iteration);

-- 9) 策略账本
CREATE TABLE strategies_ledger (
  entry_id TEXT PRIMARY KEY,
  loop_id TEXT NOT NULL,
  iteration INTEGER NOT NULL,
  strategy TEXT NOT NULL,                -- ≤ 30 字，强制上限
  expected_outcome TEXT NOT NULL,
  actual_outcome TEXT,
  failure_reason TEXT,
  created_at INTEGER NOT NULL,
  FOREIGN KEY (loop_id) REFERENCES loops(loop_id) ON DELETE CASCADE
);
```

### 8.3 与旧表的关系

> **2026-09-22 校准**：按 `migrations.ts` 实际状态更正条目命名与落库情况。

- `tasks` / `subtasks` / `agents` / `token_wallets` / `token_transactions` 保留不变（迁移 v3/v4/v2/v5/v6，与 §2 一致；仅索引有缺，见各节小注）；
- `sessions` 与新增 `loops` 的关系：**1 session : N loops**（一个会话内可能启动多个 Agent）；
- ⚠️ `arbitration_cases` 新增 `loop_ids` 字段（多对多，JSON 数组）——**双重未落库**：`arbitration_cases` 表本身不存在于迁移（见 §2.7 标注），`loop_ids` 亦无从落地，整体列为待办迁移；
- ⚠️ `global_workspace`（§4.1，原名 `workspace_entries`）**不再需要"新增 `version / last_modified_by / causal_tokens`"**：三列已随建表落库，实际列名为 `version` / `last_modified_by` / `causal_tokens_json`（`migrations.ts:302-305`，对齐 Docs/07 §8.2）；
- `events` 库保留为 **仅追加事实日志**，不作为恢复入口（仅审计）；但 `events` / `domain_events` 两表**当前无写入方**（§3 接线状态），且 `effect_journal` **不在** events 库而在 main 库（§1 / §8.1）；
- 新增 `chat_sessions` / `chat_messages`（main v18/v19，`migrations.ts:270-292`，Docs/16）：`chat_messages.session_id` 外键级联至 `chat_sessions`，与 §2.8 的 `sessions`（编排域会话键）**不同源、不同表**，二者关系尚未定义，归并与否列为待办裁决。

### 8.4 保留策略补充

| 数据 | 保留 | 清理方式 | 配置键（`Configs/session.json → retention.*`） |
|------|------|---------|------|
| `loops` | 完成后 90 天 | 归档 JSONL | `retention.loopsCompletedDays: 90` |
| `loop_checkpoints` | 只保留最后 20 个 + 里程碑 | 每 24h 整理 | `durable.checkpoint.keepLastCount: 20`（Docs/11 §2.9） |
| `pending_approvals` | 已完成 30 天 | 删除 | `retention.pendingApprovalsDoneDays: 30` |
| `effect_journal` | **至少 180 天**（安全） | 归档不删 | `durable.effect.journalRetentionDays: 180` |
| `idempotency_cache` | 24h | 自动过期 | `durable.idempotency.cacheTtlHour: 24` |
| `verifier_results` | 90 天 | 删除（Rubric 校准集另存） | `retention.verifierResultsDays: 90` |
| `strategies_ledger` | 与 loop 同生命周期 | 级联 | —（不单独配置） |
| `failure_feedbacks` | 90 天 | 归档 | `retention.failureFeedbacksDays: 90`（v2.1 补，原表遗漏） |
| `action_fingerprints` | 90 天 | 归档 | `retention.actionFingerprintsDays: 90`（v2.1 补，原表遗漏） |

> 配置键名与 Docs/11 §2.9 / §2.10 **逐字对齐**；本表只引用不定义，改数值请改 Docs/11。

### 8.5 事务与持久化约束

- `effect_journal` 写入使用 `PRAGMA synchronous = FULL`；
- 其他表默认 `NORMAL`；
- `loops + loop_checkpoints + effect_journal` 写入 **必须同事务**，否则恢复时状态不一致；
- 写入失败重试 1 次，仍失败 → **上报 FATAL 并中止 Loop**（不允许默默丢）。

> **实现状态（2026-09-22 校准）**：前两条与第三条**均未落地**。
> - 连接初始化对三库统一设 `synchronous = NORMAL`（`Src/Infra/Db/database.ts:140`），**没有**针对 `effect_journal` 的 `FULL` 设置（`Src/Infra/DurableExecution/effectJournal.ts:7` 仅注释声称）；
> - `createCheckpoint()` 的 `db.transaction()` **只包裹 `loop_checkpoints` 单表插入**（`checkpointStore.ts:53-69`），未与 `loops` / `effect_journal` 同事务；
> - 上述差异列为待办加固项，语义要求见 Docs/13。

---

## 9. 修订历史

| 版本 | 日期 | 主要变化 |
|------|------|---------|
| v1 | 2026-09-13 | 初稿：三库划分与主业务 Schema |
| v2 | 2026-09-13 | 新增 §8（9 张 Loop Control / Durable Execution 表 + 事务约束） |
| v2.1 | 2026-09-13 | 参数对齐：确立 §1.1 DDL 全局约定、`decided_by → decided_by_json`、`timeout_ms` 统一、`quality_score REAL` 0–1、补 §8.4 保留策略配置键 |
| **v2.1.1** | **2026-09-22** | **文档-代码对账校准**（逐条核对 `Src/Infra/Db/migrations.ts`）：① §1 库职责更正——`civitas_memory.db` 为空壳（仅 `_migrations`，无域表）、`effect_journal` 实属 **main** 库、`events` 库含 3 表；② §2.6/§2.7/§2.9/§2.10 四表标注 **⚠️ 未落库**（迁移无建表，数据为内存态/纯设计态）并列待办迁移，DDL 作目标态保留；③ §4.1 `workspace_entries → global_workspace`、§4.2 `long_term_memories → long_term_memory`，库归属 memory → main，列结构逐字对齐并删除不存在的 `semantic_vector`，补"表已建但服务层仍内存态"接线说明；④ 新增 §3.2 `domain_events` DDL（含 `loop_id` / `published_at`）；⑤ §6 迁移契约更正为 `{ version; name; up; down(必填); database? }` 按表逐版本注册、`_migrations` 三库各一份、失败不回滚；⑥ §8.1 `effect_journal` 库归属改 main 并补迁移版本号；⑦ §8.3 同步 `arbitration_cases.loop_ids` 双重未落库、`global_workspace` 三列已落表、登记 `chat_sessions`/`chat_messages`；⑧ 补索引缺项小注（`idx_tasks_session`、`idx_subtasks_agent`、`idx_agents_role`、`idx_tx_trace`、`idx_tx_created`、`idx_events_created`）与 §8.2 `action_fingerprints` 跨库外键未建说明 |
