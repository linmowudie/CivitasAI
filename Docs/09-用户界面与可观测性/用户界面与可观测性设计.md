# 用户界面与可观测性设计

> 本文档定义 Civitas-AI 的前端界面（React 城邦大屏 + 用户交互）和可观测性方案。所有可视化必须具备现实业务意义，能够解释实际问题或支持决策。
>
> **实现现状与改造路线**：见 `Docs/16-前端改造基础/`。本文 §4 选型与 §5 目录已于 2026-09-14 根据实际代码核查修订；§3.2 事件名已与 `Docs/07 §4.3` 注册表对齐。

---

## 1. 界面总体结构

```
┌─────────────────────────────────────────────────────────┐
│  顶部导航栏：系统名称 | 当前任务 | 全局 Token 余额       │
├────────────┬────────────────────────────────────────────┤
│            │                                            │
│  左侧导航  │           主内容区                          │
│            │                                            │
│  · 任务面板 │  ┌────────────────────────────────────┐   │
│  · Agent   │  │                                    │   │
│    监控    │  │     根据左侧选择切换视图：            │   │
│  · Token   │  │     - 城邦大屏（总览）               │   │
│    账本    │  │     - 任务详情                       │   │
│  · 仲裁    │  │     - Agent 详情                     │   │
│    案件    │  │     - Token 流向                     │   │
│  · 系统    │  │     - 仲裁时间线                     │   │
│    配置    │  │                                    │   │
│            │  └────────────────────────────────────┘   │
│            │                                            │
│            │  ┌────────────────────────────────────┐   │
│            │  │     底部：实时事件流（滚动）          │   │
│            │  └────────────────────────────────────┘   │
├────────────┴────────────────────────────────────────────┤
│  底部状态栏：系统状态 | 活跃 Agent 数 | 当前税率          │
└─────────────────────────────────────────────────────────┘
```

---

## 2. 视图模块

### 2.1 城邦大屏（Dashboard）

> 核心原则：每个可视化元素必须解释实际问题或支持决策，禁止仅为展示而生成无实质价值的图表。

| 区域 | 可视化内容 | 业务意义 |
|------|-----------|---------|
| 系统状态卡片 | 活跃任务数、活跃 Agent 数、系统 Token 总量 | 快速了解系统整体运行状态 |
| Agent 活跃状态图 | 力导向图：节点=Agent，边=通信关系，颜色=状态 | 直观展示当前协作拓扑和 Agent 健康状态 |
| Token 流向桑基图 | 桑基图：系统池→Agent 钱包→消耗/税收/分润 | 理解 Token 的分配和消耗路径 |
| 任务进度时间线 | 甘特图：各任务的子任务执行时间线 | 追踪任务执行进度和瓶颈 |
| 仲裁案件状态 | 卡片列表：进行中的仲裁案件 + 状态 | 监控仲裁效率和积压情况 |
| 异常告警 | 告警列表：最近的异常事件 | 快速发现和响应系统问题 |

### 2.2 任务面板（TaskPanel）

| 组件 | 功能 |
|------|------|
| 任务提交区 | 文本输入框 + 提交按钮，支持流式输出预览 |
| 任务列表 | 所有任务的状态（进行中/已完成/失败） |
| 任务详情 | 子任务拆解视图、各 Worker 进度、质量评分 |
| 流式输出 | WebSocket 实时推送 Agent 的推理过程 |

### 2.3 Agent 监控（AgentMonitor）

| 组件 | 功能 |
|------|------|
| Agent 列表 | 所有 Agent 的状态、角色、Token 余额 |
| Agent 详情 | 执行历史、Token 消耗曲线、操作日志 |
| Agent 拓扑图 | 当前协作关系的可视化（Director-Partner-Worker） |

### 2.4 Token 账本（TokenLedger）

| 组件 | 功能 |
|------|------|
| 钱包总览 | 系统池余额、各 Agent 钱包余额 |
| 交易流水 | 按时间排序的交易记录，支持筛选 |
| 消耗统计 | 按 Agent/模型/任务维度的消耗柱状图 |
| 税率趋势 | 税率随时间变化的折线图 |

### 2.5 仲裁视图（ArbitrationView）

| 组件 | 功能 |
|------|------|
| 案件列表 | 所有仲裁案件的状态和结果 |
| 案件详情 | 证据对比、裁决过程、恢复状态 |
| 仲裁者池状态 | 当前仲裁者数量、负载 |

---

## 3. 实时通信方案

### 3.1 WebSocket 连接管理

```typescript
// 前端 WebSocket 服务
class WebSocketService {
  private ws: WebSocket;
  private reconnectAttempts = 0;
  private maxReconnectAttempts = 5;
  
  // 连接
  connect(url: string): void;
  
  // 发送消息
  send(message: ClientMessage): void;
  
  // 订阅消息类型
  on(type: string, handler: (data: any) => void): void;
  
  // 断线重连（指数退避）
  reconnect(): void;
}
```

### 3.2 实时推送事件

> **本节不维护数值**（SSOT 裁决）。下表的频率均由 `Configs/default.json → ui.*` 提供（Docs/11 §2.1）；事件名引用 `Docs/07 §4.3` 注册表，**前端不得自接新事件字符串**。

| 事件 | 推送方式 | 节流配置键 | 数据量 |
|------|---------|-----------|--------|
| 任务进度更新 | WS 推（`task:progress`） | 状态变化即推，无节流 | 小 |
| Agent 状态变化 | WS 推（`agent:*` 域） | 同上 | 小 |
| Token 消耗 | WS 推（`token:consumed`） | 同上 | 小 |
| 流式输出 | WS 推（`agent:stream_chunk`，收尾 `agent:stream_end`） | `ui.streamFlushIntervalMs`（默认 100ms 批量合并，**禁止逐 token 推送**） | 中 |
| 会话消息 | WS 推（`agent:chat_message`）+ REST 补齐 | 状态变化即推 | 小 |
| 仲裁状态 | WS 推（`arbitration:` 域） | 状态变化即推 | 中 |
| 大屏概览 | HTTP 轮询 | `ui.dashboardPollIntervalSec`（默认 5 秒） | 中 |
| 审批队列 | HTTP 轮询 + `loop:approval_requested` 推 | `ui.approvalQueueRefreshSec`（默认 3 秒） | 小 |

**前端约束**：
- 本地事件缓冲上限 = `ui.maxEventBufferSize`，超限丢弃最旧项（**不阻断主流程**）；
- WS 断开重连：退避基数 `ui.wsReconnectBackoffMs`，最多 `ui.wsReconnectMaxAttempts` 次，耗尽后降级为纯轮询并显示离线横幅；
- 展示层的百分比均 ×100 渲染，内部存储一律 0.0–1.0（Docs/10 §1.1）。

---

## 4. 技术选型

> **2026-09-14 修订**（依据 `Docs/16 §3` 裁决）：原表为设计意图，现按实际代码与改造决策重定，并补「现状」列区分已落地 / 待引入。

| 组件 | 选择 | 现状 | 理由 / 修订说明 |
|------|------|------|----------------|
| 前端框架 | React 19 + TypeScript + Vite | ✅ 已落地 | 原定 React 18+，实际已用 19，兼容 |
| 样式体系 | **Tailwind 4 + 自绘组件** | ✅ 已落地 | ✏️ 修订：原选 Ant Design。现 `index.css` 已形成整套暗色驾驶舱设计语言且 8 个视图均基于它，引入 antd 会双轨打架 |
| 状态管理 | Zustand | ⏳ 待引入（F1） | 现为组件内 `useState` 散落；F1 建 `stores/` 后为唯一状态源 |
| 可视化库 | **ECharts 5**（主）+ D3（按需） | ⏳ 待引入（F4） | ✏️ 修订：现实际为纯 CSS 条形图，且 `recharts` 已装未用（F1 移除）；桑基/甘特/关系图 ECharts 原生支持 |
| WebSocket | 前端原生 API + 后端 `ws` 包 | ⏳ 后端待实现（F0.2） | 后端现为内存事件桥，未绑端口；`ws` 为全项目唯一新增 runtime 依赖，已登记于 Docs/11 §5《依赖关系》表 |
| HTTP 客户端 | **原生 `fetch`** | ⏳ 待引入（F1） | ✏️ 修订：原选 axios。前端无拦截器需求，少一个依赖 |

---

## 5. 前端目录结构

```
Client/
├── src/
│   ├── components/
│   │   ├── Layout/                    # 布局组件
│   │   │   ├── AppLayout.tsx
│   │   │   ├── Sidebar.tsx
│   │   │   └── StatusBar.tsx
│   │   ├── Dashboard/                 # 城邦大屏
│   │   │   ├── DashboardView.tsx
│   │   │   ├── SystemStatusCards.tsx
│   │   │   ├── AgentTopologyGraph.tsx
│   │   │   ├── TokenFlowSankey.tsx
│   │   │   ├── TaskTimeline.tsx
│   │   │   └── AlertList.tsx
│   │   ├── TaskPanel/                 # 任务面板
│   │   │   ├── TaskPanelView.tsx
│   │   │   ├── TaskSubmitForm.tsx
│   │   │   ├── TaskList.tsx
│   │   │   ├── TaskDetail.tsx
│   │   │   └── StreamOutput.tsx
│   │   ├── AgentMonitor/              # Agent 监控
│   │   │   ├── AgentMonitorView.tsx
│   │   │   ├── AgentList.tsx
│   │   │   └── AgentDetail.tsx
│   │   ├── TokenLedger/               # Token 账本
│   │   │   ├── TokenLedgerView.tsx
│   │   │   ├── WalletOverview.tsx
│   │   │   ├── TransactionList.tsx
│   │   │   └── ConsumptionChart.tsx
│   │   ├── ArbitrationView/           # 仲裁视图
│   │   │   ├── ArbitrationView.tsx
│   │   │   ├── CaseList.tsx
│   │   │   └── CaseDetail.tsx
│   │   └── SystemConfig/              # 系统配置
│   │       ├── ConfigView.tsx
│   │       └── ConfigForm.tsx
│   ├── hooks/
│   │   ├── useWebSocket.ts
│   │   ├── useTaskStream.ts
│   │   └── useDashboardData.ts
│   ├── services/
│   │   ├── api.ts                     # REST API 封装
│   │   └── ws.ts                      # WebSocket 服务
│   ├── stores/
│   │   ├── taskStore.ts
│   │   ├── agentStore.ts
│   │   ├── tokenStore.ts
│   │   └── dashboardStore.ts
│   ├── App.tsx
│   └── main.tsx
├── package.json
├── vite.config.ts
└── tsconfig.json
```

> **实现现状差异（2026-09-14 核查）**：
> - 实际为 `Client/src/views/*.tsx`（8 个单体视图）+ 空的 `components/` 子目录，与上述「视图即组件目录」的结构不一致；
> - `hooks/` / `services/` / `stores/` **均未创建**（F1 补齐）；
> - 需新增 `Client/src/shared/eventTypes.ts`，由 `Scripts/genEventTypes.ts` 从后端 `eventTypes.ts` 构建期生成，**前端不得自写事件名字符串**；
> - 实际额外存在 ChatView / ApprovalQueue / LoopDebugger / TraceReplay 四个视图（未列入本目录树，与 S13 的 approvalApi / loopApi 契约对应）；
> - `data/mockData.ts` 仅作为 `?mock=1` 展示与单测用途保留，不得作为默认数据源。

**前端分层约束（与五层单向依赖同构）**：`views → hooks → stores → services`，禁止反向与跨层直连；视图层不得出现 `fetch` / `new WebSocket`。

---

## 6. 可观测性设计

### 6.1 日志追踪

| 维度 | 实现 | 说明 |
|------|------|------|
| trace_id | 贯穿所有 Agent 调用 | 一个任务从开始到结束的全链路 |
| span | 每次 LLM 调用、工具执行 | 记录耗时、Token 消耗 |
| 关联 | operation_id 关联具体操作 | 工具调用、文件操作 |

### 6.2 监控指标

| 指标类别 | 具体指标 | 采集方式 |
|---------|---------|---------|
| 系统健康 | 活跃 Agent 数、任务队列长度、内存使用 | 定时采集 |
| 性能 | 任务平均完成时间、LLM 调用延迟 P50/P95 | 事件驱动 |
| 成本 | 总 Token 消耗、每任务平均成本、税率 | 交易驱动 |
| 质量 | 任务成功率、Agent 开除率、仲裁案件数 | 状态变化驱动 |

### 6.3 告警规则

| 告警 | 条件 | 级别 |
|------|------|------|
| Token 异常消耗 | 单 Agent 5 分钟消耗超预算 | Critical |
| 任务积压 | 任务队列长度 > 10 | Warning |
| Agent 高失败率 | 近 10 次任务失败率 > 50% | Warning |
| 仲裁积压 | 待处理仲裁案件 > 5 | Warning |
| 系统 Token 不足 | 系统池余额 < 10000 | Critical |

---

## 7. Phase 规划

| Phase | 前端范围 |
|-------|---------|
| Phase 1 | 任务提交 + 流式输出 + 基础 Agent 列表 |
| Phase 2 | Token 账本 + 仲裁视图 + Agent 详情 |
| Phase 3 | 城邦大屏（全量可视化）+ 系统配置 |

> ⚠️ 截至 v0.1.0-S14，上述 Phase 1 仍未达到可用状态（前端为 mock 驱动静态原型，后端接口层未绑定端口）。
> 实际执行顺序以 `Docs/16-前端改造基础/前端改造方案与构建顺序.md` 的 **F0–F6** 为准：F0 先补传输层与契约缺口，再逐层向前端推进。

---

## 8. 依赖关系

| 依赖组件 | 来源 | 用途 |
|---------|------|------|
| REST API | Interface/RestApi | 数据查询 |
| WebSocket | Interface/WebSocket | 实时推送 |
| EventBus | Services | 前端事件订阅的数据源 |
