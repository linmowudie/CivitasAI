# Docs 目录结构迁移计划

> P3-13：将当前 Docs/ 目录结构对齐 skill 规范。
> 状态：**计划中**（实际重命名需单独迁移，避免破坏引用）

---

## 当前 → 目标映射

| 当前路径 | 目标路径 | 优先级 |
|---------|---------|--------|
| `Docs/01-系统概览/` | `Docs/01-需求与规划/` | 低 |
| `Docs/02-核心架构/` | `Docs/02-设计方案/Architecture/` | 低 |
| `ADR/` (根目录) | `Docs/02-设计方案/Decisions/` | 中 |
| `Docs/03-开发规范/` | 保持不变 | — |
| `Docs/04-使用指南/` | 保持不变 | — |
| `Docs/05-部署运维/` | 保持不变 | — |

## 迁移步骤（未来执行）

1. 创建目标目录结构
2. 移动文件
3. 更新所有内部引用（`grep -r` 查找旧路径）
4. 更新 CHANGELOG.md
5. 提交为单独的迁移 commit

## 注意

- 此迁移不影响代码编译，仅影响文档引用
- 建议在无其他文档变更时执行
- ADR 迁移需同步更新 `loopConfig.json` 中的引用（如有）
