# 共享记忆与上下文系统设计

> 本文档定义 Civitas-AI 的共享记忆系统（Shared Memory）与上下文管理，包括事件总线、共享/隔离双层存储、写入守卫、上下文胶囊和知识沉淀机制。

---

## 1. 职责边界

| 职责 | 说明 |
|------|------|
| 全局工作区管理 | 维护所有 Agent 可读写的共享状态 |
| 私有上下文隔离 | 每个 Agent 的中间推理过程物理隔离 |
| 写入守卫 | 拦截异常写入，检测语义冲突 |
| 上下文胶囊 | 按需为 Agent 组装轻量级上下文片段 |
| 知识沉淀 | 将临时事实提炼为长期知识 |
| 事件溯源 | 所有状态变更转化为不可变事件 |

**不负责：** 仲裁裁决（由 Arbitration 负责）、Token 扣减（由 TokenEconomy 负责）。

> ⚠️ **2026-09-22 校准**：上表为职责目标态。现态中「写入守卫」只做到**拦截异常写入**（红线 key / 乐观锁版本 / assertion），
> 「检测语义冲突」**未实现**（见 §3.2）；「上下文胶囊」在 `Src/` 中**无对应类**，实际由 `Services/Context`
> 四级分区装配承担（见 §3.3）；「事件溯源」已实现（`EventBus`），但状态本身**未落库**（见 §2.1）。

---

## 2. 双层存储架构

```
┌───────────────────────────────────────────────────────┐
│                    存储层总览                           │
│                                                       │
│  ┌─────────────────────────────────────────────────┐  │
│  │  共享层（SharedState）                            │  │
│  │  ┌────────────┐ ┌────────────┐ ┌────────────┐  │  │
│  │  │ 任务上下文  │ │ 全局消息流  │ │ 裁决结果   │  │  │
│  │  │ TaskContext │ │ Messages   │ │ Verdicts   │  │  │
│  │  └────────────┘ └────────────┘ └────────────┘  │  │
│  │  所有 Agent 可读写，WriteGuard 拦截              │  │
│  └─────────────────────────────────────────────────┘  │
│                                                       │
│  ┌─────────────────────────────────────────────────┐  │
│  │  隔离层（PrivateState）                           │  │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐       │  │
│  │  │ Agent-A  │ │ Agent-B  │ │ Agent-C  │       │  │
│  │  │ Scratch  │ │ Scratch  │ │ Scratch  │       │  │
│  │  └──────────┘ └──────────┘ └──────────┘       │  │
│  │  各 Agent 独立，执行完毕后自动丢弃               │  │
│  └─────────────────────────────────────────────────┘  │
│                                                       │
│  ┌─────────────────────────────────────────────────┐  │
│  │  长期记忆（LongTermMemory）                       │  │
│  │  从工作区提炼的静态知识，跨任务持久化              │  │
│  └─────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────┘
```

### 2.1 各层定义

| 层 | 数据范围 | 生命周期 | 存储位置 |
|----|---------|---------|---------|
| 共享层 | `messages`, `task_context`, `verdicts`, `contracts` | 全局持久/会话级 | SQLite + 内存缓存 |
| 隔离层 | `internal_scratchpad`, `draft_content`, `tool_results` | 子图执行期 | 内存（执行完丢弃） |
| 长期记忆 | 提炼后的静态知识、重要决策 | 跨任务持久 | SQLite + 向量索引 |

> ⚠️ **未接线（2026-09-22 校准）**：上表"存储位置"列为**目标态**。`global_workspace` / `long_term_memory` 两表已由
> `Src/Infra/Db/migrations.ts` v16 / v17 建好，但 `SharedMemory/globalWorkspace.ts:19` 与 `longTermMemory.ts:36`
> 的数据**只存内存 `Map`**，全仓 `Src/` 无任何 SQL 读写这两张表（除迁移文件本身），进程重启即丢失；
> "向量索引"亦无实现（`Src/` 内无 `semanticVector` / `searchByVector` / 余弦相似度代码，见 §3.2）。

---

## 3. 核心组件

### 3.1 全局工作区（GlobalWorkspace）

```typescript
// ⚠️ 2026-09-22 校准：实际实现为 Services/SharedMemory/globalWorkspace.ts 的**模块级函数**（非 class），
//    全部**同步**返回 Result/数组（无 Promise）；write 入参为 **params 对象**而非 (entry, agentId)。
interface GlobalWorkspace {
  // 读写操作（受 WriteGuard 保护）
  write(entry: WorkspaceEntry, agentId: string): Promise<WriteResult>;
  // ✅ 实为 write(params: { key, content, contentType, assertion, traceId, agentId,
  //    taskId?, taskAuthority?, evidenceChain?, expectedVersion?, conflictStrategy? }): Result<WriteResult>（:29）
  read(query: WorkspaceQuery): Promise<WorkspaceEntry[]>;   // ✅ 实为 read(query): WorkspaceEntry[]（:117，同步）
  delete(entryId: string, agentId: string): Promise<void>;
  // ⚠️ 无 delete：实为 discardEntry(entryId, agentId): Result<void>（:141），仅置 status='discarded'
  
  // 语义检索
  searchBySemantic(query: string, topK: number): Promise<WorkspaceEntry[]>;
  // ⚠️ 未实现（2026-09-22 校准）：Src 内不存在 searchBySemantic / searchByVector；
  //    read() 仅支持 key / traceId / agentId / taskId / contentType / status / assertion / limit 过滤
  
  // 按 trace 查询
  getByTrace(traceId: string): Promise<WorkspaceEntry[]>;   // ✅ 实为 getByTrace(traceId): WorkspaceEntry[]（:162，同步）
}

// ⚠️ 持久化未接线（2026-09-22 校准）：条目只存内存 `const entries: Map<string, WorkspaceEntry>`（:19），
//    不写 `global_workspace` 表（表已由 migrations.ts v16 建好）；见 §2.1 校准注。

interface WorkspaceEntry {
  entryId: string;                     // 全局唯一 ID
  traceId: string;                     // 关联运行会话
  agentId: string;                     // 写入者 Agent ID
  taskId?: string;                     // 关联任务 ID
  
  // 内容
  content: string;                     // 记忆内容
  contentType: 'fact' | 'decision' | 'verdict' | 'contract' | 'status';
  
  // 元数据（强制携带）
  metadata: {
    timestamp: number;                       // epoch ms
    sourceAgentId: string;             // 来源 Agent
    taskAuthority: number;             // 任务权威性（**0.0–1.0**，与 Docs/11 §1.2.1 标度一致；展示层才 ×100）
    evidenceChain?: string[];          // 证据链（关联的 operation_id 列表）
  };
  
  // 向量（用于语义冲突检测）
  semanticVector?: number[];           // ⚠️ 未实现（2026-09-22 校准）：versionedEntry.ts:16-48 无此字段，
                                       //    迁移 v16 亦无对应列；语义冲突检测见 §3.2 校准注
  
  // 状态
  status: 'active' | 'superseded' | 'discarded';
  supersededBy?: string;               // 被哪条新记忆取代
  
  createdAt: number;                        // epoch ms
  updatedAt: number;                        // epoch ms
}
```

> **代码实际字段（2026-09-22 校准）**：`versionedEntry.ts:16-48` 的 `WorkspaceEntry` 除上述字段外还强制携带
> `key`（乐观锁粒度）、`assertion`（§8.4）与 §8.2 的 `version` / `lastModifiedBy` / `conflictStrategy` / `causalTokens`；
> **无 `semanticVector`**。

### 3.2 写入守卫（WriteGuard）

> **2026-09-22 校准（现状 + 待办，不改代码）**：实际实现是 `Src/Services/SharedMemory/writeGuard.ts` 的
> `interceptWrite(entry, currentVersion, expectedVersion): Result<WriteResult>`（:32，**同步**，非 class、非 `intercept`），
> 只做**三件事**：
> ① 红线 key 校验（`FORBIDDEN_KEYS` :19-22 = `goal` / `immutable_constraints` / `successCriteria` 及 `goal.*` 变体）；
> ② 乐观锁版本校验，不匹配 → 发布 **`MEMORY_VERSION_CONFLICT`**（:54）并返回 `success: false`；
> ③ `assertion` 级别标记（`inferred` / `assumed` → 附 warning，不入 L 低频分区与长期记忆）。
>
> ⚠️ **语义冲突检测属 Phase 未实现（2026-09-22 校准）**：下方 [1] 生成向量、[2] `searchByVector`、[3] 余弦相似度 > 0.85
> 与 `checkSemanticOpposition` 在 `Src/` 中**均不存在**（无 `generateVector` / `cosineSimilarity` / `searchByVector` /
> `semanticVector` 任一符号）；`Configs/memory.json → memory.conflictSimilarityThreshold` 在 `Src/`、`Tests/` 中
> **0 引用**（仅 `Client/src/config/configSchema.ts` 作为配置项展示）。
>
> ⚠️ **`CONFLICT_DETECTED` 的实际发布方不是 WriteGuard**：`Src/Services/SharedMemory/` 内无任何 `CONFLICT_DETECTED`
> 发布点，唯一发布点是 `Core/Decision/orchestrator/conflictPrecheck.ts:55`，检测维度为"多个 Agent 修改了同一文件路径"
> （目录隔离层 + git worktree 层），详见 Docs/05 Step 1 与 §5 数据流图校准注。

```typescript
class WriteGuard {
  // 拦截写入请求                        // ⚠️ 目标态签名（2026-09-22 校准），实际见上方校准注
  async intercept(entry: WorkspaceEntry): Promise<InterceptResult> {
    // [1] 生成/验证语义向量              // ⚠️ 未实现（2026-09-22 校准）
    const vector = entry.semanticVector ?? await this.generateVector(entry.content);
    
    // [2] 相似度碰撞检测                 // ⚠️ 未实现（2026-09-22 校准）：无 searchByVector
    const similarEntries = await this.workspace.searchByVector(vector, 5);
    
    // [3] 冲突判定                        // ⚠️ 未实现（2026-09-22 校准）：0.85 阈值与语义相反判定均无代码
    for (const existing of similarEntries) {
      const similarity = cosineSimilarity(vector, existing.semanticVector);
      
      if (similarity > 0.85) {
        // 高相似度 → 检查是否语义相反
        const isOpposite = await this.checkSemanticOpposition(entry.content, existing.content);
        
        if (isOpposite) {
          // 语义冲突 → 阻断并上报         // ⚠️ 未实现：实际由 conflictPrecheck.ts:55 按文件冲突发布 CONFLICT_DETECTED
          return {
            allowed: false,
            conflict: {
              conflictId: generateId(),
              newMemoryId: entry.entryId,
              oldMemoryId: existing.entryId,
              similarity,
              conflictType: 'semantic_opposition',
            }
          };
        }
        
        // 高相似 + 非相反 → 可能是重复 → 警告但放行
        return { allowed: true, warning: 'potential_duplicate' };
      }
    }
    
    // 无冲突 → 放行                      // ✅ 现状等价行为：红线 + 版本 + assertion 三项通过后放行
    return { allowed: true };
  }
}
```

### 3.3 上下文胶囊构建器（CapsuleBuilder）⚠️ 未实现（2026-09-22 校准）

> **2026-09-22 校准（现状 + 待办，不改代码）**：`CapsuleBuilder` / `buildForAgent` / `Capsule.sections` 在 `Src/` 中**不存在**
> （全仓无该符号），本节整段为**目标态**。
> **实际替代物**：`Src/Services/Context/` 的**四级分区装配器** —— `assembleContext(options: AssemblyOptions): Result<AssemblyResult>`
> （`assembler.ts:67`），按 `PARTITION_ORDER = ['S','L','M','H']`（`partitions/partitions.ts:19`、`:69`）顺序装配、
> 按 `scoring.ts` 打分选丢弃项、由 `truncation.ts` 在超预算（默认阈值 0.92）时截断，输出带 `cachePrefixHash`（S+L 前缀）
> 的消息列表；语义与"按需拼装轻量胶囊"相近，但**没有 capsule/sections 概念，也没有 `searchBySemantic` 相关记忆检索**（见 §3.1）。
> ⚠️ **该装配器同样未接线**：`Src/Core/Loop/runIteration.ts:320` 仍为 `TODO: 当 PartitionState 完整初始化后，启用 assembleContext 装配路径`，
> 现态是「system prompt + `ctx.chatMessages` 全量拼接」（`runIteration.ts:312-319`）。

```typescript
class CapsuleBuilder {                                    // ⚠️ 目标态（2026-09-22 校准）：Src 无此类
  // 为指定 Agent 构建上下文胶囊
  async buildForAgent(agentId: string, taskContext: TaskContext): Promise<Capsule> {
    const capsule: Capsule = {
      capsuleId: generateId(),
      agentId,
      sections: [],
      totalTokens: 0,
    };
    
    // [1] 任务描述（必选）
    capsule.sections.push({
      type: 'task_description',
      content: taskContext.description,
      tokenCount: await this.countTokens(taskContext.description),
    });
    
    // [2] 相关共享记忆（语义检索 Top-K）
    const relevantMemories = await this.workspace.searchBySemantic(
      taskContext.description,
      taskContext.memoryTopK ?? 5
    );
    capsule.sections.push({
      type: 'relevant_memories',
      content: relevantMemories.map(m => m.content).join('\n---\n'),
      tokenCount: await this.countTokens(relevantMemories.map(m => m.content).join('\n')),
    });
    
    // [3] 最近对话历史（按时间倒序，直到预算用尽）
    const recentMessages = await this.getRecentMessages(taskContext.traceId, taskContext.messageBudget);
    capsule.sections.push({
      type: 'recent_messages',
      content: recentMessages.map(m => formatMessage(m)).join('\n'),
      tokenCount: await this.countTokens(recentMessages.map(m => formatMessage(m)).join('\n')),
    });
    
    // [4] 约束条件
    if (taskContext.constraints?.length) {
      capsule.sections.push({
        type: 'constraints',
        content: taskContext.constraints.join('\n'),
        tokenCount: await this.countTokens(taskContext.constraints.join('\n')),
      });
    }
    
    // [5] 预算检查
    const totalTokens = capsule.sections.reduce((sum, s) => sum + s.tokenCount, 0);
    if (totalTokens > taskContext.capsuleBudget) {
      // 超预算 → 按优先级裁剪（constraints > task > memories > messages）
      await this.trimToBudget(capsule, taskContext.capsuleBudget);
    }
    
    return capsule;
  }
}
```

### 3.4 长期记忆（LongTermMemory）

```typescript
interface LongTermMemoryEntry {
  memoryId: string;
  
  // 知识内容
  title: string;                       // 知识标题
  content: string;                     // 知识内容（已剥离对话过程）
  category: 'rule' | 'fact' | 'decision' | 'pattern';
  
  // 来源追溯
  sourceTraceIds: string[];            // 来源 trace
  sourceArbitrationIds?: string[];     // 来源仲裁案件
  
  // 向量
  semanticVector: number[];            // ⚠️ 未实现（2026-09-22 校准）：longTermMemory.ts:19-32 无此字段，
                                       //    迁移 v17 `long_term_memory` 亦无对应列；实际多一个 `assertion` 字段
  
  // 元数据
  createdAt: number;                        // epoch ms
  lastAccessedAt: number;                   // epoch ms
  accessCount: number;
  
  // 有效性
  status: 'active' | 'deprecated' | 'contradicted';
  contradictedBy?: string;             // 被哪条新知识取代
}
```

**知识沉淀触发条件：**
- 仲裁裁决完成后（由 KnowledgeConsolidator 触发）
- 任务完成后，Director 判定有值得沉淀的决策经验
- 定期巡检时，Audit Bureau 发现可提炼的模式

> ⚠️ **现状（2026-09-22 校准）**：`Src/` 中无 `KnowledgeConsolidator` 类；实际实现为
> `SharedMemory/memoryConsolidator.ts` 的 `consolidateFromTrace()`（按 trace 过滤 `assertion='observed'` 的条目写入
> 长期记忆，并在 `consolidated > 0` 时于 :55 发布 `KNOWLEDGE_CONSOLIDATION`）与
> `Arbitration/tribunal.ts:297`（`consolidateKnowledge(caseId)` 只要案件已有 `finalVerdict` 即发布同名事件，但**不写长期记忆库**）。
> 双发布问题见 §4.3 待办。

---

## 4. 事件总线（EventBus）

### 4.1 设计原则

| 原则 | 说明 |
|------|------|
| 事件溯源 | 所有状态变更转化为不可变事件 |
| 发布-订阅 | 组件仅订阅关心的事件类型，极致解耦 |
| 异步通信 | 事件处理不阻塞发布者 |
| 有序性 | 同一 trace 下的事件保证顺序 |

### 4.2 实现方案

**Phase 0-2：** 内存事件总线（进程内 EventEmitter 模式）

```typescript
interface EventBus {
  // 发布事件
  publish(event: DomainEvent): void;
  
  // 订阅事件
  subscribe(eventType: EventType, handler: EventHandler): Subscription;
  
  // 订阅多个事件
  subscribeMany(eventTypes: EventType[], handler: EventHandler): Subscription;
  
  // 等待特定事件（Promise 化）
  waitFor(eventType: EventType, filter?: (event: DomainEvent) => boolean, timeoutMs?: number): Promise<DomainEvent>;
}

interface DomainEvent {
  eventId: string;                     // UUID
  eventType: EventType;                // 🔴 必须是 §4.3 注册表成员，禁止自由字符串
  traceId?: string;                    // 关联 trace
  loopId?: string;                     // v2.1 补：Loop 级事件必填（供 §8.5 与 Docs/12 回放对齐）
  timestamp: number;                   // epoch **毫秒**（与 Docs/10 §1.1 时间列统一）
  priority: 'low' | 'normal' | 'high' | 'critical';
  payload: Record<string, unknown>;     // 事件数据
  source: string;                      // 发布者标识（组件名，如 'SharedMemory/WriteGuard'）
}
```

> **v2.1 修正**：① `eventType` 由 `string` 改为 `EventType`（否则§4.3 的"禁止自由字符串"红线在类型层不可执行）；
> ② `subscribe` / `subscribeMany` 的参数同步改为 `EventType` / `EventType[]`；
> ③ `timestamp` 由 ISO 字符串改为 epoch ms（全系统时间统一为整数毫秒，见 Docs/10 §1.1）；
> ④ `timeout` → `timeoutMs`（强制单位后缀）；⑤ 新增 `loopId`，原来 Loop 事件只能塞在 `payload` 里，导致索引与回放无法按 Loop 检索。

**Phase 3 升级路径：** 内存事件总线 → Redis Pub/Sub（接口不变，仅替换实现）

### 4.3 事件类型注册表

所有事件类型必须在本注册表登记，**禁止使用自由字符串**（类型层由 `DomainEvent.eventType: EventType` 强制）。

> **本表为全系统事件名的唯一定义处**：其他文档（含 Docs/12、Docs/13）只能引用成员名，不得自写新事件字符串；
> 新增事件必须先在此追加，命名格式 `<域>:<小写 snake_case 动作>`，域前缀固定为
> `task` / `agent` / `token` / `arbitration` / `regulation` / `audit` / `memory` / `loop` / `durable` / `system`。
>
> ✅ **2026-09-22 校准**：本表已与 `Src/Services/EventBus/eventTypes.ts` 逐成员比对——唯一漏登记的
> `AGENT_ITERATION_COMPLETE`（`eventTypes.ts:31`）已在 Agent 段补登，其余成员与代码**一一对应**（含发布方注释的修正见下）。

```typescript
enum EventType {
  // 任务生命周期
  TASK_RECEIVED = 'task:received',
  TASK_DECOMPOSED = 'task:decomposed',
  TASK_ASSIGNED = 'task:assigned',
  TASK_STARTED = 'task:started',          // ← 唯一正确拼写；Docs/05/06 若出现 TASK_START 均属错误
  TASK_PROGRESS = 'task:progress',
  TASK_COMPLETED = 'task:completed',
  TASK_FAILED = 'task:failed',

  // Agent 生命周期
  AGENT_RECRUITED = 'agent:recruited',
  AGENT_READY = 'agent:ready',
  AGENT_EXPELLED = 'agent:expelled',
  AGENT_DESTROYED = 'agent:destroyed',
  // 🔴 前端改造新增（Docs/16 F0.6；未新增域前缀，遵本表白名单）
  // ✅ 2026-09-22 校准：原标"待实现"已过期——本组四事件在代码中**均已发布**（发布点见各行注）；
  //    但 stream_chunk 的"合批"约束仍为 ⚠️ 未实现（见下行）。
  AGENT_STREAM_CHUNK = 'agent:stream_chunk',   // LLM 增量输出片；服务端按 ui.streamFlushIntervalMs 合批后推，**禁止逐 token 推送**
  // ↑ 发布点 `wsHandler.ts:281`；⚠️ **合批未实现（2026-09-22 校准）**：`Src/` 内**无 `streamFlushIntervalMs` 引用**
  //   （仅 `Configs/default.json:26` 定义），现态为 `onStreamChunk` 回调内**逐 chunk 直接 publish**
  AGENT_STREAM_END = 'agent:stream_end',       // 一轮流式结束（携带 model / tokensUsed / stopped 标志）（wsHandler.ts:159 / 223 / 236 / 248 / 350 / 357 / 371）
  AGENT_CHAT_MESSAGE = 'agent:chat_message',   // 会话消息落库完成（含 role / sessionId / traceId）（RestApi/chatApi.ts:90）
  AGENT_ITERATION_COMPLETE = 'agent:iteration_complete',
  // ↑ 🔴 补登记（2026-09-22 校准）：`eventTypes.ts:31` 已存在本成员但原表漏登记；
  //   实际发布方为 `Interface/WebSocket/wsHandler.ts:312`（单次迭代完成，含 toolCalls / toolResults），
  //   前端镜像 `Client/src/shared/eventTypes.ts` 已同步；契约见 Docs/03 Interfaces §`agent:iteration_complete`。

  // Token 经济
  TOKEN_CONSUMED = 'token:consumed',
  TOKEN_EARNED = 'token:earned',
  TOKEN_DISTRIBUTED = 'token:distributed',
  TAX_PAID = 'token:tax_paid',
  TAX_RATE_UPDATED = 'token:tax_rate_updated',
  WALLET_FROZEN = 'token:wallet_frozen',      // 发布方：**TokenEconomy**（执行状态变更的组件）；审计/监管仅为 `payload.initiator`（Docs/04 §5.1、Docs/06 §3）
  WALLET_UNFROZEN = 'token:wallet_unfrozen',  // 发布方：TokenEconomy；自动解冻时 initiator='system'
  TOKEN_CONFISCATED = 'token:confiscated',
  INSUFFICIENT_BALANCE = 'token:insufficient_balance',
  LEDGER_MISMATCH = 'token:ledger_mismatch',

  // 仲裁
  CONFLICT_DETECTED = 'arbitration:conflict_detected',
  // ↑ 发布方更正（2026-09-22 校准）：原写"发布方：WriteGuard（本文件 §3.2）"**不成立**——
  //   SharedMemory 内无任何 CONFLICT_DETECTED 发布点；唯一发布方为
  //   `Core/Decision/orchestrator/conflictPrecheck.ts:55`（source='Decision/Orchestrator/conflictPrecheck'，
  //   检测维度 = 多 Agent 修改同一文件路径），payload 为 { taskId, conflictCount, conflicts[] }，
  //   **不含** §3.2 目标态的 conflictId/newMemoryId/oldMemoryId/similarity。
  //   ⚠️ WriteGuard 侧的语义向量冲突检测仍为目标态（见 §3.2），本行不改事件名。
  ARBITRATION_FILED = 'arbitration:filed',
  SUSPEND_AND_NOTIFY = 'arbitration:suspend_and_notify',
  ARBITRATION_VERDICT = 'arbitration:verdict',
  RESTORATION_ACK = 'arbitration:restoration_ack',
  KNOWLEDGE_CONSOLIDATION = 'arbitration:knowledge_consolidation',
  // ↑ 发布方裁定：由 Loop 成功退出（Docs/12 §2.3）触发，**仲裁不再重复发布**，否则一次任务写两份长期记忆
  // ⚠️ 现状 + 待办（2026-09-22 校准，**代码侧待收敛，本文不改代码**）：上述裁定**未落地**——
  //   ① 仲裁仍发布：`Arbitration/tribunal.ts:297` 的 `consolidateKnowledge(caseId)` 仅校验"案件存在且已有 finalVerdict"
  //      即发布（payload = { caseId, conflictId, verdict }），**不写长期记忆、不做窗口检查**；
  //   ② 沉淀器也发布：`SharedMemory/memoryConsolidator.ts:55` 在 `consolidated > 0` 时发布（payload = { consolidated, skipped, taskId }）；
  //   ③ Loop 成功退出**根本不发布**：`Core/Loop/iterationController.ts` 的 `ExitReason` 虽声明 `'success'`（:10），
  //      但 `decideIteration` 只会产出 risk / budget_exhausted / max_iterations / no_progress 四值，无 success 分支；
  //   ④ 全系统**无 KNOWLEDGE_CONSOLIDATION 订阅者**（`Src/` 内 grep 无订阅点），故当前不产生"写两份长期记忆"的实际后果，
  //      但双发布属**待办收敛项**：应由唯一入口（Loop 成功退出或 consolidateFromTrace 之一）发布并补订阅方。
  ARBITRATION_DEADLOCK = 'arbitration:deadlock',
  ARBITRATOR_POOL_RESIZED = 'arbitration:pool_resized',

  // 监管
  RULE_UPDATED = 'regulation:rule_updated',
  BROADCAST_MESSAGE = 'regulation:broadcast',
  EMERGENCY_INTERVENTION = 'regulation:emergency',

  // 审计
  ANOMALY_DETECTED = 'audit:anomaly_detected',
  AUDIT_COMPLETED = 'audit:completed',
  PATROL_REPORT = 'audit:patrol_report',

  // 共享记忆
  MEMORY_WRITTEN = 'memory:written',
  MEMORY_SUPERSEDED = 'memory:superseded',
  MEMORY_VERSION_CONFLICT = 'memory:version_conflict',   // v2.1 补（§8.2 乐观锁冲突）
  MEMORY_SELF_REINFORCING = 'memory:self_reinforcing',   // v2.1 补（§8.4 自增强回路）

  // 🔴 v2.1 新增：Loop 控制（Docs/12 引用，原均未登记）
  LOOP_STARTED = 'loop:started',
  LOOP_COMPLETED = 'loop:completed',
  LOOP_ABORTED = 'loop:aborted',                         // payload.reason 取 stopped_reason 七值之一
  BUDGET_WARMING = 'loop:budget_warming',                // 命中 warmRatio，不阻断
  BUDGET_SOFT_REACHED = 'loop:budget_soft_reached',      // 命中 softRatio，降级+减并行+继续
  BUDGET_HARD_REACHED = 'loop:budget_hard_reached',      // 命中 hardRatio，中止+回滚
  NO_PROGRESS_STAGNATED = 'loop:no_progress_stagnated',  // 命中 stopRules.noProgress
  ACTION_FINGERPRINT_DUPLICATE = 'loop:action_fingerprint_duplicate',  // 取代未登记的旧名 LOOP_DETECTED
  APPROVAL_REQUESTED = 'loop:approval_requested',
  APPROVAL_DECIDED = 'loop:approval_decided',
  APPROVAL_TIMEOUT_REJECTED = 'loop:approval_timeout_rejected',
  VERIFIER_FAILED = 'loop:verifier_failed',
  CONTEXT_COMPRESSED = 'loop:context_compressed',

  // 🔴 v2.1 新增：持久执行（Docs/13 引用）
  EFFECT_UNKNOWN = 'durable:effect_unknown',
  LOOP_SUSPENDED = 'durable:loop_suspended',
  LOOP_RESUMED = 'durable:loop_resumed',
  RECOVERY_HUMAN_REQUIRED = 'durable:recovery_human_required',
  ARTIFACT_DRIFT_DETECTED = 'durable:artifact_drift_detected',

  // 🔴 v2.1 新增：系统级
  SHUTDOWN_FLUSH_TIMEOUT = 'system:shutdown_flush_timeout',
  CONFIG_RELOAD_FAILED = 'system:config_reload_failed',
}
```

**已废弃事件名（不得再出现，均归入上表正式名）**：

| 旧名（文档中出现过） | 裁决 |
|-------------------|------|
| `stream_chunk`（Docs/09 §3.2 旧写法，未登记） | → `AGENT_STREAM_CHUNK` = `'agent:stream_chunk'` |
| `LOOP_DETECTED` | → `ACTION_FINGERPRINT_DUPLICATE` |
| `TASK_START` | → `TASK_STARTED` |
| `APPROVAL_TIMEOUT` | → `APPROVAL_TIMEOUT_REJECTED` |
| `MEMORY_CONFLICT` | → `CONFLICT_DETECTED` |
| `COMPENSATION_*` | → `RESTORATION_ACK`（统一用恢复确认事件携带补偿明细，不拆多条） |

---

## 5. 数据流图

> **2026-09-22 校准**：下图为**目标态**。实际代码路径为
> `globalWorkspace.write(params)`（:29，内存 Map）→ `writeGuard.interceptWrite()`（:32，红线 key / 乐观锁版本 / assertion）
> → 成功即 `MEMORY_WRITTEN`、版本不匹配即 `MEMORY_VERSION_CONFLICT`（:54）；
> **向量检索与语义相反判定不在链路中**（未实现，见 §3.2）。
> `CONFLICT_DETECTED` 的实际发起方是**合并前的冲突预检** `Core/Decision/orchestrator/conflictPrecheck.ts:55`
> （检测"多 Agent 改同一文件"），而非 WriteGuard；Docs/08 §1 表与 §3.2 序列图已同步标注。

```
Agent-A 写入新记忆
    │
    ▼
WriteGuard.intercept()          // ⚠️ 目标态：实际为 interceptWrite()，不做语义冲突检测（2026-09-22 校准）
    │                            //    CONFLICT_DETECTED 实际由 conflictPrecheck.ts:55 在合并前发布
    ├── 无冲突 → GlobalWorkspace.write() → 广播 MEMORY_WRITTEN   // ✅ 已实现（顺序相反：先 write 内调 interceptWrite）
    └── 有冲突 → 生成 CONFLICT_DETECTED    // ⚠️ 语义冲突路径未实现；文件冲突路径已实现
                    │
                    ▼
              Arbitration 六步闭环
                    │
                    ├── 胶囊调阅 → 裁决推理 → 律师函挂起
                    │
                    ▼
              裁决完成
                    │
                    ├── 胜者记忆写入 → MEMORY_WRITTEN
                    ├── 败者记忆标记 → MEMORY_SUPERSEDED   // ⚠️ 未实现：write() 仅把旧条目 status 置 superseded，不发事件
                    ├── 恢复确认 → RESTORATION_ACK
                    └── 知识沉淀 → KNOWLEDGE_CONSOLIDATION   // ⚠️ 现状（2026-09-22）：由 tribunal.ts:297 与
                                    │                        //    memoryConsolidator.ts:55 双点发布，Loop 不发
                                    ▼
                              LongTermMemory.write()        // ✅ 实为 writeMemory()（longTermMemory.ts:44，内存 Map）
```

---

## 6. 配置参数

> **本文件不维护配置默认值**（SSOT 裁决，见 Docs/11 文档头）。下表仅列出本子系统**消费**的键与其定义位置；新增值一律先写 Docs/11 §2。

| 本文使用的键 | 定义位置 | 用途 |
|-------------|---------|------|
| `memory.conflictSimilarityThreshold` | Docs/11 §2.11 | WriteGuard 语义冲突检测阈值（取代原 `memory.similarityThreshold`，与仲裁侧归一） |
| `memory.capsuleBudgetTokens` | Docs/11 §2.11 | 默认胶囊 Token 预算（原与 `arbitration.capsuleBudgetTokens` 同值双定义，现以本键为准） |
| `memory.capsuleMemoryTopK` | Docs/11 §2.11 | 胶囊中相关记忆条数 |
| `memory.capsuleMessageBudgetTokens` | Docs/11 §2.11 | 胶囊中对话历史 Token 预算 |
| `memory.longTermConsolidationDelayMs` | Docs/11 §2.11 | 知识沉淀延迟（毫秒；原名无单位后缀） |
| `memory.privateStateTtlSec` | Docs/11 §2.11 | 隔离层数据 TTL（秒；原名 `privateStateTTL` 无单位后缀） |
| `eventBus.maxQueueSize` | Docs/11 §2.11 | 事件队列最大长度 |
| `eventBus.consumerTimeoutMs` | Docs/11 §2.11 | 事件消费超时（毫秒） |
| `audit.loopSimilarityThreshold` / `audit.loopConsecutiveRounds` | Docs/11 §2.6 | 循环指纹相似度与连续轮次（与记忆侧阈值**语义不同**，不得合并） |
| `arbitration.caseArbitratorCount` 等 | Docs/11 §2.5 | 胶囊调阅与仲裁并发（计数口径裁决见同节） |

> ⚠️ **Src 消费状态（2026-09-22 校准）**：上表 `memory.*` 各键在 `Src/` 内**均无引用**——
> `conflictSimilarityThreshold` 因语义冲突检测未实现而无消费点（见 §3.2，仅 `Configs/memory.json:3` 定义 +
> `Client/src/config/configSchema.ts:181` 展示）；`capsuleBudgetTokens` / `capsuleMemoryTopK` / `capsuleMessageBudgetTokens` /
> `longTermConsolidationDelayMs` / `privateStateTtlSec` 因胶囊构建器与隔离层 TTL 未实现而无消费点（见 §3.3）。
> 本表现阶段属**配置契约**，不描述运行时行为。

---

## 7. 依赖关系

| 依赖组件 | 来源 | 用途 |
|---------|------|------|
| `LlmCaller` | Tools | 语义向量生成、语义相反判定 |
| `Database` | Infra | 工作区/长期记忆持久化 |
| `Logger` | Infra | 事件溯源日志 |
| `VectorStore` | Infra | 向量检索（Phase 3，Phase 0-2 用 SQLite 全文检索替代） |

> ⚠️ **现状（2026-09-22 校准）**：本表为**目标态依赖**。`LlmCaller` 的"语义向量生成 / 语义相反判定"与 `VectorStore`
> 均无实现（见 §3.2）；`Database` 目前也**未**用于工作区/长期记忆持久化（见 §2.1 校准注）；
> `Logger` 亦未被 `Src/Services/SharedMemory/*` 任一文件引用。本子系统**实际接线的依赖只有 EventBus**
> （`EventBus/eventBus.js` 的 `createEvent` / `publish`）。

---

## 8. v2 补丁：Context ≠ State 拆分与并行隔离

> **修订依据**：`Docs/99-审查记录/设计审查报告-Loop与Harness工程.md` §3 失效三、§4.3、§4.4。

### 8.1 SharedMemory 职责边界重新定义（红线）

旧版中 `GlobalWorkspace` 同时充当了【跨 Agent 共享上下文】与【任务事实】两个角色，导致：
- 上下文压缩会意外修改任务事实；
- 不同 Agent 看到不同时间点的 workspace 无版本号；
- 一个 Agent 错误写入会污染其他 Agent 下一轮上下文。

**v2 红线**：

| 存放 | 内容 | 写入时机 | 允许丢失 |
|------|------|---------|---------|
| **LoopState**（Docs/12 §4） | 任务事实 | 每轮末尾强一致 | ❌ |
| **GlobalWorkspace** | 跨 Agent 可共享的观察 | 事件到达 | ⚠️ 可从 Event 重建 |
| **LongTermMemory** | 知识沉淀 | 任务完成后 | ⚠️ |
| **Context（本轮装配）** | 从上面三者临时拼装 | ⑥ 之前 | 不适用 |

**禁止**：任何将 `goal / immutable_constraints / successCriteria` 写入 GlobalWorkspace 的行为——必须写入 LoopState。

### 8.2 版本号与乐观锁（新增）

`WorkspaceEntry` 必须包含：

```typescript
interface WorkspaceEntry {
  // ... 旧字段保留
  version: number;                // 🔴 新增：单调递增
  lastModifiedBy: string;         // 🔴 新增：agentId
  conflictStrategy: 'lww' | 'authority' | 'arbitrate';  // 🔴 新增
  causalTokens: string[];         // 🔴 新增：因果一致性向量
}
```

**写入约束**：
- `GlobalWorkspace.write(key, value, expectedVersion)` —— expectedVersion 不匹配 → 拒写 → 产生 `VERSION_CONFLICT`；
  - ✅ 事件名更正（2026-09-22 校准）：正式名是 **`MEMORY_VERSION_CONFLICT`** = `'memory:version_conflict'`
    （`eventTypes.ts:68`；`VERSION_CONFLICT` 属未登记的自由字符串，违反 §4.3 红线）；实际发布点 `writeGuard.ts:54`，
    payload 为 `{ conflict: { entryId, key, expectedVersion, actualVersion, agentId } }`；
  - ✅ 调用形态更正（2026-09-22 校准）：实为 `write(params)` **单对象入参**（`globalWorkspace.ts:29`），
    `key` / `content` / `expectedVersion` 均为 params 字段；`expectedVersion` 缺省时取当前版本（即不校验）。
- `causalTokens` 记录本次写前看到过的其他 Agent 写入事件 id；用于仲裁时重建 happens-before 关系。
  —— ✅ 已实现：`causalTokens.ts` 提供 `incrementClock` / `mergeClocks` / `happensBefore` / `isConcurrent`，
  `write()` 内以「同 key 最新条目的时钟 + 本 Agent 自增」生成（`globalWorkspace.ts:57-58`）。

### 8.3 WorkspaceLock（并行隔离）

对齐审查报告 §4.3：“并行 Agent 必须隔离工作区”。

```typescript
// Services/SharedMemory/workspaceLock.ts
// ✅ 路径校正（2026-09-22 校准）：文件名为**小写** workspaceLock.ts（原写 WorkspaceLock.ts 大小写有误）
interface LockRequest {
  key: string;                 // 文件路径或 entryKey
  agentId: string;
  mode: 'READ' | 'WRITE' | 'EXCLUSIVE';
  ttlMs: number;               // ⚠️ **必填**，无默认值（原写"默认 60_000"未实现）
}

// 语义
acquire(req): Promise<Grant | Rejected | Queued>
release(grantId): Promise<void>
```

> **✅ 签名校正（2026-09-22 校准）**：按代码为
> `acquireLock(request: LockRequest): Result<LockGrant | LockRejected>`（`workspaceLock.ts:53`）与
> `releaseLock(grantId: string): Result<void>`（`:109`）——
> ① 名为 `acquireLock` / `releaseLock`，不是 `acquire` / `release`；② **同步**返回 `Result`，不是 `Promise`；
> ③ **无 `Queued` 态**：与持有者冲突时直接返回 `LockRejected { key, agentId, reason }`；
> ④ `LockGrant` 字段为 `grantId / key / agentId / mode / acquiredAt / expiresAt`；
> ⑤ ⚠️ 锁状态仅存内存 `Map`（`:47`），过期锁在 acquire 时惰性清理；
> ⑥ ⚠️ `Src/` 内目前**无运行时调用方**（仅 `SharedMemory/index.ts:22` 再导出），即"并行 Agent 取锁"尚未接线。

**策略**：
| 目录/资源 | 默认锁 | 适用 |
|---------|-------|------|
| `Data/Workspace/{session}/{taskId}/{agentId}/**` | 无锁（天然隔离） | 代码任务个人目录 |
| `Data/Workspace/{session}/shared/**` | 乐观锁 | 需合并的产物 |
| 同一仓库 | Git worktree | Consortium 代码任务 |
| `GlobalWorkspace.entries` | 版本号 + LWW/仲裁 | 共享上下文 |

### 8.4 Self-Reinforcing 防御（错误传播）

针对“Agent 上一轮错误结论被下一轮当事实前提”，新增：

- 写入 GlobalWorkspace 时区分 `assertion: 'observed' | 'inferred' | 'assumed'`；
- 只有 `observed` （工具返回值/硬验证结果）可直接入 L 低频分区；
- `inferred` 与 `assumed` 必须进入 M/H，不会进入长期记忆；
- GoalReanchor 从原始数据源重新推导时丢弃 `inferred/assumed`。

### 8.5 与 Docs/12 Docs/13 的接口契约

| 交互 | 方式 |
|------|------|
| GlobalWorkspace.write | 必须走 EffectJournal.wrapToolCall（Docs/13 §4） |
| GlobalWorkspace.read | 不写日志；但携带 LoopState.lastCheckpointId 作为快照时间戳 |
| MEMORY_WRITTEN 事件 | Docs/13 Checkpoint 会记录写入后的 entry 快照 |
| CONFLICT_DETECTED 事件 | 同时写入 LoopState.suspension（Docs/12 §4.2） |

---

## 9. 修订记录

> 更早的 v2 / v2.1 修订以正文内联标记（`v2 补丁`、`v2.1 补`、`v2.1 修正`）形式保留，未在此重复登记。

| 日期 | 修订内容 |
|------|---------|
| 2026-09-22 | **按代码现状校准**（与 Docs/04/05/06 同口径，仅改文档不改代码）：① §3.2 WriteGuard 的向量 + 余弦 > 0.85 语义相反检测标 **⚠️ Phase 未实现**，`CONFLICT_DETECTED` 发布方更正为 `Core/Decision/orchestrator/conflictPrecheck.ts:55`（§3.2/§4.3/§5 三处同步）；② §3.1 GlobalWorkspace 签名按 `globalWorkspace.ts` 校正（`write(params)` / `discardEntry` / 无 `searchBySemantic` / 同步返回），持久化标 **⚠️ 未接线**（§2.1 同步加注）；③ `WorkspaceEntry` / `LongTermMemoryEntry` 的 `semanticVector` 标 ⚠️ 未实现；④ §4.3 补登记 `AGENT_ITERATION_COMPLETE`（并修正同段已过期的"待实现"标注——三个 stream/chat 事件代码已发布，仅 `stream_chunk` 的合批 ⚠️ 未实现）；⑤ `KNOWLEDGE_CONSOLIDATION` 裁定记为**现状 + 待办**（`tribunal.ts:297` 与 `memoryConsolidator.ts:55` 双发布，Loop 成功退出不发布）；⑥ §3.3 CapsuleBuilder 标 ⚠️ 未实现，指明实际替代为 `Services/Context` 四级分区 `assembleContext`（自身待接线）；⑦ §8.2 `VERSION_CONFLICT` → `MEMORY_VERSION_CONFLICT`；⑧ §8.3 路径大小写与方法名（`acquireLock` / `releaseLock`，同步）校正；⑨ §6 补 `memory.*` 键的 Src 消费状态。 |

