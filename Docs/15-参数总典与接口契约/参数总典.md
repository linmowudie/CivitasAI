# 参数总典（开发速查）

> **本文定位**：全系统 **参数、单位、枚举、映射、契约** 的**唯一速查入口**。开发时先查本文定位"某个值在哪、叫什么、什么类型、谁消费"，再跳到对应事实源看完整上下文。
>
> **本文不是事实源**。所有数值以「定义位置」列指向的文件为准；本文与事实源冲突时，**以事实源为准并立即回修本文**。
>
> **上游四层事实源（SSOT）**：
>
> | 层 | 唯一事实源 | 管辖范围 |
> |----|-----------|---------|
> | 配置键 + 默认值 | `Docs/11 §1.1 / §2.1–§2.11` | 全部 `Configs/*.json` 的键名与默认值 |
> | DDL | `Docs/10 §1.1 / §2–§4 / §8.2` | 全部库表、列名、列类型、约束 |
> | 事件名 | `Docs/07 §4.3` | `EventType` 枚举全集（禁止自由字符串） |
> | 类型 schema 语义 | `Docs/12`（Loop）· `Docs/13`（Durable）· `Docs/02`（架构/契约） | 接口字段语义与判定规则 |
>
> **禁止行为**：在任何其他设计文档内联配置默认值、`CREATE TABLE`、或自写新事件字符串。

---

## 0. 五层参数地图（自底向上）

按此顺序实现与查参，**下层不得依赖上层参数**（对齐 skill 五层单向依赖）。

| 层 | 目录 | 参数域 | 主要事实源 | 本文档节 |
|----|------|--------|-----------|---------|
| **L1 Infra** | `Infra/` | 数据库、LLM Provider、文件、配置加载、持久执行 | Docs/10、Docs/11 §2.1/§2.2/§2.9、Docs/13 | §2.1 §2.2 §2.3 §2.9 §3 |
| **L2 Tools** | `Tools/` | 工具契约（dangerLevel / idempotency / reversibility / 沙箱 / 限流） | Docs/11 §6 | §4.1 §4.6 |
| **L3 Services** | `Services/` | 上下文装配、共享记忆、Token 经济、仲裁、审计、监管 | Docs/07、Docs/11 §2.4–§2.7/§2.11、Docs/04–§06 | §2.4 §2.5 §2.6 §2.7 §2.11 |
| **L4 Core** | `Core/` | 主循环、StopRules、Verifier、LoopState、编排路由 | Docs/12、Docs/02、Docs/03、Docs/11 §2.3/§2.8 | §2.3 §2.8 §4 §5 |
| **L5 Interface** | `Client/`、`Interface/` | 服务端口、WS 协议、推送节流、审批面板 | Docs/08、Docs/09、Docs/11 §2.1 | §2.1(ui) §4.5 |

**跨层红线（三条）**：
1. **单位/标度不得跨层变形**：比例只在 L5 展示层 ×100，其余层恒 0.0–1.0；时间戳全链路 epoch ms。
2. **参数不得跨层复制**：L3–L5 需要 L1 的值时**引用配置键名**，不写数值。
3. **ID 语义不得跨层混用**：`session_key` / `trace_id` / `operation_id` / `loop_id` 粒度固定（§6）。

---

## 1. 命名与单位规范

### 1.1 六条强制规则

| # | 对象 | 规则 | 正例 | 反例（v2.1 已清） |
|---|------|------|------|------------------|
| N1 | 配置文件键 | **camelCase** | `httpPort`、`qualityThreshold` | `max_consecutive_failures` |
| N2 | DB 表名/列名 | **snake_case**，与 TS 字段**机械转换** | `budget_used_tokens` | `timeoutMs` 对应列写成 `t_out` |
| N3 | TS 接口字段 | **camelCase**（`LoopConfig` 除外，见 X2） | `budgetUsedTokens` | — |
| N4 | 时长 | 键名**必带单位后缀**，数值不带单位 | `busyTimeoutMs: 5000`、`approvalTimeoutSec: 60`、`loopConsecutiveRounds: 5` | `timeout: 5000`、`monitoringInterval: "60s"` |
| N5 | 比例 / 评分 | 一律 **0.0–1.0** 浮点；DB 列 `REAL` | `qualityScore: 0.85` | `quality_score INTEGER /* 0-100 */` |
| N6 | 时间戳 | TS `number`（**epoch 毫秒**）+ `_at` 后缀；DB 列一律 `INTEGER` | `recordedAt` / `recorded_at` | ISO-8601 `TEXT` |

其他后缀：金额 `_usd`（REAL）· JSON 列 `_json` 结尾 · 布尔列 `is_*` / `*_enabled`（INTEGER 0/1）· 计数 `*Count` · 上限 `*Ceiling` · 倍率 `*Multiplier` / `*Factor`。

### 1.2 命名豁免清单（穷举，新增须先登记 Docs/11 §1.2.1）

| # | 豁免对象 | 风格 | 理由 | 边界（超出即违规） |
|---|---------|------|------|------------------|
| **X1** | `modelRouter.json` 的 `providers[]` / `models[]` **内层键** | snake_case：`base_url` `api_key_ref` `context_window` `max_output` `supports_vision` `supports_tools` `cost_per_1k_input` `cost_per_1k_output` | 上游 OpenAI / Anthropic 兼容契约 | 仅内层；`routing` 段与 `reasoningSandwich` 段仍 camelCase；`Infra/Llm` 边界层负责转成 camelCase 领域对象（如 `ModelPricing.costPer1kInput`） |
| **X2** | `loopConfig.json` 的 `loopDefaults` / `roleOverrides[*]` **内层键** ＋ TS `LoopConfig` 接口字段 | snake_case：`model` `max_iterations` `timeout_ms` `temperature` `token_budget` `stream` | skill `source-code.md`「LoopConfig 字段清单（强制）」，benchmark 数据集与 prompt frontmatter 复用同名 | 同名字段**不改名、不分层转换**；`hardLimits` / `stopRules` / `verifier` / `fingerprint` / `goalReanchor` / `failureFeedback` / `context` / `efficiency` 为本项目新增段，**仍 camelCase** |
| **X3** | 安全类枚举 | SCREAMING_SNAKE：`SAFE\|CONTROLLED\|DANGEROUS\|FORBIDDEN` · `LOW\|MEDIUM\|HIGH\|CRITICAL` · `PENDING\|APPROVED\|REJECTED\|TIMEOUT` · `REVERSIBLE\|PARTIAL\|IRREVERSIBLE` · `YES\|NO\|CONDITIONAL` | skill `security.md` 威胁分级，已在全局契约广泛引用 | **不得新增第四个大写枚举族**；其余枚举一律 lower snake_case |

> **枚举三处同形原则**：同一枚举在 **TS 联合类型 / DB 列存值 / 配置文件字面量** 三处必须逐字符相同，不做大小写化转换。违反即视为缺陷（v2.1 已修 `phase`、`status`、`risk_level`）。

---

## 2. 配置键全集（14 个文件 · 按文件分节）

图例：**T**=类型 **D**=默认值 **U**=单位/标度 **C**=消费方（`Infra`/`Tools`/`Services`/`Core`/`Interface`）

### 2.1 `Configs/default.json` — 系统基础 + 服务端口 + UI（Docs/11 §2.1）

| 键 | T | D | U | C | 说明 |
|----|---|---|---|---|------|
| `system.name` | string | `Civitas-AI` | — | Core | — |
| `system.version` | string | `0.1.0` | 语义化版本 | Interface | 随 `/api/health` 暴露 |
| `system.logLevel` | enum | `info` | `debug\|info\|warn\|error\|fatal` | Infra | 热更新会重开日志句柄 |
| `system.dataDir` | path | `Data/` | 相对根目录 | Infra | 禁止绝对路径 |
| `system.logDir` | path | `Logs/` | 同上 | Infra | — |
| `system.promptDir` | path | `Prompts/` | 同上 | Core | — |
| `server.host` | string | `0.0.0.0` | — | Interface | 生产应改 `127.0.0.1` + 反代 |
| `server.httpPort` | int | `3000` | — | Interface | — |
| `server.wsPort` | int | `3001` | — | Interface | 与 HTTP 分离，便于独立限流 |
| `server.corsOrigins` | string[] | `["http://localhost:5173"]` | — | Interface | 生产必须显式白名单，禁 `*` |
| `database.mainPath` | path | `Data/db/civitas_main.db` | — | Infra | 主业务库（§3 表） |
| `database.eventsPath` | path | `Data/db/civitas_events.db` | — | Infra | 事件溯源 + `effect_journal`；**仅追加，不作恢复入口** |
| `database.memoryPath` | path | `Data/db/civitas_memory.db` | — | Infra | 共享记忆 + 长期记忆 |
| `database.walMode` | bool | `true` | — | Infra | 关闭会导致恢复能力降级 |
| `database.busyTimeoutMs` | int | `5000` | ms | Infra | 原名 `busyTimeout`（N4 违规，已改） |
| `ui.dashboardPollIntervalSec` | int | `5` | 秒 | Interface | 大屏概览轮询（v2.1 新登记） |
| `ui.approvalQueueRefreshSec` | int | `3` | 秒 | Interface | ApprovalQueue 面板 |
| `ui.streamFlushIntervalMs` | int | `100` | ms | Interface | 流式**批量合并**，禁逐 token 推送 |
| `ui.maxEventBufferSize` | int | `500` | 条 | Interface | 超限丢最旧，不阻断 |
| `ui.wsReconnectBackoffMs` | int | `1000` | ms | Interface | 指数退避基数 |
| `ui.wsReconnectMaxAttempts` | int | `5` | 次 | Interface | 耗尽后降级纯轮询 + 离线横幅 |

### 2.2 `Configs/modelRouter.json` — 模型与路由（Docs/11 §2.2）

| 键 | T | D | U | C | 说明 |
|----|---|---|---|---|------|
| `providers[]` | array | 4 个 provider | — | Infra | 按 `provider` 名注册，全局唯一 |
| `providers[].provider` | string | `openai`/`anthropic`/`aliyun` | 短名 | Infra | X1 |
| `providers[].base_url` | string | 各厂商 | — | Infra | X1 |
| `providers[].api_key_ref` | string | `env:OPENAI_API_KEY` | `env:` 前缀 | Infra | **只存引用，禁明文/禁模板插值** |
| `providers[].models[].id` | string | `gpt-4o` 等 | — | Infra | 全局唯一标识 = `provider/model` |
| `models[].context_window` | int | `128000`/`200000`/`32000` | token | Infra/Core | **预算基数来源**；未注册模型默认 128K |
| `models[].max_output` | int | `16384`/`8192` | token | Infra | 单次输出上限（≠ `context_window`） |
| `models[].supports_vision` / `supports_tools` | bool | `true`（qwen-max vision=false） | — | Infra | 替代废弃的 `capabilities[]` |
| `models[].cost_per_1k_input` / `cost_per_1k_output` | number | 见 §2.2.1 | USD / 1K token | Services(TokenEconomy) | **唯一定价源**；`Configs/modelPricing.json` 已裁决删除（未登记 + 双事实源） |
| `routing.defaultModel` | string | `openai/gpt-4o-mini` | — | Core | — |
| `routing.directorModel` | string | `openai/gpt-4o` | — | Core | Director 规划 |
| `routing.workerModel` | string | `openai/gpt-4o-mini` | — | Core | **v2.1 补齐**；`loopDefaults.model` 取值即此键名 |
| `routing.verifierModel` | string | `anthropic/claude-sonnet-4-20250514` | — | Core | **必须 ≠ `producerModel`**（Maker≠Checker，ADR-0004） |
| `routing.arbitrationModels` | string[] | 3 个不同厂商 | — | Services | **长度 ≥3 且厂商互异**（多数决前提） |
| `routing.fallbackOrder` | string[] | `["openai","anthropic","aliyun"]` | — | Infra | Provider 降级顺序 |
| `routing.timeoutMs` | int | `60000` | ms | Infra | **单次模型调用**超时 |
| `routing.firstByteTimeoutMs` | int | `10000` | ms | Infra | 流式首字节（skill 强制） |
| `routing.interChunkTimeoutMs` | int | `15000` | ms | Infra | 块间超时（v2.1 补齐） |
| `reasoningSandwich.enabled` | bool | `true` | — | Core | 推理三明治总开关 |
| `reasoningSandwich.planningTier` / `actingTier` / `verifyingTier` | enum | `strong` / `economic` / `strong` | `strong\|economic` | Core | 三段档位 |
| `reasoningSandwich.fallbackOnBudgetSoft.*` | object | `economic/economic/strong` | 同上 | Core | **软预算命中时的降级目标**（Docs/12 §2.3 引用） |

#### 2.2.1 定价速查（展开自 `models[].cost_per_1k_*`，**非事实源**）

| provider/model | 输入 /1K | 输出 /1K | context_window |
|----------------|---------|---------|----------------|
| `openai/gpt-4o` | 2.5 | 10.0 | 128000 |
| `openai/gpt-4o-mini` | 0.15 | 0.6 | 128000 |
| `anthropic/claude-sonnet-4-20250514` | 3.0 | 15.0 | 200000 |
| `aliyun/qwen-max` | 2.0 | 6.0 | 32000 |

### 2.3 `Configs/routingRules.json` — 编排路由阈值（Docs/11 §2.3）

| 键 | T | D | U | C | 判定含义 |
|----|---|---|---|---|---------|
| `routing.consortiumTokenThreshold` | int | `50000` | token | Core | ≥ 则升 Consortium（多专家并联） |
| `routing.consortiumDomainThreshold` | int | `2` | 领域数 | Core | ≥ 则需跨领域协作 |
| `routing.delegationMaxCouplingScore` | number | `0.3` | 0–1 | Core | ≤ 才可拆给多 Worker（耦合度上限） |
| `routing.delegationMinSubtaskCount` | int | `2` | 个子任务 | Core | < 则不值得委派，走 Direct |
| `routing.assemblyLineSopRequired` | bool | `true` | — | Core | 流水线模式强制 SOP（`Skills/playbooks/`） |
| `routing.directExecMaxTokens` | int | `10000` | token | Core | ≤ 走 Direct 直执 |

> `routing` 命名空间在 `modelRouter.json`（模型选择）与 `routingRules.json`（判定阈值）**不重叠**；加载器合并为单一 `routingConfig`，**出现同名键 → 启动失败**。

### 2.4 `Configs/economyRules.json` — Token 经济（Docs/11 §2.4）

| 键 | T | D | U | C | 说明 |
|----|---|---|---|---|------|
| `economy.initialSupply` | int | `1000000` | token | Services | 初始发行量 |
| `economy.defaultWalletBalance` | int | `10000` | token | Services | 新建 Agent 钱包默认余额 |
| `economy.tax.baseRate` | number | `0.05` | 0–1 | Services | 基础税率 |
| `economy.tax.dynamicEnabled` | bool | `true` | — | Services | 动态税率开关 |
| `economy.tax.adjustmentIntervalSec` | int | `300` | 秒 | Services | 原名 `adjustmentInterval`（N4） |
| `economy.tax.maxRate` / `minRate` | number | `0.20` / `0.02` | 0–1 | Services | 动态调节边界 |
| `economy.tax.highLoadMultiplier` / `lowLoadDiscount` | number | `1.5` / `0.8` | 倍率 | Services | 高/低负载调子 |
| `economy.tax.loadThresholdAgents` | int | `10` | 活跃 Agent 数 | Services | 原名 `loadThreshold`（歧义已消） |
| `economy.profitSharing.qualityWeight` / `quantityWeight` / `efficiencyWeight` | number | `0.5` / `0.3` / `0.2` | 0–1，**和=1** | Services | **分润**权重；≠ 质量分权重（0.4/0.3/0.3） |
| `economy.rollingWindow.tokens` | int | `10000` | token | Services | 窗口消耗上限（原 `audit.rollingWindow*` 重复定义，已归一） |
| `economy.rollingWindow.windowSec` | int | `300` | 秒 | Services | — |
| `economy.rollingWindow.checkIntervalSec` | int | `5` | 秒 | Services | 原名 `checkIntervalSeconds` |
| `economy.rollingWindow.deviationMultiplierWarn` / `Critical` | number | `3` / `5` | 均值倍数 | Services | 原 `audit.deviationMultiplier` 单值拆两档 |
| `economy.rollingWindow.budgetWarnRatio` | number | `0.8` | 0–1 | Services | — |
| `economy.budgetLimits.singleCallMaxTokens` | int | `5000` | token | Infra/Tools | 单次 LLM 调用上限 |
| `economy.budgetLimits.globalTraceHardTokens` | int | `200000` | token | Core | **整 `trace_id` 硬预算**（跨 Loop） |
| `economy.budgetLimits.globalTraceSoftRatio` | number | `0.6` | 0–1 | Core | trace 级软预算比例，与 Loop 级 `softRatio` 同值但**不同层级** |

> **层级区分（易错）**：`economy.budgetLimits.globalTrace*` = 一个 trace 下**所有 Loop 之和**；`loopConfig.stopRules.budget.*` = **单个 Loop** 的 `token_budget`。两层各自独立判定，任一层命中即生效。

### 2.5 `Configs/arbitration.json` — 仲裁（Docs/11 §2.5）

| 键 | T | D | U | C | 说明 |
|----|---|---|---|---|------|
| `arbitration.coreArbitrators` | int | `1` | 个 | Services | 核心层常驻（不随负载缩） |
| `arbitration.caseArbitratorCount` | int | `3` | 个 | Services | **单案实际并发 LLM 数**（多数决需 ≥3） |
| `arbitration.minArbitrators` / `maxArbitrators` | int | `1` / `15` | 个 | Services | 辅助层动态池容量上下限 |
| `arbitration.frequencyThreshold` | number | `5` | 次/窗口 | Services | 扩触发线 |
| `arbitration.frequencyWindowSec` | int | `300` | 秒 | Services | 原 `5/5min` 复合值已拆两键 |
| `arbitration.scaleUpFactor` / `scaleDownFactor` | number | `2.0` / `0.5` | 倍率 | Services | — |
| `arbitration.monitoringIntervalSec` | int | `60` | 秒 | Services | 原名 `monitoringInterval: 60s` |
| `arbitration.consecutiveHighCount` / `consecutiveLowCount` | int | `3` / `3` | 窗口数 | Services | 连续高/低负载判定轮数 |
| `arbitration.deadlockTimeoutSec` | int | `120` | 秒 | Services | 裁决超时 → `ARBITRATION_DEADLOCK` |
| `arbitration.cpuPerArbitratorCores` | number | `0.1` | 核 | Infra | 原名 `cpuPerArbitrator` |
| `arbitration.memoryPerArbitratorMb` | int | `256` | MB | Infra | 原名 `memoryPerArbitrator: 256MB` |
| `arbitration.snapshotStore` | enum | `sqlite` | `sqlite\|redis` | Infra | **Phase 0–2 强制 `sqlite`**（§7.2） |

> **实际并发** = `min(caseArbitratorCount, 池当前规模)`；池规模由扩缩容算法维护（Docs/05 §5.2）。

### 2.6 `Configs/audit.json` — 审计（Docs/11 §2.6）

| 键 | T | D | U | C | 说明 |
|----|---|---|---|---|------|
| `audit.loopSimilarityThreshold` | number | `0.85` | 0–1 | Services | **行为指纹**相似度（≠ `memory.conflictSimilarityThreshold`，同值不同义） |
| `audit.loopConsecutiveRounds` | int | `5` | **轮** | Services | 原名 `loopConsecutiveCount`；裁决为 5（3 次那个是 fingerprint 维度） |
| `audit.patrolIntervalSec` | int | `86400` | 秒 | Services | 定期巡检周期（24h） |
| `audit.patrolTopPercentile` | number | `0.1` | 0–1 | Services | 消耗 Top 10% 标"需关注" |
| `audit.patrolFailureRateThreshold` | number | `0.3` | 0–1 | Services | — |
| `audit.patrolWindowHours` | int | `24` | 小时 | Services | — |
| `audit.freezeAutoUnfreezeSec` | int | `3600` | 秒 | Services | 自动解冻（`initiator='system'`） |
| `audit.auditRecentOpsCount` / `auditRecentTxCount` | int | `50` / `20` | 条 | Services | 稽查取数条数 |

> **已删除键**（与 §2.4 重复）：`rollingWindowTokens` `rollingWindowSeconds` `deviationMultiplier` `checkIntervalSeconds` —— **禁止在 `audit.json` 再造同名键**。

### 2.7 `Configs/supervision.json` — 监管（Docs/11 §2.7）

| 键 | T | D | U | C | 说明 |
|----|---|---|---|---|------|
| `supervision.qualityThreshold` | number | `0.60` | **0–1** | Services | 原为 `60`（百分制，N5 违规，已改）；低于此 → 质量告警 |
| `supervision.agentConsecutiveFailuresToExpel` | int | `3` | **跨任务**次数 | Services | 开除阈值；≠ `stopRules.limits.maxConsecutiveErrors`（单 Loop 内） |
| `supervision.patrolEnabled` | bool | `true` | — | Services | — |
| `supervision.broadcastAckTimeoutSec` | int | `300` | 秒 | Services | 广播 ACK 期限 |
| `supervision.triggerDedupWindowSec` | int | `300` | 秒 | Core | 触发去重窗（防风暴） |
| `supervision.triggerCooldownSec` | int | `30` | 秒 | Core | 同规则冷却 |
| `supervision.triggerIdempotencyTtlHour` | int | `24` | 小时 | Infra | 幂等记录 TTL |
| `supervision.maxConcurrentLoopsGlobal` | int | `20` | 个 | Core | 全局并发 Loop 上限 |
| `supervision.maxConcurrentLoopsPerUser` | int | `5` | 个 | Core | 单 `session_key` 上限 |
| `supervision.stormBreakerThresholdPerMin` | int | `100` | 次/分钟 | Core | 熔断触发线 |
| `supervision.stormBreakerOpenSec` | int | `60` | 秒 | Core | 熔断保持时间 |
| `supervision.lawyerLetterDedupWindowSec` | int | `300` | 秒 | Services | 律师函去重窗 |
| `supervision.approvalTimeoutSec` | int | `60` | 秒 | Core | **全局唯一审批超时源**；原 `budget.expandApprovalTimeoutSec: 20` 已废弃 |
| `supervision.approvalDefaultOnTimeout` | enum | `reject` | `reject\|abort_loop` | Core | **禁止 `approve`**（fail-closed） |
| `supervision.criticalApprovalRoles` | int | `2` | 个审批角色 | Core | `risk_level=CRITICAL` 时生效；同时强制 `decision_policy='unanimous'` |

### 2.8 `Configs/loopConfig.json` — 循环控制（Docs/11 §2.8）⭐ 最高频查阅

> **大小写混用注意**：`loopDefaults` / `roleOverrides[*]` 内层是 **snake_case**（豁免 X2）；其余段是 **camelCase**。

#### (a) `loopDefaults.*` — TS `LoopConfig` 的 6 个强制字段（一一对应，不改名）

| 配置键（snake） | TS 字段 | T | D | U | 语义（v2.1 裁决） |
|----------------|---------|---|---|---|------------------|
| `loopDefaults.model` | `model` | string | `workerModel` | **角色模型别名**（指向 §2.2 `routing.*`） | 非模型 ID；解析后才是 `provider/model` |
| `loopDefaults.max_iterations` | `max_iterations` | int | `30` | 轮 | 单层轮次上限 |
| `loopDefaults.timeout_ms` | `timeout_ms` | int | `60000` | ms | **单次模型调用**超时（skill 原义）——**不是整轮墙钟** |
| `loopDefaults.temperature` | `temperature` | number | `0.2` | 0–2 | 见 §7.3 温度红线 |
| `loopDefaults.token_budget` | `token_budget` | int | `100000` | token | **该 Loop 硬预算基准**，四档比例全部相对它 |
| `loopDefaults.stream` | `stream` | bool | `true` | — | — |

#### (b) `hardLimits.*` — 配置校验天花板（超过 → 启动失败，不是运行期中止）

| 键 | T | D | 卡住的键 |
|----|---|---|---------|
| `hardLimits.maxIterationsCeiling` | int | `200` | `max_iterations` / `stopRules.limits.maxIterations` |
| `hardLimits.timeoutMsCeiling` | int | `600000` | `timeout_ms`（**单次调用**） |
| `hardLimits.maxWallClockMsCeiling` | int | `3600000` | `stopRules.limits.maxWallClockMs`（**整轮墙钟**） |
| `hardLimits.tokenBudgetCeiling` | int | `2000000` | `token_budget` / `stopRules.budget.hardTokens` |

#### (c) `roleOverrides[role]` — 8 个角色（键必须与 `agents.role` 逐一对应）

| role | `max_iterations` | `token_budget` | `temperature` | 允许覆盖？ |
|------|-----------------|---------------|--------------|-----------|
| `prime_director` | 100 | 500000 | 0.3 | ✅ 仅此三键 |
| `partner` | 50 | 200000 | 0.3 | ✅ |
| `worker` | 30 | 100000 | 0.2 | ✅ |
| `reviewer` | 5 | 50000 | **0.0** | ✅ 温度**不得调高** |
| `assembly_node` | 10 | 60000 | 0.1 | ✅ |
| `arbitrator` | 5 | 50000 | **0.0** | ✅ 温度**不得调高** |
| `auditor` | 20 | 80000 | **0.0** | ✅ 温度**不得调高** |
| `regulator` | 15 | 80000 | 0.1 | ✅ |

> 出现 `timeout_ms` / `model` / `stream` 等**其他键 → 启动失败**。

#### (d) `stopRules.*` — 五类退出（字段名以 `Docs/12 §2.1` 为准）

| 键 | T | D | U | 退出类别 |
|----|---|---|---|---------|
| `stopRules.limits.maxIterations` | int | `30` | 轮 | ②上限 |
| `stopRules.limits.maxWallClockMs` | int | `900000` | ms | ②上限（**整轮墙钟唯一源**） |
| `stopRules.limits.maxToolCalls` | int | `100` | 次 | ②上限 |
| `stopRules.limits.maxConsecutiveErrors` | int | `3` | **连续次数** | ②上限（单 Loop 内，≠ 开除阈值） |
| `stopRules.budget.warmRatio` | number | `0.36` | ×`token_budget` | ③预热：仅记 `BUDGET_WARMING`，**不阻断** |
| `stopRules.budget.softRatio` | number | `0.6` | 同上 | ③软预算：**降级 + 并行÷2 + 继续**（组合固定，三项不拆开） |
| `stopRules.budget.expandRequestRatio` | number | `0.8` | 同上 | ③扩展审批：发 `PendingApproval(kind='budget_expand')` |
| `stopRules.budget.hardRatio` | number | `1.0` | 同上 | ③硬预算：**中止 + 回滚 `lastCheckpointId`** |
| `stopRules.noProgress.metric` | enum | `goal_distance` | `failed_tests\|validation_score\|goal_distance\|custom` | ④无进展 |
| `stopRules.noProgress.stagnationWindowRounds` | int | `3` | 轮 | ④ |
| `stopRules.noProgress.minDeltaRatio` | number | `0.05` | 0–1 相对增益 | ④ |
| `stopRules.noProgress.action` | enum | `switch_strategy` | `NextAction` 子集（§4.3） | ④ |
| `stopRules.evaluationOrder` | string[] | `["risk","limits","budget","noProgress","success"]` | — | **判定优先级，禁止调换** |

> **推导值（不单独配置，由比例算出）**：`StopRuleSet.budget.warmTokens = token_budget × warmRatio`；`softTokens / expandRequestTokens / hardTokens` 同理。`softUsd = hardUsd × softRatio`；`hardUsd` 未配置时**不参与 USD 判定**。

#### (e) `verifier.*` — 四级验证器

| 键 | T | D | U | 说明 |
|----|---|---|---|------|
| `verifier.minLevelsRequired` | int | `2` | 层 | 每 Loop 至少 L1 + 一层更高（禁只用 L3） |
| `verifier.l3MinAgreementWithHuman` | number | `0.85` | 0–1 | L3 Judge 与人工标注一致率下限 |
| `verifier.l3CalibrationDataset` | path | `Tests/GoldenSets/review-50` | — | 校准集（50 条黄金样本） |
| `verifier.l3RecalibrateIntervalDay` | int | `90` | 天 | 超期未校准 → L3 不可用，降级 L2 |
| `verifier.antiGamingMinRules` | int | `1` | 条 | 防刷：至少 N 条 L2 规则 |
| `verifier.antiGamingMaxChangedFiles` | int | `8` | 文件 | 防刷：单轮改动文件数上限（超出视为绕过） |

#### (f) `fingerprint.*` / `goalReanchor.*` / `failureFeedback.*` / `context.*` / `efficiency.*`

| 键 | T | D | U | 说明 |
|----|---|---|---|------|
| `fingerprint.singleIterationWarnThreshold` | int | `2` | 次 | 同轮内重复即预警 |
| `fingerprint.consecutiveDuplicateRounds` | int | `3` | 轮 | **连续 3 轮完全相同 → 强制终止**（Docs/06 的 5 次是审计维度，两者并存） |
| `fingerprint.windowRounds` | int | `5` | 轮 | 滑动窗 |
| `fingerprint.windowHitThreshold` | int | `3` | 次/窗 | 窗内命中即告警 |
| `fingerprint.crossAgentDuplicateThreshold` | int | `2` | 次 | 跨 Agent 重复（委派重复工检测） |
| `goalReanchor.reanchorEveryRounds` | int | `5` | 轮 | 目标重锚频率 |
| `goalReanchor.alwaysOnAfterCompression` | bool | `true` | — | 压缩后必重锚（防目标漂移） |
| `goalReanchor.injectRecentFailedStrategiesCount` | int | `5` | 条 | 注入"已试败策"条数 |
| `failureFeedback.evidenceExcerptMaxChars` | int | `500` | 字符 | 人类可读片段上限 |
| `failureFeedback.strategyTextMaxChars` | int | `30` | 字符 | 策略摘要上限（与 `strategies_ledger.strategy` 强制一致） |
| `failureFeedback.sameDefectCategoryEscalateRounds` | int | `3` | 轮 | 同缺陷类别连续出现 → 升级 |
| `failureFeedback.noImprovementAbortRounds` | int | `2` | 轮 | 无改善 → 放弃 |
| `failureFeedback.improvementMinRatio` | number | `0.05` | 0–1 | 改善判定最小相对量 |
| `failureFeedback.unreasonableGoalEscalateCount` | int | `5` | 次 | 目标不合理判定 → 上报人工 |
| `context.partitionCeilingRatio.static/lowFreq/midFreq/highFreq` | number | `0.15/0.15/0.15/0.45` | **预算上限**，和=0.90 | skill 强制；剩 10% 为余量，**不得填满** |
| `context.tokenCoefficient.static/lowFreq/midFreq/highFreq` | number | `0.05/0.5/0.3/1.0` | **压缩折算因子**（非百分比） | 与上一行**正交**，勿混用 |
| `context.outputReserveTokens` | int | `4096` | token | 先从 `context_window` 扣除，余量为分区预算基数 |
| `context.truncationUsageRatio` | number | `0.92` | 0–1 | 实际截断触发线 |
| `context.summaryTriggerUsageRatio` | number | `0.80` | 0–1 | 压缩监管监听线（skill 80%） |
| `context.scoreCompressThreshold` | number | `0.30` | 0–1 | 低分优先丢弃线 |
| `efficiency.invalidLoopRepeatRatio` | number | `0.85` | 0–1 | 无效循环：重复率 |
| `efficiency.invalidLoopGoalDelta` | number | `0.02` | 0–1 | 无效循环：目标前进量下限（两者同时命中才算） |

### 2.9 `Configs/durable.json` — 持久执行（Docs/11 §2.9）

| 键 | T | D | U | 说明 |
|----|---|---|---|------|
| `durable.checkpoint.everyIteration` | bool | `true` | — | 每轮必写（**失败轮也写**，供回滚） |
| `durable.checkpoint.afterEachEffect` | bool | `true` | — | 每个副作用后写 |
| `durable.checkpoint.beforeCompression` | bool | `true` | — | 压缩前写（防 State 与 Context 脱节） |
| `durable.checkpoint.onExplicitPause` | bool | `true` | — | 显式暂停时写 |
| `durable.checkpoint.fallbackIntervalSec` | int | `60` | 秒 | 兜底：以上全不命中时至少每 60s 一次 |
| `durable.checkpoint.keepLastCount` | int | `20` | 个 | 每 Loop 保留 checkpoint 数（**`loop_checkpoints` 保留策略的唯一源**） |
| `durable.checkpoint.milestoneKeep` | bool | `true` | — | 里程碑 checkpoint 不参与裁剪 |
| `durable.checkpoint.compactIntervalSec` | int | `86400` | 秒 | 裁剪任务周期 |
| `durable.effect.defaultTimeoutMs` | int | `30000` | ms | 写入 `effect_journal.timeout_ms` 的默认快照 |
| `durable.effect.journalRetentionDays` | int | `180` | 天 | **`effect_journal` 保留策略唯一源** |
| `durable.effect.pendingAlertThreshold` | int | `100` | 条 | `INTENT`/`EXECUTING` 堆积告警 |
| `durable.effect.unknownAlertThreshold` | int | `1` | 条 | **出现 1 条 UNKNOWN 即告警**（最严） |
| `durable.effect.writeSynchronous` | enum | `FULL` | `OFF\|NORMAL\|FULL` | SQLite `PRAGMA synchronous`；**不得降为 NORMAL** |
| `durable.idempotency.cacheTtlHour` | int | `24` | 小时 | **`idempotency_cache` 保留策略唯一源** |
| `durable.idempotency.store` | enum | `sqlite` | `sqlite\|redis` | **Phase 0–2 强制 `sqlite`** |
| `durable.recovery.autoResumeMaxUnknownEffects` | int | `0` | 条 | 硬规则：**存在任何 UNKNOWN → 禁自动恢复** |
| `durable.recovery.autoResumeMaxBudgetUsedRatio` | number | `0.8` | 0–1 | AUTO_RESUME 前置③ |
| `durable.recovery.requireHumanOnArtifactDrift` | bool | `true` | — | AUTO_RESUME 前置④ |
| `durable.recovery.startScanTimeoutSec` | int | `10` | 秒 | 启动扫描超时 → 记 WARN 不阻塞启动 |
| `durable.shutdown.flushTimeoutSec` | int | `30` | 秒 | 关闭刷盘超时 → `SHUTDOWN_FLUSH_TIMEOUT` |
| `durable.shutdown.journalFsyncTimeoutSec` | int | `5` | 秒 | journal fsync 子超时 |
| `durable.metrics.checkpointWriteDurationP99Ms` | int | `500` | ms | SLO |
| `durable.metrics.recoveryHumanRequiredAlertPerHour` | int | `5` | 次/小时 | SLO |
| `durable.metrics.idempotencyHitRatioMin` | number | `0.3` | 0–1 | 低于此说明幂等键设计有问题 |

### 2.10 `Configs/session.json` — 会话与保留（Docs/11 §2.10）

| 键 | T | D | U | 说明 |
|----|---|---|---|------|
| `session.sessionKeyHexLength` | int | `16` | 十六进制位 | `session_key` = SHA-256 前 16 位 |
| `session.archiveInactiveDays` | int | `30` | 天 | 不活跃即归档 |
| `session.historyMaxMessagesInMemory` | int | `200` | 条 | 内存驻留上限 |
| `retention.tasksActiveDays` | int | `30` | 天 | `tasks` |
| `retention.agentsDestroyedDays` | int | `90` | 天 | `agents`（已销毁） |
| `retention.operationHistoryDays` | int | `180` | 天 | `operation_history` |
| `retention.eventsDays` | int | `90` | 天 | `events` |
| `retention.loopsCompletedDays` | int | `90` | 天 | `loops`（终态） |
| `retention.pendingApprovalsDoneDays` | int | `30` | 天 | `pending_approvals`（终态） |
| `retention.verifierResultsDays` | int | `90` | 天 | `verifier_results` |
| `retention.failureFeedbacksDays` | int | `90` | 天 | `failure_feedbacks`（v2.1 补，原 Docs/10 §8.4 遗漏） |
| `retention.actionFingerprintsDays` | int | `90` | 天 | `action_fingerprints`（v2.1 补，同上） |
| `retention.longTermMemoryPermanent` | bool | `true` | — | `long_term_memories` **永久保留** |
| `retention.tokenTransactionsPermanent` | bool | `true` | — | `token_transactions` 永久（审计法定留存） |

> **禁止在 `retention.*` 造的三个键**：`effect_journal` / `idempotency_cache` / `loop_checkpoints` 的保留期分别由 `durable.effect.journalRetentionDays` / `durable.idempotency.cacheTtlHour` / `durable.checkpoint.keepLastCount` 提供。

### 2.11 `Configs/memory.json` — 共享记忆与事件总线（Docs/11 §2.11）

| 键 | T | D | U | 说明 |
|----|---|---|---|------|
| `memory.conflictSimilarityThreshold` | number | `0.85` | 0–1 | **写入冲突检测唯一阈值**（原三处同名已归一） |
| `memory.capsuleBudgetTokens` | int | `4000` | token | 冲突胶囊总预算 |
| `memory.capsuleMemoryTopK` | int | `5` | 条 | 语义召回 Top K |
| `memory.capsuleRecentOpsCount` | int | `5` | 条 | 近期操作条数 |
| `memory.capsuleMessageBudgetTokens` | int | `2000` | token | 胶囊内消息部分子预算 |
| `memory.longTermConsolidationDelayMs` | int | `5000` | ms | 沉淀延迟（合并写） |
| `memory.privateStateTtlSec` | int | `3600` | 秒 | Agent 私有态 TTL |
| `memory.vectorDim` | int | `1536` | 维 | **换 embedding 模型必须同步改**；不符 → 启动失败 |
| `memory.supersedeGraceSec` | int | `60` | 秒 | 版本被取代后的宽限期（在途读仍可见） |
| `eventBus.maxQueueSize` | int | `10000` | 条 | 满则丢弃 + `WARN`（发布不阻塞主流程） |
| `eventBus.consumerTimeoutMs` | int | `30000` | ms | 单订阅者处理超时 |
| `eventBus.retryMaxAttempts` | int | `1` | 次 | 失败重试（仅 1 次，事件不保证 exactly-once） |
| `eventBus.deadLetterKeepDays` | int | `7` | 天 | 死信保留 |
| `eventBus.store` | enum | `in-memory` | `in-memory\|redis` | **Phase 0–2 强制 `in-memory`**（§7.2） |

### 2.12 `Configs/security.json`（L0 启动锁定）· `local.json` · `{env}.json`

| 项 | 规则 |
|----|------|
| `security.json` 加载级别 | **L0**：启动读取后**不热更新**，改之须重启（防止运行期放宽安全策略） |
| 加载优先级 | `local.json` **>** `{env}.json` **>** `default.json`（`{env}` 由 `CIVITAS_ENV` 决定） |
| 环境变量角色 | **仅**解析 `api_key_ref: "env:XXX"` 引用，**不参与层级竞争**（v2.1 修正） |
| `local.json` | gitignore，仅本地开发 |
| 空配置启动 | 必须可行：`default.json` 覆盖全部键，任一缺失 → 用内置默认；**类型错误 → 启动失败**（不用默认值兜底） |

---

## 3. 三层映射表：配置键 ↔ TS 字段 ↔ DB 列

> **本表是 v2.1 冲突修复的核心成果**。开发时按此写 repository 映射层，**禁止人为改名**。
> 标记：⚡ = 曾发生冲突已修 · 🛡 = 豁免（X1/X2/X3）

### 3.1 主库 `civitas_main.db`（23 表中的 15 张）

| 表 | 关键列（DDL） | TS 字段 | 来源配置键 |
|----|--------------|---------|-----------|
| `tasks` | `route_mode` / `status` / `quality_score REAL` ⚡ / `created_at INTEGER` ⚡ | `routeMode` / `status` / `qualityScore`(0–1) / `createdAt`(ms) | 路由阈值 §2.3 |
| `subtasks` | `max_iterations` 🛡X2 / `timeout_ms` ⚡ / `token_budget` 🛡X2 | 同名（X2 不转换） | `roleOverrides[role].*` |
| `agents` | `role` / `status` / `created_at INTEGER` ⚡ | `role` / `status` / `createdAt` | `roleOverrides` 键集合必须与 `role` 全集一致 |
| `token_wallets` | `balance INTEGER`(token) / `is_frozen INTEGER` | `balance` / `isFrozen` | `economy.defaultWalletBalance` |
| `token_transactions` | `amount INTEGER` / `tx_type` / `created_at INTEGER` ⚡ | `amount` / `txType` | `economy.tax.*` |
| `consumption_records` | `calculated_cost REAL`(USD) / `prompt_tokens` / `completion_tokens` | `calculatedCost` / `promptTokens` | §2.2.1 定价 |
| `arbitration_cases` | `verdict` / `confidence REAL`(0–1) / `filed_at INTEGER` ⚡ | `verdict` / `confidence` | §2.5 |
| `sessions` | `session_key` / `trace_id` / `created_at INTEGER` ⚡ | `sessionKey` / `traceId` | `session.sessionKeyHexLength` |
| `operation_history` | `operation_id` / `created_at INTEGER` ⚡ | `operationId` | `retention.operationHistoryDays` |
| `behavior_rules` | `rule_content` / `updated_at INTEGER` ⚡ | `ruleContent` | — |
| `system_config` | `config_key` / `config_value` | — | 运行期派生值，**不替代 `Configs/*.json`** |
| `workspace_entries` | `version INTEGER` / `vector` / `written_at INTEGER` ⚡ | `version` / `writtenAt` | `memory.vectorDim`、`memory.supersedeGraceSec` |
| `long_term_memories` | `quality_score REAL`(0–1) ⚡ / `created_at INTEGER` ⚡ | `qualityScore` | `retention.longTermMemoryPermanent` |
| `loops` | `iteration` / `phase` / `budget_used_tokens` / `budget_used_usd REAL` / `stopped_reason` / `last_checkpoint_id` / `updated_at INTEGER` ⚡ | `iteration` / `phase` / `budgetUsedTokens` / `budgetUsedUsd` / `stoppedReason` / `lastCheckpointId` | §2.8 全部 |
| `strategies_ledger` | `strategy`(≤30 字) / `expected_outcome` / `actual_outcome` | `strategy` | `failureFeedback.strategyTextMaxChars` |

### 3.2 事件库 `civitas_events.db`

| 表 | 关键列 | TS | 配置键 |
|----|-------|----|-------|
| `events` | `event_id` / `event_type` / `trace_id` / `timestamp INTEGER` ⚡ / `payload_json` | `DomainEvent`（§4.4） | `eventBus.*`、`retention.eventsDays` |
| `effect_journal` | `effect_id`(=`operation_id`) / `kind` / `idempotency_key` / `status` / `started_at` / **`timeout_ms` ⚡（v2.1 补列）** / `completed_at` / `error_class` | `EffectRecord.startedAt` / **`timeoutMs`** / `completedAt` / `errorClass` | `durable.effect.defaultTimeoutMs`、`.journalRetentionDays` |

### 3.3 记忆库 + v2 新表

| 表 | 关键列 | TS | 配置键 |
|----|-------|----|-------|
| `loop_checkpoints` | `state_snapshot_json` / `artifact_manifest_json` / `pending_effects_json` / **`next_step_hint` ⚡（v2.1 补列）** / `created_at INTEGER` | `stateSnapshot` / `artifactManifest` / `pendingEffects` / `nextStepHint` | `durable.checkpoint.keepLastCount` |
| `pending_approvals` | `iteration` ⚡ / `kind` / `risk_level` / `requested_at` / **`timeout_sec` ⚡** / `expires_at` / `status` / `deciders_json` / **`decision_policy` ⚡** / **`decided_by_json` ⚡（原 `decided_by`）** / `decided_at` / **`default_on_timeout` ⚡** | `iteration` / `timeoutSec` / `decisionPolicy` / `decidedBy` / `defaultOnTimeout` | `supervision.approvalTimeoutSec`、`.criticalApprovalRoles` |
| `verifier_results` | `level` / `verifier_kind` / `pass INTEGER(0/1)` / `evidence_json` / `defect_category` / `cost_tokens` / `duration_ms` / `judge_model` | `level` / `verifierKind` / `pass: boolean` / `defectCategory` / `durationMs` | `verifier.*` |
| `failure_feedbacks` | `category`(FailureCategory) / `evidence_json` / `compared_with_last_json` / `tried_strategies_json` / `remaining_budget_json` / `recommended_next_action` | `failureCategory` / `evidence` / `comparedWithLastAttempt` / `triedStrategies` / `remainingBudget` / `recommendedNextAction` | `failureFeedback.*`、`retention.failureFeedbacksDays` |
| `idempotency_cache` | `idem_key` / `result_hash` / `expires_at INTEGER` | `idemKey` / `resultHash` | `durable.idempotency.*` |
| `action_fingerprints` | **复合主键 `(fingerprint, loop_id, iteration)` ⚡** / `tool_name` / `args_hash` / **`count_in_iteration` ⚡** / **`recorded_at` ⚡** / `FK → loops ON DELETE CASCADE ⚡` | `LoopState.actionFingerprints[]`（同名直存） | `fingerprint.*`、`audit.loopConsecutiveRounds`、`retention.actionFingerprintsDays` |

### 3.4 曾出现的**双事实源**与最终裁决

| 参数 | 冲突表现 | 唯一源（v2.1） |
|------|---------|--------------|
| 整轮超时 | `LoopConfig.timeout_ms=900_000` 与 `stopRules.limits.maxWallClockMs=900000` 语义重叠 | **`stopRules.limits.maxWallClockMs`**；`timeout_ms` 回退为单次调用 60000 |
| 审批超时 | `supervision.approvalTimeoutSec: 60` vs `economyRules.budget.expandApprovalTimeoutSec: 20` | **`supervision.approvalTimeoutSec`**（20 那个键已废弃） |
| 预算档位 | Docs/03 写 50%/80%/100% · Docs/04 写 `taskSoft` + `soft×0.6` 嵌套 · Docs/11 写四档平铺 | **`stopRules.budget.*Ratio`**（四档平铺，全部相对 `token_budget`） |
| 滚动窗口 | `economy.rollingWindow*` 与 `audit.rollingWindow*` 同值两份 | **`economy.rollingWindow`** |
| 相似度阈值 | `memory.similarityThreshold` / `arbitration.conflictSimilarityThreshold` / `memory.conflictSimilarityThreshold` 三名同值 | **`memory.conflictSimilarityThreshold`**（审计 `loopSimilarityThreshold` 语义不同，保持独立） |
| 分区比例 | `partitionRatio 30/35/20/15`（Docs/11）vs `S15/L15/M15/H45`（Docs/01）vs 系数 0.05/0.5/0.3/1.0（skill） | **两个正交键并存**：`context.partitionCeilingRatio`(0.15/0.15/0.15/0.45) + `context.tokenCoefficient`(0.05/0.5/0.3/1.0) |
| 模型定价 | `Configs/modelPricing.json`（未登记）vs `modelRouter.json → cost_per_1k_*` | **`modelRouter.json`**；`modelPricing.json` 删除 |
| `effect_journal` 保留 | `retention.effectJournalDays` vs `durable.effect.journalRetentionDays` | **`durable.*`** |
| 幂等缓存保留 | `retention.idempotencyCacheHour` vs `durable.idempotency.cacheTtlHour` | **`durable.*`** |
| 循环检测次数 | Docs/03 "连续 3 次" vs Docs/06 "5 次" | 两个不同参数：**`fingerprint.consecutiveDuplicateRounds=3`**（引擎层）与 **`audit.loopConsecutiveRounds=5`**（审计层），都保留 |
| UNKNOWN 判据 | 时间基（`started_at+timeout<now`）vs `iteration` 基 | **时间基唯一**；`iteration` 不参与判定 |
| 事件发布方 | `WALLET_FROZEN` 三处分别写 Audit / Regulation / TokenEconomy | **`DomainEvent.source` = 执行状态变更的组件**；触发方记 `payload.initiator`。`WALLET_FROZEN/UNFROZEN/TOKEN_CONFISCATED` 均由 **TokenEconomy** 发布 |
| `KNOWLEDGE_CONSOLIDATION` | Loop 成功退出与仲裁裁决各发布一次 | **仅由 Loop 成功退出发布**（Docs/12 §2.3），仲裁不重复发布 |
| checkpoint 写入主干 | 先写文件再写 DB vs DB 事务为主 | **DB 事务为主干**：`BEGIN IMMEDIATE → UPDATE loops → INSERT loop_checkpoints → UPDATE last_checkpoint_id → COMMIT → fsync`，文件副本异步导出（失败仅 WARN） |

---

## 4. 枚举全集（按作用域分组）

> 开发时直接从这里复制联合类型；**任何不在本表的取值均为缺陷**。完整语义看「定义位置」。

### 4.1 工具安全类（🛡 X3：SCREAMING_SNAKE）

| 枚举 | 取值 | 定义位置 |
|------|------|---------|
| `ToolSpec.dangerLevel` | `SAFE` \| `CONTROLLED` \| `DANGEROUS` \| `FORBIDDEN` | Docs/11 §6（skill `security.md`） |
| `RiskLevel`（审批 `risk_level`） | `LOW` \| `MEDIUM` \| `HIGH` \| `CRITICAL` | Docs/12 §6、`pending_approvals.risk_level` |
| 审批 `status` | `PENDING` \| `APPROVED` \| `REJECTED` \| `TIMEOUT` | `pending_approvals.status` |
| `ToolSpec.idempotency` | `YES` \| `NO` \| `CONDITIONAL` | Docs/11 §6 |
| `ToolSpec.reversibility` | `REVERSIBLE` \| `PARTIAL` \| `IRREVERSIBLE` | Docs/11 §6 |
| `EffectRecord.status` | `INTENT` \| `EXECUTING` \| `SUCCEEDED` \| `FAILED` \| `UNKNOWN` | `effect_journal.status` |
| `RecoveryStrategy` | `AUTO_RESUME` \| `REQUIRE_HUMAN` \| `RESTART_FROM_SCRATCH` \| `REBUILD_FROM_JOURNAL` | **Docs/13 §6.1 全集唯一定义处** |

### 4.2 业务状态类（lower snake_case）

| 枚举 | 取值 | 定义位置 |
|------|------|---------|
| `loops.phase` | `planning` \| `acting` \| `verifying` \| `awaiting_approval` \| `completed` \| `failed` | `loops.phase`（Docs/10 §8.2） |
| `loops.stopped_reason` | `success` \| `limits` \| `budget_soft` \| `budget_hard` \| `no_progress` \| `risk` \| `human_abort` | Docs/10 §8.2（与 Docs/12 §2 五类退出同名；`budget_soft/hard` 拆两值） |
| `tasks.status` | `pending` \| `running` \| `completed` \| `failed` \| `cancelled` | Docs/10 §2.1 |
| `subtasks.status` | `pending` \| `assigned` \| `running` \| `completed` \| `failed` | Docs/10 §2.2 |
| `agents.status` | `creating` \| `ready` \| `running` \| `suspended` \| `expelled` \| `destroyed` | Docs/10 §2.3 |
| `token_wallets.status` | `active` \| `frozen` \| `closed` | Docs/10 §2.4 |
| `arbitration_cases.status` | `filed` \| `arbitrating` \| `verdict_reached` \| `restoring` \| `closed` \| `deadlocked` | Docs/10 §2.7 |
| `sessions.status` | `active` \| `archived` | Docs/10 §2.8 |
| `operation_history.status` | `success` \| `failed` \| `timeout` | Docs/10 §2.9 |
| `workspace_entries.status` | `active` \| `superseded` \| `discarded` | Docs/10 §3 |
| `long_term_memories.status` | `active` \| `deprecated` \| `contradicted` | Docs/10 §3 |
| `pending_approvals.kind` | `tool_execute` \| `budget_expand` \| `irreversible_action` \| `judge_override` | Docs/10 §8.2 |
| `pending_approvals.decision_policy` | `majority` \| `unanimous`（`CRITICAL` **强制** `unanimous`） | Docs/10 §8.2 |
| `pending_approvals.default_on_timeout` | `reject` \| `abort_loop`（**无 `approve`**） | Docs/10 §8.2 |
| `effect_journal.kind` | `tool_execute` \| `file_write` \| `http_call` \| `db_write` \| `agent_message_send` \| `token_debit` \| `external_api` | Docs/10 §8.2 |
| `effect_journal.error_class` | `retryable` \| `non_retryable` \| `unknown` | Docs/10 §8.2 |

### 4.3 Loop / 验证类

| 枚举 | 取值 | 定义位置 |
|------|------|---------|
| `FailureCategory`（**8 成员唯一定义处**） | `schema` \| `logic` \| `external` \| `timeout` \| `risk` \| `no_progress` \| `budget_soft` \| `budget_hard` | Docs/12 §7.2（= `failure_feedbacks.category`） |
| `NextAction`（**动作全集 5 值**） | `switch_strategy` \| `request_approval` \| `abort` \| `escalate_human` \| `rollback_and_abort` | Docs/12 §7.2 |
| ├─ `stopRules.noProgress.action` 子集 | `switch_strategy` \| `escalate_human` \| `abort`（无进展时请求审批无意义） | Docs/12 §7.2 |
| ├─ `stopRules.riskTriggers.action` 子集 | `pause_and_request_approval` \| `abort` \| `rollback_and_abort`（风险时不得自行换策略） | Docs/12 §7.2 |
| `verifier_results.level` | `L1` \| `L2` \| `L3` \| `L4` | Docs/12 §3.1 |
| `verifier_results.verifier_kind` | `test` \| `schema` \| `numeric` \| `rule` \| `golden` \| `llm_judge` \| `human_gate` | Docs/10 §8.2 |
| `defect_category` | `spec_missing` \| `logic_error` \| `quality_low` \| `risk_violation` | Docs/10 §8.2 |
| `stopRules.noProgress.metric` | `failed_tests` \| `validation_score` \| `goal_distance` \| `custom` | Docs/12 §2.1 |
| `stopRules.evaluationOrder` 成员 | `risk` \| `limits` \| `budget` \| `no_progress` \| `success` | Docs/11 §2.8（顺序不可改） |
| `evidence.kind` | `test_output` \| `error_stack` \| `diff_from_expected` \| `tool_error` \| `rule_violation` \| `judge_rubric` | Docs/12 §7.2 |

### 4.4 事件与优先级

| 枚举 | 取值 | 说明 |
|------|------|------|
| `DomainEvent.priority` | `low` \| `normal` \| `high` \| `critical` | Docs/07 §4.2 |
| `EventType` 域前缀（**10 个，不得新增**） | `task` `agent` `token` `arbitration` `regulation` `audit` `memory` `loop` `durable` `system` | 命名格式固定 `<域>:<小写 snake_case 动作>` |
| `MessageType`（Docs/08，**与 `EventType` 不同命名空间**） | `TASK_ASSIGN` `TASK_START` `TASK_PROGRESS` … `ACK`（25 值） | Agent 间**点对点消息**类型；**不进事件总线、不写入 `events.event_type`**。与 `EventType.TASK_STARTED` 同名相近但分属不同命名空间，不构成废弃关系 | 

### 4.5 接口与路由类

| 枚举 | 取值 | 定义位置 |
|------|------|---------|
| `tasks.route_mode` 🛡X4 | `DIRECT` \| `DELEGATION` \| `CONSORTIUM` \| `ASSEMBLY_LINE` | Docs/10 §2.1（大写原因：与 §2.3 路由判定输出的模式名逐字对齐，登记为 X4） |
| `ToolSpec.sideEffectScope` | `none` \| `workspace` \| `filesystem` \| `network` \| `process` \| `external` | Docs/11 §6 |
| `ToolSpec.sandboxMode` | `strict` \| `standard` \| `none` | Docs/11 §6 |
| `reasoningSandwich.*Tier` | `strong` \| `economic` | Docs/11 §2.2 |
| `ServerMessage.type`（WS） | 事件帧：直接取 `EventType` 成员值（如 `loop:started` \| `agent:stream_chunk`）；RPC 帧：`error` \| `response` | Docs/08 §5.1（🔴 2026-09-14 修订：旧值 `task_update`/`agent_update`/`stream_chunk` 作废，与 Docs/07 §4.3 归一） |
| `ClientMessage.type`（WS） | `subscribe` \| `unsubscribe` \| `get_dashboard` \| `get_approvals` \| `ping` | Docs/08 §5.1（🔴 按代码现态登记；旧五值 `submit_task`/`cancel_task`/`query_status`/`query_agent`/`query_token` 均未实现，写操作裁决走 REST） |
| REST 路径权威表 | 见 `Docs/16 §附录 A` | 🔴 Docs/08 §5.2 与代码存在偏离，**以代码为准**（如 `/api/loops/dashboard` ≠ `/api/dashboard/overview`） |

### 4.6 副作用分类对照（易错：三套体系必须能互推）

| `ToolSpec.sideEffectScope` | → `effect_journal.kind` | → 是否必须写 journal | 恢复策略默认 |
|---------------------------|------------------------|--------------------|-------------|
| `none` | —（不记录） | ❌ | `AUTO_RESUME` |
| `workspace` | `db_write` | ✅ | `AUTO_RESUME` |
| `filesystem` | `file_write` | ✅ | `AUTO_RESUME`（有 checkpoint）／`REBUILD_FROM_JOURNAL` |
| `network` | `http_call` \| `external_api` | ✅ | UNKNOWN 风险高 → `REQUIRE_HUMAN` |
| `process` | `tool_execute` | ✅ | `REQUIRE_HUMAN` |
| `external` | `external_api` \| `token_debit` \| `agent_message_send` | ✅ | `REBUILD_FROM_JOURNAL` |

> **硬规则**：`reversibility ∈ {IRREVERSIBLE, PARTIAL}` 或 `sideEffectScope ∈ {network, process, external}` 的工具调用，**必须在执行前写 `INTENT`**（`wrapToolCall` 的 `EffectJournal`，order=5，Docs/02 §4），否则重启后无法判定是否已执行。

---

## 5. 核心接口契约速查

### 5.1 `LoopConfig`（🛡 X2：全字段 snake_case，不得改名）

```typescript
// 由 skill `source-code.md` 强制的 6 字段；字段名 = loopConfig.json 键名（同名直存 DB）
interface LoopConfig {
  model: string;            // 角色模型别名（非模型 ID），解析自 routing.*
  max_iterations: number;   // 轮
  timeout_ms: number;       // 单次模型调用超时，默认 60000
  temperature: number;      // 0–2
  token_budget: number;     // 本 Loop 硬预算基准（token）
  stream: boolean;
}
```

### 5.2 `DomainEvent`（事件总线唯一信封）

```typescript
interface DomainEvent {
  eventId: string;
  eventType: EventType;      // 🔒 枚举，禁自由字符串（Docs/07 §4.3）
  traceId?: string;
  loopId?: string;           // v2.1 新增：Loop 级事件必填
  timestamp: number;         // v2.1：epoch ms（原 ISO string）
  priority: 'low'|'normal'|'high'|'critical';
  payload: Record<string, unknown>;   // 触发方记入 payload.initiator
  source: string;            // 执行状态变更的组件（非触发方）
}
```

### 5.3 `EffectRecord` / `RecoveryPlan`

```typescript
interface EffectRecord {
  effectId: string;          // = operation_id
  kind: EffectKind;
  idempotencyKey: string;
  status: 'INTENT'|'EXECUTING'|'SUCCEEDED'|'FAILED'|'UNKNOWN';
  startedAt: number;         // epoch ms
  timeoutMs: number;         // = durable.effect.defaultTimeoutMs（30000）快照
  completedAt?: number;
  errorClass?: 'retryable'|'non_retryable'|'unknown';
}
interface RecoveryPlan {
  loopId: string;
  strategy: RecoveryStrategy;         // 4 值全集，Docs/13 §6.1
  lastCheckpointId?: string;
  unknownEffects: EffectRecord[];
  estimatedWastedTokens: number;
  reason: string;
}
```

**UNKNOWN 判定（唯一判据）**：`status == 'EXECUTING' AND startedAt + timeoutMs < now`（`iteration` 不参与）。

### 5.4 参数访问约束

| 约束 | 说明 |
|------|------|
| 禁止字面量 | `Src/**` 内不得出现 §2 已登记参数的字面量（如 `900_000`、`0.85`、`30000`）；一律走 `ConfigService.get()` |
| 禁止默认值兜底 | 配置**类型错误** → 启动失败；**键缺失** → 用 `default.json` 内置值（不写代码内默认） |
| 启动校验器 | 必项校验：§7 全部硬约束 + `roleOverrides` 键集与 `agents.role` 全集一致 + `memory.vectorDim` 与模型一致 + 三处枚举同形 |
| L0 拒热更新 | `security.json` 修改必须重启；收到其 reload 事件 → 拒绝并记 `CONFIG_RELOAD_FAILED` |

---

## 6. 全局 ID 与幂等键（粒度不得混用）

| ID | 粒度 | 生成时机 | 格式 | 对应列 | 配置键 |
|----|------|---------|------|--------|--------|
| `session_key` | 一个逻辑会话 | 会话首次创建 | SHA-256 前 **16** 位 hex | `sessions.session_key`、`loops.session_key` | `session.sessionKeyHexLength` |
| `trace_id` | 一次运行会话（可跨多轮） | 运行启动 | UUID v4 | `tasks.trace_id`、`loops.trace_id`、`events.trace_id` | — |
| `operation_id` | 单次工具/文件/命令调用 | 操作发起 | `{timestamp_ms}-{4位hex}` | `operation_history.operation_id`、`effect_journal.effect_id` | — |
| `loop_id` | 一个 Loop | Loop 创建 | UUID v4 | `loops.loop_id`（其余表 FK） | — |
| `agent_id` | 一个 Agent | 招募时 | `{role}-{uuid8}` | `agents.agent_id` | — |
| `task_id` / `subtask_id` / `approval_id` / `checkpoint_id` / `feedback_id` / `result_id` / `case_id` | 各自实体 | 创建时 | UUID | 同名 `_id` 列 | — |

**幂等键全集**：

| 场景 | 幂等键表达式 | 存储 | TTL |
|------|-------------|------|-----|
| 工具调用去重（重启重放） | `{loop_id}:{operation_id}:{payload_hash}` | `idempotency_cache.idem_key` | `durable.idempotency.cacheTtlHour` |
| 副作用 journal 唯一定位 | `UNIQUE(loop_id, idempotency_key)` | `effect_journal` | `durable.effect.journalRetentionDays` |
| 行为指纹（循环检测） | `SHA-256(toolName + canonicalJson(args))` | `action_fingerprints` **主键 `(fingerprint, loop_id, iteration)`**（v2.1 修：原单列主键会跨 Loop 碰撞） | `retention.actionFingerprintsDays` |
| 监管触发防风暴 | `{rule_id}:{trigger_hash}` | 触发记录 | `supervision.triggerIdempotencyTtlHour` |
| 事件消费去重 | `event_id` | 订阅者本地 | — |

> **`session_key` 与 `trace_id` 的关系**（skill 全局约定）：一个 `session_key` 恢复运行后会产生**新的** `trace_id`，两者通过会话历史关联；禁止用 `trace_id` 做用户维度聚合，禁止用 `session_key` 做单次运行追踪。

---

## 7. 硬约束与不变式（违反 = 启动失败或门禁拦截）

### 7.1 数值天花板（配置校验）

```
loopDefaults.max_iterations / roleOverrides[*].max_iterations / stopRules.limits.maxIterations
    ≤ hardLimits.maxIterationsCeiling            (200)
loopDefaults.timeout_ms                          ≤ hardLimits.timeoutMsCeiling            (600000)
stopRules.limits.maxWallClockMs                  ≤ hardLimits.maxWallClockMsCeiling       (3600000)
loopDefaults.token_budget / roleOverrides[*].token_budget
                                                 ≤ hardLimits.tokenBudgetCeiling          (2000000)
stopRules.budget.warmRatio < softRatio < expandRequestRatio < hardRatio ≤ 1.0
economy.profitSharing.{quality+quantity+efficiency}Weight 之和 == 1.0（允许 1e-9 误差）
economy.tax.minRate ≤ economy.tax.baseRate ≤ economy.tax.maxRate
context.partitionCeilingRatio 四项之和 ≤ 1.0 且默认 0.90
routing.arbitrationModels.length ≥ 3 且厂商互异
verifier.minLevelsRequired ≥ 2
```

### 7.2 Phase 0–2 **禁 Redis 三连**（必须同时成立）

| 键 | 强制值 |
|----|--------|
| `durable.idempotency.store` | `sqlite` |
| `arbitration.snapshotStore` | `sqlite` |
| `eventBus.store` | `in-memory` |

> 违反后果：单发式本地部署引入额外运维依赖，且与 SQLite 事务主干产生双写不一致。Redis 仅作为 Phase 3 升级选项登记。

### 7.3 不可调低的保守项

| 约束 | 理由 |
|------|------|
| `reviewer` / `arbitrator` / `auditor` 的 `temperature` **不得 > 0** | 违反 P2 Maker≠Checker 的可重现性假设 |
| `durable.effect.writeSynchronous` **不得降为 `NORMAL`/`OFF`** | 掉电丢 journal = 失去 UNKNOWN 恢复能力 |
| `supervision.approvalDefaultOnTimeout` **不得为 `approve`** | fail-closed |
| `durable.recovery.autoResumeMaxUnknownEffects` **不得 > 0** | 存在 UNKNOWN 即禁自动恢复 |
| `verifier.l3MinAgreementWithHuman` **不得 < 0.8** | Judge 校准下限 |
| `reasoningSandwich.verifyingTier` **不得降为 `economic`** | 软预算降级也必须保留强验证 |
| 任何表的时间列 **不得改回 `TEXT`** | Docs/10 §1.1 |
| `stopRules.evaluationOrder` **不得调换顺序** | 成功判定必须在所有失败判定之后（防“Agent 自称完成”红利） |

### 7.4 存储层不变式

| 不变式 | 作用域 |
|--------|------|
| `PRAGMA foreign_keys = ON`（连接初始化） | 否则 CASCADE 静默失效 |
| 子表必须有 `FK ... ON DELETE CASCADE` | v2.1 已补 `action_fingerprints` |
| `events` / `effect_journal` 仅追加 | 不作恢复入口；恢复只读 `loop_checkpoints` + `effect_journal` |
| `LoopState` 独立存 `Data/Loops/{session_key}/state.json` | 上下文分区仅用于装配；压缩不得破坏 State |
| 账本守恒：`initialSupply == Σ(balance) + 系统池 + 已销毁` | Docs/04 §7；不平 → `LEDGER_MISMATCH` |

---

## 8. v2.1 冲突裁决总表

图例：**✅已修** = 源文档已回写 · **🔗引用化** = 默认值已收敛至 Docs/11 · **🛡豁免** = 保留但已登记 · **⏳待办** = 本轮未改（需你裁决）

| # | 冲突 | 处置 | 状 |
|---|------|------|-----|
| 1 | 全库 31 个时间列 `TEXT` / `INTEGER` 双标 | 统一 `INTEGER` epoch ms，入 Docs/10 §1.1 | ✅ |
| 2 | `time_limit_ms` vs `timeout_ms` 双名 | 统一 `timeout_ms` | ✅ |
| 3 | `quality_score INTEGER(0-100)` | 改 `REAL(0-1)` | ✅ |
| 4 | `pending_approvals` 缺 `iteration`/`timeout_sec`/`decision_policy`/`default_on_timeout` | 全补（闭环 Docs/12 §6 悬空引用） | ✅ |
| 5 | `decided_by TEXT` 存 JSON | 改 `decided_by_json` | ✅ |
| 6 | `action_fingerprints` 单列主键 → 跨 Loop 碰撞且无 FK | 改复合主键 + FK CASCADE + `count_in_iteration` + `recorded_at` | ✅ |
| 7 | `LoopConfig` 拟改 camelCase（会违反 skill） | **不改动**，登记豁免 **X2** | 🛡 |
| 8 | `timeout_ms=900_000`（整轮）与 `maxWallClockMs` 双事实 | `timeout_ms` 回 60000（单次调用），整轮唯一由 `maxWallClockMs` | ✅ |
| 9 | 审批超时 20s vs 60s | 废 `expandApprovalTimeoutSec`，统一 `supervision.approvalTimeoutSec=60` | ✅ |
| 10 | 预算比例三套口径（50/80/100、taskSoft×0.6、四档） | 统一 `stopRules.budget.*Ratio` 平铺四档 | ✅ |
| 11 | 滚动窗口在 `economy.*` 与 `audit.*` 各一份 | 归 `economy.rollingWindow`，`audit.json` 删同名键 | ✅ |
| 12 | 相似度阈值三名同值 | 归 `memory.conflictSimilarityThreshold`；审计侧语义不同保留 | ✅ |
| 13 | 分区比例 30/35/20/15 vs 15/15/15/45 vs 系数 0.05/0.5/0.3/1.0 | 辨明为**两个正交量**，键名拆为 `partitionCeilingRatio` + `tokenCoefficient` | ✅ |
| 14 | `Configs/modelPricing.json` 未登记且与路由表双源 | 删该文件，定价统一 `modelRouter.json` | ✅ |
| 15 | `ModelPricing.promptCostPer1K`（废弃字段名） | 改 `costPer1kInput`/`costPer1kOutput` | ✅ |
| 16 | Docs/04/05/06/07/10/13 各自维护配置默认值 | 全量改**引用表**（只列键名与定义位置） | 🔗 |
| 17 | Docs/02 §12 自带 T6 角色数值表（与 §2.8 不兼容） | 删表，改指 `roleOverrides`；声明只允许覆盖三键 | 🔗 |
| 18 | Docs/05 §5.4 默认值表含 `60s`/`256MB` 等带单位字串 | 改引用 + 键名补单位后缀 | ✅ |
| 19 | 仲裁计数口径（固定 3 vs 池 1~15） | 拆 `coreArbitrators` / `caseArbitratorCount` / `min~maxArbitrators` 三层 | ✅ |
| 20 | 20 个事件名未注册（违反“禁自由字符串”） | Docs/07 §4.3 新增 `loop:`/`durable:`/`system:` 三域共 20+ 成员 | ✅ |
| 21 | 旧事件名 `LOOP_DETECTED`/`TASK_START`/`APPROVAL_TIMEOUT`/`MEMORY_CONFLICT`/`COMPENSATION_*` | 登记废弃映射表，均归正式名 | ✅ |
| 22 | `DomainEvent.timestamp: string`（ISO）与全局 epoch ms 冲突 | 改 `number`；Docs/08 两处同步 | ✅ |
| 23 | `WALLET_FROZEN` 发布方三处不一致 | 统一为 **TokenEconomy**，触发方记 `payload.initiator` | ✅ |
| 24 | `KNOWLEDGE_CONSOLIDATION` 重复发布（一次任务写两份长期记忆） | 仅由 Loop 成功退出发布 | ✅ |
| 25 | UNKNOWN 判据时间基 vs `iteration` 基 | 时间基唯一 | ✅ |
| 26 | `EffectRecord.timeout` 引用不存在的列 | 补 `effect_journal.timeout_ms` 列 | ✅ |
| 27 | AUTO_RESUME 只判 1/4 前置（首轮崩溃会被误判） | 四并联前置写进决策矩阵（Docs/13 §6.2） | ✅ |
| 28 | `RecoveryStrategy` 在多处出现不足值集 | Docs/13 §6.1 定为 4 值全集唯一定义处 | ✅ |
| 29 | §5.3 先写文件再写 DB，与 SQLite SSOT 冲突 | 改 DB 事务为主干，文件副本异步（失败仅 WARN） | ✅ |
| 30 | 指标名 `execution_journal` 表不存在 | 改 `effect_journal` | ✅ |
| 31 | `wallet_transaction` 表名不存在 | 改 `token_transactions` | ✅ |
| 32 | `retention` 遗漏 `failure_feedbacks` / `action_fingerprints` | 各补 90 天，与 `loops` 对齐 | ✅ |
| 33 | `retention.effectJournalDays` 与 `durable.effect.journalRetentionDays` 双源 | 删 `retention.*` 中的三项，统一 `durable.*` | ✅ |
| 34 | 配置优先级写“环境变量 > local > default”（与全局约定冲突） | 改 `local > {env} > default`；环境变量仅解析 `env:` 引用 | ✅ |
| 35 | `Configs/*.toml` 与 `.json` 混用 | 扩展名统一 `.json`，Docs/02 目录树重写 | ✅ |
| 36 | UI 参数（“每 5 秒轮询”）无配置家 | 新增 `default.json → ui.*` 6 键并引用化 | ✅ |
| 37 | `agents.role` 无 `reviewer`，但 Docs/03 §7.3 定义了它 | Docs/10 §2.3 注释补 `reviewer`，与 `roleOverrides` 8 键对齐 | ✅ |
| 38 | 跨文档事件发布方无统一口径 | 定为“`source` = 执行状态变更的组件”，Docs/04/06/07 三处同步 | ✅ |
| 39 | `Prompts/roles/` 目录树缺 `reviewer.md` | 已补入 `Docs/02 §2` 目录树（与 §2.8 8 角色对齐） | ✅ |
| 40 | Docs/02 §6 状态机表用中文态名（执行/挂起/销毁），与 `agents.status` 英文枚举不同形 | 已统一为 `规范值(中文名)` 写法 | ✅ |
| 41 | `tasks.route_mode` 为大写，而 X3 只允许三个安全类大写族 | 已登记为 **X4**（与 §2.3 路由输出模式名逐字对齐，跨 4 文档引用，改名成本 > 收益） | 🛡 |
| 42 | Docs/12 §5 指纹检测与 Docs/06 审计检测次数相同会被误认为同一值 | 已辨明为两个独立参数（3 轮引擎硬终止 / 5 轮审计告警） | ✅ |
| 43 | 按角色差异化分区上限（Docs/01 §2.4 承诺）无配置键 | Phase 0–2 已明确**不生效**；如需先登记 `context.partitionCeilingByRole[role]` 再实现 | ⏳ |

### 8.1 待办汇总（仅一项，需你确认后执行）

| # | 事项 | 建议处置 |
|---|------|---------|
| 43 | `context.partitionCeilingByRole[role]`（Docs/01 §2.4 承诺的按角色差异化） | Phase 0–2 明确**不生效**；如需，先登记键再实现 |

---

## 9. 开发自查清单（每次改参数跑一遍）

```
[ ] 新键已在 Docs/11 §2 登记并给默认值？
[ ] 新键带单位后缀（_ms/_sec/_min/_hour/_days/_rounds）？
[ ] 新数值未在 Docs/02–§09 重复写默认值（只引用）？
[ ] 新表列已在 Docs/10 定义，时间列为 INTEGER，JSON 列以 _json 结尾？
[ ] 比例列为 REAL 且 0–1？金额列带 _usd？
[ ] 新事件名已入 Docs/07 §4.3？带正确域前缀？未使自由字符串？
[ ] 新枚举已在本文 §4 登记？TS/DB/配置 三处同形？
[ ] 新 ID 已查 §6 可否复用？
[ ] snake↔camel 映射机械吗（无人为改名）？LoopConfig 例外已标 X2？
[ ] 未违反 §7.2 禁 Redis 三连 / §7.3 保守项？
[ ] 若引入新命名风格，已先登记豁免清单（X1–X4）再实现？
```

---

## 10. 相关文档

| 主题 | 入口 |
|------|------|
| 配置加载与热更新、安全策略 | `Docs/11-配置体系与安全/配置体系与安全设计.md` |
| DDL 与存储层不变式 | `Docs/10-数据模型与持久化/数据模型与持久化设计.md` |
| 事件驱动与共享记忆 | `Docs/07-共享记忆与上下文系统/共享记忆与上下文系统设计.md` |
| Loop 停止规则 / Verifier / FailureFeedback | `Docs/12-循环控制系统/循环控制系统设计.md` |
| EffectJournal / Checkpoint / Recovery | `Docs/13-持久执行与恢复/持久执行与恢复设计.md` |
| 构建顺序（S0–S14）与门禁 | `Docs/14-构建与实施/项目构建顺序.md` |
| 版本变更历史 | `Docs/CHANGELOG.md` |
