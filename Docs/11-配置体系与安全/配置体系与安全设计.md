# 配置体系与安全设计

> 本文档定义 Civitas-AI 的配置管理方案和安全威胁模型，包括配置项汇总、加载策略、安全信任模型、注入防御和审计机制。
>
> **本文档 §2 是全局配置键的唯一事实源（Single Source of Truth）**。其他任何设计文档 **禁止重复定义配置键与默认值**，只能引用；
> 开发时的统一查询入口为 `Docs/15-参数总典与接口契约/参数总典.md`（含跨层参数映射与冲突裁决）。

---

## 1. 配置管理

> **2026-09-22 代码校准基线**：对 `configLoader.ts` 实际加载的 12 个 JSON 做叶子键扫描，
> **204 个叶子键中 104 个（51%）在 `Src/` 内无任何字面量引用**（含 `hardLimits.*`、`verifier.*`、`context.*`、
> `retention.*`、`supervision.*` 的 trigger/storm 段、`durable.checkpoint.*`、`durable.metrics.*`、`reasoningSandwich.*` 全部）。
> 故 §2 各参数表自本修订起带 **生效状态** 列：
> - **✅ 运行期消费** = 该键被运行期读出并传入实际组件（当前唯一读者为 `Src/main.ts` 的 `getConfigValueOr` 调用）；
> - **⚠️ 未接线** = 键在 `Configs/` 中存在，但运行期无人读取；代码中生效的是同名硬编码字面量，改文件不改变行为。

### 1.1 配置文件清单

| 文件 | 路径 | 用途 | 可变性级别 |
|------|------|------|-----------|
| `default.json` | `Configs/default.json` | 全局默认配置（**所有键必须有默认值**） | L0（启动锁定） |
| `{env}.json` | `Configs/dev.json` / `Configs/prod.json` | 环境层配置，`{env}` 由 `CIVITAS_ENV` 指定，缺省 `dev`。**⚠️ 2026-09-22 校准：两文件当前均不存在，属可选层**（`configLoader.ts:62-69` 缺失即跳过） | L1（启动时读取） |
| `local.json` | `Configs/local.json` | 本地覆盖（gitignore）。**⚠️ 2026-09-22 校准：文件当前不存在，属可选层**；且见 §1.2 的覆盖失效问题 | L1（启动时读取） |
| `loopConfig.json` | `Configs/loopConfig.json` | 各角色 LoopConfig 默认值 | L2（运行时可重载） |
| `durable.json` | `Configs/durable.json` | 持久执行与恢复参数 | L2（运行时可重载） |
| `session.json` | `Configs/session.json` | 会话与归档参数 | L2（运行时可重载） |
| `modelRouter.json` | `Configs/modelRouter.json` | 模型路由配置 | L2（运行时可重载） |
| `routingRules.json` | `Configs/routingRules.json` | 路由判定规则 | L2（运行时可重载） |
| `economyRules.json` | `Configs/economyRules.json` | Token 经济规则 | L2（运行时可重载） |
| `supervision.json` | `Configs/supervision.json` | 监管参数 | L2（运行时可重载） |
| `memory.json` | `Configs/memory.json` | 共享记忆与事件总线参数 | L2（运行时可重载） |
| `arbitration.json` | `Configs/arbitration.json` | 仲裁系统参数 | L2（运行时可重载） |
| `audit.json` | `Configs/audit.json` | 审计系统参数 | L2（运行时可重载） |
| `security.json` | `Configs/security.json` | 安全策略配置 | L0（启动锁定） |
| `coverageBaseline.json` | `Configs/coverageBaseline.json` | 覆盖率基线 | L2（CI 读写） |
| `benchBaseline.json` | `Configs/benchBaseline.json` | 基准评估基线 | L2（CI 读写） |
| `dataStorage.json` | `Configs/dataStorage.json` | 七类数据的保留期 / 体积配额 / 清理优先级 | L2（运行时可重载）**⚠️ 2026-09-22 校准：`configLoader` 不加载本文件，`Src/` 内 0 引用，全部键未接线（见 §3.7）** |

> **`configLoader.ts` 实际加载清单（2026-09-22 校准）**：`default.json`（必需）+ `{env}.json` + `local.json` +
> `extraConfigs` 白名单 11 个文件 = `modelRouter` / `routingRules` / `economyRules` / `arbitration` / `audit` / `supervision` / `loopConfig` / `durable` / `session` / `memory` / `security`（`configLoader.ts:87-92`）。
> **`dataStorage.json` / `coverageBaseline.json` / `benchBaseline.json` 不在白名单内 → 不进入 `merged`**；
> `coverageBaseline.json` / `benchBaseline.json` 在 `Src/` 与 `Scripts/` 中亦无任何引用，"CI 读写"为**目标态（⚠️ 未实现）**。
>
> **`security.json` 的消费现状**（逐键核实，2026-09-22）：`trustLevels` / `forbiddenPaths` / `forbiddenCommands` ✅ 运行期消费
> （`main.ts:76-87` → `initTrustLevels` / `initPathGuard` / `initWhitelist`；执行侧消费者分别为 `toolFactory.ts:51`、
> 文件类工具统一走的 `checkPath`、`shellRunner.ts:49` 的 `isCommandForbidden`）；
> `networkWhitelist` ⚠️ 半接线（值注入 `initWhitelist`，但判定函数 `isNetworkAllowed` 在 `Src/` 内 0 调用者，`web.search` 不做域名白名单校验）；
> `sandboxDefaultMode` / `criticalSecurityEvents` ⚠️ 未接线（`Src/` 内 0 字面量引用，见 §3.3 与 §6.5）。
> 本文件不设 §2 参数表，键定义以本节为准。

> **扩展名统一 `.json`**（v2.1 裁决）：原 `Docs/02` 目录树中的 `default.toml` / `session.toml` / `durable.toml` 一律改称 `.json`，禁止混用两种格式。

### 1.2 配置加载优先级

```
local.json  >  {env}.json  >  default.json        （配置文件三层，优先级固定）
环境变量     →  仅用于注入密钥引用，不参与配置层级竞争
```

**规则：**
- `default.json` 提供所有配置项的默认值，**空配置下系统必须可直接启动**（`default.json` 本身缺失 → `configLoader.ts:55-58` 直接 FATAL）；
- `{env}.json` 为环境层（`dev.json` / `prod.json`），`{env}` 由 `CIVITAS_ENV` 决定；
- `local.json` 优先级最高，覆盖本地开发环境（不提交到 git）；
- **环境变量不再凌驾于 `local.json` 之上**（v2.1 修正：原写为 `环境变量 > local > default`，与全局约定冲突）；
  环境变量的唯一作用是解析 `api_key_ref: "env:OPENAI_API_KEY"` 这类引用；
- 运行时可重载的配置（L2）通过 `Infra/Watcher/configWatcher.ts` 文件监听触发。
  **⚠️ 未接线（2026-09-22 校准）**：见 §1.3，该监听当前不存在实际行为，L2 配置只能靠**重启进程**生效。

> **⚠️ 三层优先级的落地缺陷（2026-09-22 校准 · 现状 + 代码债，不抹平）**：
> 上面那条 `local.json > {env}.json > default.json` 只对 **`default.json` 内的键** 成立。
> 实际顺序是：`configLoader.ts:81` 完成三层深度合并得到 `merged` 之后，`:87-113` 再把 §1.1 白名单里的 **11 个功能 JSON**
> 逐个 `deepMerge(merged[key], fileValue)` / 直接赋值覆盖到同一个 `merged` 上——**功能 JSON 永远最后写入，且不看来源层**。
> 后果：凡是被功能 JSON 定义的键（`routing.*`、`economy.*`、`loopDefaults.*`、`retention.*` 等），
> `local.json` / `{env}.json` 里的**同名覆盖会被无条件吃掉**，"local 优先级最高"对这些键不成立；
> 只有**仅存在于 `default.json`** 的键（`system.*` / `server.*` / `database.*` / `ui.*`）才真正遵守三层优先级。
> - **现状**：本地调参只能直接改 `Configs/*.json`（会污染 git 工作区）或改 `default.json`。
> - **待办（代码债）**：功能 JSON 应先合并 `default.json → {env}.json → local.json` 各自的同名文件，
>   或在 `:94-113` 的循环里对 `envConfig` / `localConfig` 中已存在的同名键保留覆盖值，之后再写入 `merged`。

### 1.2.1 命名与单位规范（强制 · v2.1 新增）

| 对象 | 风格 | 例 | 说明 |
|------|------|----|------|
| 配置文件键 | **camelCase** | `httpPort`、`qualityThreshold` | 例外见下方《命名豁免清单》 |
| DB 表名与列名 | **snake_case** | `timeout_ms`、`budget_used_tokens` | 与 TS 字段的对应关系必须是**机械转换**，禁止人为改名 |
| TS 接口字段 | **camelCase** | `budgetUsedTokens` ↔ 列 `budget_used_tokens` | repository 层统一做 snake↔camel 映射；`LoopConfig` 除外（见豁免清单） |
| 时长后缀 | 必带单位 | 毫秒 `_ms` / 秒 `_sec` / 分 `_min` / 时 `_hour` / 天 `_days` / 轮 `_rounds` | `busyTimeout: 5000` → **`busyTimeoutMs: 5000`**；禁止无单位后缀的时间配置 |
| 比例与阀值 | 内部一律 **0.0–1.0 浮点** | `qualityScore: 0.85` | 展示层才 ×100；禁止 0-100 与 0-1 混用；DB 对应列为 `REAL` |
| 金额 | `_usd` 后缀 + number | `hardUsd`、`budgetUsedUsd` | 与 token 计量严格分开 |
| 时间戳 | `number`（epoch **毫秒**）+ `_at` 后缀；DB 列一律 `INTEGER` | `recordedAt`、`created_at` | 禁止 ISO-8601 字符串与 epoch 整数在同一库内混用 |
| 枚举 | **三处同形**（TS / DB / 配置文件不做大小写转换） | `'awaiting_approval'` | 分两类：① 状态/阶段类用 lower snake_case；② 安全类沿用 skill 的 SCREAMING_SNAKE（见豁免清单）；禁止 UI 引入第二套拼写 |

**命名豁免清单（穷举，新增需先登记）**：

| # | 豁免对象 | 风格 | 理由 | 边界 |
|---|---------|------|------|------|
| X1 | `modelRouter.json` 的 `providers[]` / `models[]` 内层键 | snake_case（`base_url`/`api_key_ref`/`context_window`/`cost_per_1k_input`） | 上游 OpenAI/Anthropic 兼容契约要求 | 仅内层；`routing` 段仍 camelCase；由 `Infra/Llm` 边界层转成 camelCase 领域对象 |
| X2 | `loopConfig.json` 的 `loopDefaults` / `roleOverrides` 内层键 | snake_case（`model`/`max_iterations`/`timeout_ms`/`temperature`/`token_budget`/`stream`） | **skill `source-code.md`「LoopConfig 字段清单（强制）」明文规定**，且 benchmark 数据集与 prompt frontmatter 复用同名字段 | TS 侧 `LoopConfig` 接口**保持 snake_case，不改名**；repository 写入 `subtasks`/`agents` 时同名直存，无需转换。`stopRules.*`、`verifier.*` 等**本项目新增**段不受此约束，仍用 camelCase |
| X3 | `ToolSpec.dangerLevel`、`RiskLevel`、审批 `status` | SCREAMING_SNAKE_CASE（`SAFE`/`CONTROLLED`/`DANGEROUS`/`FORBIDDEN`；`LOW`/`MEDIUM`/`HIGH`/`CRITICAL`；`PENDING`/`APPROVED`/`REJECTED`/`TIMEOUT`） | skill `security.md` 定义的威胁分级枚举，已在全局契约中广泛引用 | 不得新增第四个大写枚举族；新增安全类枚举必须先登记进本表 |

### 1.3 可变性分级

| 级别 | 名称 | 说明 | 适用配置 |
|------|------|------|---------|
| L0 | 启动锁定 | 启动后不可更改，修改需重启 | 安全策略、数据库路径 |
| L1 | 启动时读取 | 启动时读取一次，运行期间不变 | 本地覆盖配置 |
| L2 | 运行时可重载 | 通过文件监听或 API 热更新 | 路由规则、税率、模型路由 |
| L3 | 实时可变 | 每次使用时重新读取 | 无（当前不使用此级别） |

> **⚠️ 未实现 / 未接线（2026-09-22 校准）**：上表 L0/L1/L2/L3 是**目标态分级**，当前**没有任何运行期含义**——
> 全部配置都在 `loadConfig()` 一次性读入 `merged`，此后运行期不再回读文件，因此 L0/L1/L2/L3 的实际行为**完全相同（改任何键都要重启进程）**。
> 逐条现状：
> - `configWatcher.detectChanges()` 是**注释桩**（`Src/Infra/Watcher/configWatcher.ts:66-69`，函数体只有两行注释，不扫描文件系统）；
> - `startWatching()` / `stopWatching()` / `onConfigChange()` 在 `Src/` 生产代码里 **0 调用者**（`Watcher/index.ts:9` 的再导出不算调用）；
>   `main.ts:135` 只调 `initConfigWatcher({ watchDir: 'Configs/', pollIntervalMs: 5000 })` **存下参数而不启动轮询**；
> - `chokidar` 已在 `package.json:95` 引入依赖，但 `Src/` 内 **0 处 import**（引入未使用，见 §5）；
> - 分级判定函数 `getMutabilityLevel` / `isHotReloadable` / `isStartupLocked` / `getStartupLockedSections`
>   （`Src/Infra/Config/mutationLevels.ts:49-72`）**0 生产调用者**，仅 `Tests/Infra/config.spec.ts` 断言其返回值；
>   `MUTABILITY_REGISTRY` 也只登记到"段名 → 级别"，无任何组件据其拒绝写入。
> - **待办**：接通 `startWatching()` + 让 `notifyChange()` 回调到各组件的重载入参；
>   并在 L0 段的写入路径（若有 API 热更新）用 `isStartupLocked()` 做拒绝，否则本表继续只作设计意图记录。

---

## 2. 配置项汇总

### 2.1 系统基础配置（default.json）

```json
{
  "system": {
    "name": "Civitas-AI",
    "version": "0.1.0",
    "logLevel": "info",
    "dataDir": "Data/",
    "logDir": "Logs/",
    "promptDir": "Prompts/"
  },
  "server": {
    "host": "0.0.0.0",
    "httpPort": 3000,
    "wsPort": 3001,
    "corsOrigins": ["http://localhost:5173"]
  },
  "database": {
    "mainPath": "Data/db/civitas_main.db",
    "eventsPath": "Data/db/civitas_events.db",
    "memoryPath": "Data/db/civitas_memory.db",
    "walMode": true,
    "busyTimeoutMs": 5000
  },
  "ui": {
    "dashboardPollIntervalSec": 5,
    "approvalQueueRefreshSec": 3,
    "streamFlushIntervalMs": 100,
    "maxEventBufferSize": 500,
    "wsReconnectBackoffMs": 1000,
    "wsReconnectMaxAttempts": 5
  }
}
```

> **本节只列 `default.json` 的四个段**：`system` / `server` / `database` / `ui`（v2.1 新增 `ui`：原 `Docs/09 §3.2` 自带"每 5 秒轮询"等无家可归的数值，现全部登记于此）。

**生效状态（2026-09-22 校准 · 本文件唯一读者为 `Src/main.ts`）：**

| 键 | 生效状态 | 消费位置 / 未接线原因 |
|----|:---:|------|
| `system.name` / `system.version` / `system.logLevel` / `system.logDir` | ✅ | `main.ts:52-55` |
| `system.dataDir` | ⚠️ | `main.ts:112` 硬编码 `resolve('Data')`，未读该键 |
| `system.promptDir` | ⚠️ | `Src/` 内除 `configValidator` 的 schema 存在性检查外 0 消费者 |
| `server.host` / `server.httpPort` / `server.wsPort` / `server.corsOrigins` | ✅ | `main.ts:253-268` |
| `database.mainPath` / `eventsPath` / `memoryPath` / `walMode` / `busyTimeoutMs` | ✅ | `main.ts:94-101` |
| `ui.maxEventBufferSize` | ✅ | `main.ts:266`（`startWsGateway` 的 `maxBufferSize`） |
| `ui.dashboardPollIntervalSec` / `approvalQueueRefreshSec` / `streamFlushIntervalMs` / `wsReconnectBackoffMs` / `wsReconnectMaxAttempts` | ⚠️ | 服务端 0 消费者；Client 侧为**硬编码字面量**（`Client/src/hooks/useDashboardData.ts:10`、`Client/src/services/ws.ts:21-24`、`Client/src/stores/chatStore.ts:157`），`Client/src/config/configSchema.ts:237-242` 仅登记编辑器元数据，不回读配置 |

### 2.2 模型路由配置（modelRouter.json）

> **本节格式为 v2.1 强制契约**，取代 v1 示例（旧字段 `name`/`baseUrl`/`apiKey`/`maxTokens`/`promptCostPer1K`/`completionCostPer1K`/`capabilities` 已全部废弃，迁移关系见表后）。
> `providers[]` / `models[]` 内层字段为 **snake_case**（上游契约要求，是 §1.2.1 命名规则的唯一例外）；`routing` 段仍为 camelCase。

```json
{
  "providers": [
    {
      "provider": "huawei-maas",
      "base_url": "https://api.modelarts-maas.com/plan/v2",
      "api_key_ref": "env:HUAWEI_MAAS_API_KEY",
      "display_name": "华为云 MaaS",
      "models": [
        {
          "id": "GLM-5.1",
          "context_window": 128000,
          "max_output": 8192,
          "supports_vision": false,
          "supports_tools": true,
          "cost_per_1k_input": 2.0,
          "cost_per_1k_output": 8.0
        },
        {
          "id": "DeepSeek-V4-Flash",
          "context_window": 128000,
          "max_output": 8192,
          "supports_vision": false,
          "supports_tools": true,
          "cost_per_1k_input": 0.5,
          "cost_per_1k_output": 2.0
        },
        {
          "id": "Kimi-K2.6",
          "context_window": 128000,
          "max_output": 8192,
          "supports_vision": false,
          "supports_tools": true,
          "cost_per_1k_input": 1.0,
          "cost_per_1k_output": 4.0
        }
      ]
    }
  ],
  "routing": {
    "defaultModel": "huawei-maas/DeepSeek-V4-Flash",
    "directorModel": "huawei-maas/GLM-5.1",
    "workerModel": "huawei-maas/DeepSeek-V4-Flash",
    "verifierModel": "huawei-maas/GLM-5.1",
    "arbitrationModels": [
      "huawei-maas/GLM-5.1",
      "huawei-maas/DeepSeek-V4-Flash",
      "huawei-maas/Kimi-K2.6"
    ],
    "fallbackOrder": ["huawei-maas"],
    "timeoutMs": 60000,
    "firstByteTimeoutMs": 10000,
    "interChunkTimeoutMs": 15000
  },
  "reasoningSandwich": {
    "enabled": true,
    "planningTier": "strong",
    "actingTier": "economic",
    "verifyingTier": "strong",
    "fallbackOnBudgetSoft": {
      "planningTier": "economic",
      "actingTier": "economic",
      "verifyingTier": "strong"
    }
  }
}
```

> **⚠️ 与 v2.1 裁定的冲突（2026-09-22 校准 · 现状 + 待办，裁定不抹平）**：
> 现文件只有 **1 个 provider（`huawei-maas`）**、`fallbackOrder` 也只有 1 项，
> 因此 ① "`arbitrationModels` ≥ 3 且**不同厂商**"仅满足条数不满足互异（三个模型同源，单点故障会同时击穿三票）；
> ② §4.2 "LLM 调用失败 → 按 fallback 列表尝试下一个 Provider" 在运行期**无下一个 Provider 可退**（`getFallbackProviders` 返回空）。
> **待办**：接入第 2、3 家厂商后回填；在此之前若仍用单厂商，需在 `Docs/05` 显式登记"多数决同源降级"的例外与风险。

**生效状态（2026-09-22 校准）：**

| 键 | 生效状态 | 消费位置 / 未接线原因 |
|----|:---:|------|
| `providers[]`（含 `base_url` / `api_key_ref` / `display_name` / `models[]` 全部内层键） | ✅ | `main.ts:144-162` 逐个 `new OpenAIProvider(...)` + `registerProvider`；`models[]` 的 `context_window`/`cost_per_1k_*` 经 `resolveModel` 进入 `modelCaller` |
| `routing.defaultModel` / `directorModel` / `workerModel` / `verifierModel` / `arbitrationModels` / `fallbackOrder` / `timeoutMs` | ✅ | `main.ts:163-173` → `setRoutingConfig`；`timeoutMs` 用于 `modelCaller.ts:52`，`fallbackOrder` 用于 `modelRouter.ts:107` |
| `routing.firstByteTimeoutMs` / `interChunkTimeoutMs` | ⚠️ | 值被读进 `routingConfig`，但**无执行侧消费者**：唯一的流式解析器 `parseSseStream`（`Src/Core/Model/streamParser.ts:52`）在 `Src/` 内 0 调用者，只有测试在用 |
| `reasoningSandwich.*`（`enabled` / `planningTier` / `actingTier` / `verifyingTier` / `fallbackOnBudgetSoft.*`，共 7 个叶子键） | ⚠️ | **全段未接线**：`Src/` 内 `reasoningSandwich` 0 字面量引用，阶段化档位选择未实现 |

**v1 → v2.1 字段迁移表（旧字段一律拒注）：**

| v1 字段（废弃） | v2.1 字段 | 说明 |
|----------------|-----------|------|
| `providers[].name` | `providers[].provider` | 厂商短名 |
| `providers[].baseUrl` | `providers[].base_url` | 风格转 snake |
| `providers[].apiKey`（明文/env 占位） | `providers[].api_key_ref`（`env:` 前缀） | **禁止明文与模板插值**，只存引用 |
| `models[].maxTokens` | `models[].context_window` | 语义修正：原为上下文窗口，非输出上限 |
| （无） | `models[].max_output` | 新增：单次输出上限 |
| `models[].promptCostPer1K` | `models[].cost_per_1k_input` | 单位不变 |
| `models[].completionCostPer1K` | `models[].cost_per_1k_output` | 单位不变 |
| `models[].capabilities[]` | `supports_vision` / `supports_tools` | 拆为布尔位 |
| `routing.arbitratorModel`（单值） | `routing.arbitrationModels`（数组，≥ 3 且不同厂商） | 多数决需要 ≥3 互异模型 |
| `routing.qualityInspectionModel` | `routing.verifierModel` | 与 `producerModel` 不得相同 |
| （无） | `routing.workerModel` | 补齐；角色模型全集：`defaultModel`/`directorModel`/`workerModel`/`verifierModel`/`arbitrationModels` |
| `routing.maxRetries: 3` | **删除** | 重试次数由 §10.2 错误分类矩阵逐类给定（最多 2），不存在全局重试数 |
| （无） | `routing.firstByteTimeoutMs` / `interChunkTimeoutMs` | 补齐流式超时 |

### 2.3 路由规则配置（routingRules.json）

```json
{
  "routing": {
    "consortiumTokenThreshold": 50000,
    "consortiumDomainThreshold": 2,
    "delegationMaxCouplingScore": 0.3,
    "delegationMinSubtaskCount": 2,
    "assemblyLineSopRequired": true,
    "directExecMaxTokens": 10000
  }
}
```

> 本节只放**路由判定阈值**；模型选择相关的 `routing.defaultModel` 等在 `modelRouter.json`（§2.2），两文件同名命名空间 `routing` 不重叠，加载时合并为单一 `routingConfig`。

**生效状态（2026-09-22 校准）：**

| 键 | 生效状态 | 未接线原因 |
|----|:---:|------|
| `routing.consortiumTokenThreshold` / `consortiumDomainThreshold` / `delegationMaxCouplingScore` / `delegationMinSubtaskCount` / `assemblyLineSopRequired` / `directExecMaxTokens` | ⚠️ **6/6 全部未接线** | `main.ts:163-173` 从合并后的 `routing` 中**只取模型选择类 9 键**，判定阈值被读入 `merged` 后即丢弃；运行期实际取 `Src/Core/Decision/routeDecision/routingRules.ts:12-19` 的 `DEFAULT_RULES` **字面量**（与文件同值，故"看起来生效"），唯一写入口 `updateRoutingRules()` 在 `Src/` 内 0 调用者。**改本文件不改变任何路由判定行为** |
| （同段）`routing.timeoutMs` / `firstByteTimeoutMs` / `interChunkTimeoutMs` | 见 §2.2 | 这三个键由 `modelRouter.json` 提供，本文件不重复定义 |

> **待办**：启动装配阶段调用一次 `updateRoutingRules(config.merged.routing)`，
> 或让 `routeDecision` 直接接收配置入参；否则应从 `Configs/routingRules.json` 删除该文件以免与 `DEFAULT_RULES` 双源漂移（同 §2.8 `roleOverrides` 的问题）。

### 2.4 经济规则配置（economyRules.json）

```json
{
  "economy": {
    "initialSupply": 1000000,
    "defaultWalletBalance": 10000,
    "tax": {
      "baseRate": 0.05,
      "dynamicEnabled": true,
      "adjustmentIntervalSec": 300,
      "maxRate": 0.20,
      "minRate": 0.02,
      "highLoadMultiplier": 1.5,
      "lowLoadDiscount": 0.8,
      "loadThresholdAgents": 10
    },
    "profitSharing": {
      "qualityWeight": 0.5,
      "quantityWeight": 0.3,
      "efficiencyWeight": 0.2
    },
    "rollingWindow": {
      "tokens": 10000,
      "windowSec": 300,
      "checkIntervalSec": 5,
      "deviationMultiplierWarn": 3,
      "deviationMultiplierCritical": 5,
      "budgetWarnRatio": 0.8
    },
    "budgetLimits": {
      "singleCallMaxTokens": 5000,
      "globalTraceHardTokens": 200000,
      "globalTraceSoftRatio": 0.6
    }
  }
}
```

> **单一来源裁决**：滚动窗口原在 `economyRules.budget.rollingWindow*` 与 `audit.rollingWindow*` **重复定义**（同值两份），现统一归 `economy.rollingWindow`，`audit.json` 不再保留同名键（见 §2.6）。
> `budget.*`（单/任务/全局预算）与双预算阀值属于 **Loop 控制层**，已迁至 `loopConfig.json`（§2.8），避免与 `StopRuleSet.budget` 形成两套事实；**例外**：`budgetLimits.*` 为纯经济侧消耗上限（单次调用 / 整 trace），与 Loop 预算正交，留在本节。

**生效状态（2026-09-22 校准 · 10 ✅ / 12 ⚠️）：**

| 键 | 生效状态 | 消费位置 / 未接线原因 |
|----|:---:|------|
| `economy.initialSupply` / `economy.defaultWalletBalance` | ✅ | `main.ts:224-227` → `initWalletManager` |
| `economy.tax.*`（`baseRate` / `dynamicEnabled` / `adjustmentIntervalSec` / `maxRate` / `minRate` / `highLoadMultiplier` / `lowLoadDiscount` / `loadThresholdAgents`，共 8 键） | ✅ | `main.ts:228-237` → `initTaxCollector`，逐键带同值兜底 |
| `economy.profitSharing.qualityWeight` / `quantityWeight` / `efficiencyWeight` | ⚠️ | `main.ts` 未调用任何 distributor 配置入口；运行期取 `Src/Services/TokenEconomy/profitDistributor.ts:32` 的 **`DEFAULT_WEIGHTS` 字面量**（同值 0.5/0.3/0.2） |
| `economy.rollingWindow.*`（`tokens` / `windowSec` / `checkIntervalSec` / `deviationMultiplierWarn` / `deviationMultiplierCritical` / `budgetWarnRatio`，共 6 键） | ⚠️ | 仅出现在 `Src/Services/TokenEconomy/types.ts:128-135` 的接口声明中，无配置读取方；审计侧滚动窗口实际用 `Src/Services/Audit/anomalyDetector.ts:43` 的 `rollingWindowMs: 5 * 60 * 1000` 字面量 |
| `economy.budgetLimits.singleCallMaxTokens` / `globalTraceHardTokens` / `globalTraceSoftRatio` | ⚠️ | 同上，只在 `types.ts:121-122` 声明，运行期无读取；全局 trace 上限的真实生效值来自 §2.8 `stopRules`/`dualBudget`（且该处亦为字面量，见 §2.8 校准） |

### 2.5 仲裁配置（arbitration.json）

```json
{
  "arbitration": {
    "coreArbitrators": 1,
    "caseArbitratorCount": 3,
    "minArbitrators": 1,
    "maxArbitrators": 15,
    "frequencyThreshold": 5,
    "frequencyWindowSec": 300,
    "scaleUpFactor": 2.0,
    "scaleDownFactor": 0.5,
    "monitoringIntervalSec": 60,
    "consecutiveHighCount": 3,
    "consecutiveLowCount": 3,
    "deadlockTimeoutSec": 120,
    "cpuPerArbitratorCores": 0.1,
    "memoryPerArbitratorMb": 256,
    "snapshotStore": "sqlite"
  }
}
```

> **计数口径裁决**（原 05 号文档既写"固定并行 3 个 LLM"又写"动态池 1~15"）：
> - `coreArbitrators` = 核心层固定常驻数（不随负载缩）；
> - `caseArbitratorCount` = **单次案件实际并发仲裁 LLM 数**（多数决需 ≥3）；
> - `minArbitrators`/`maxArbitrators` = 辅助层动态池的**容量上下限**；
> - 实际并发 = `min(caseArbitratorCount, 池当前规模)`，池规模由扩缩容算法维护。
>
> **命名修正**：`similarityThreshold` → `conflictSimilarityThreshold`（与审计侧 `loopSimilarityThreshold` 区分，两者同值 0.85 但语义不同）；`defaultArbitratorCount` → `caseArbitratorCount`；`capsuleBudget` → `capsuleBudgetTokens`。
> **去重（v2.1）**：`conflictSimilarityThreshold` / `capsuleBudgetTokens` / `capsuleRecentOpsCount` 三键的**定义已迁至 `memory.json`（§2.11）**，`arbitration.json` 不再保留同名键，仲裁系统在加载时从 `memory.*` 读取；上方"命名修正"仅作为历史改名记录保留。
> `snapshotStore`：Phase 0–2 **强制 `sqlite`**，禁依赖 Redis（原 05 写"Redis/SQLite"两可，无边界）。

**生效状态（2026-09-22 校准 · 0 ✅ / 15 ⚠️，整文件未接线）：**

| 键 | 生效状态 | 未接线原因 / 实际生效值 |
|----|:---:|------|
| `arbitration.minArbitrators` / `maxArbitrators` / `frequencyThreshold` / `consecutiveHighCount` / `consecutiveLowCount` | ⚠️ | `main.ts` 从不读 `arbitration` 段；运行期取 `Src/Services/Arbitration/dynamicScaling.ts:30-40` 的 `DEFAULT_CONFIG` 字面量，且**已与文件漂移**：`minArbitrators` 文件 `1` vs 代码 `0`；`frequencyThreshold`/`consecutiveHighCount`/`consecutiveLowCount` 暂同值（5/3/3） |
| `arbitration.scaleUpFactor` / `scaleDownFactor` / `monitoringIntervalSec` | ⚠️ | 代码侧对应物为 `scalingFactor: 1.5`（**键名与数值双漂移**，文件写 2.0）、`scalingExponent: 0.5`、`monitoringIntervalMs: 60_000`；`scaleDownFactor` 无对应实现 |
| `arbitration.coreArbitrators` / `caseArbitratorCount` / `deadlockTimeoutSec` / `cpuPerArbitratorCores` / `memoryPerArbitratorMb` / `snapshotStore` / `frequencyWindowSec` | ⚠️ | `Src/` 内 **0 字面量引用**（含本节"计数口径裁决"与"强制 sqlite"两项裁定，均无落地代码） |
| （整段）仲裁池动态扩缩容 | ⚠️ | 唯一入口 `initDynamicScaling()` / `startAutoScaling()`（`dynamicScaling.ts:53` / `:150`）在 `Src/` 内 **0 调用者**，即使接线配置文件也不会运行 |

> **待办**：`main.ts` 装配段补 `initDynamicScaling(config.merged['arbitration'])` 并统一键名（`scaleUpFactor` ↔ `scalingFactor`），
> 否则本节的计数口径与 Redis 禁令只是**未落地的裁定**。

### 2.6 审计配置（audit.json）

```json
{
  "audit": {
    "loopSimilarityThreshold": 0.85,
    "loopConsecutiveRounds": 5,
    "patrolIntervalSec": 86400,
    "patrolTopPercentile": 0.1,
    "patrolFailureRateThreshold": 0.3,
    "patrolWindowHours": 24,
    "freezeAutoUnfreezeSec": 3600,
    "auditRecentOpsCount": 50,
    "auditRecentTxCount": 20
  }
}
```

> 删除原 `rollingWindowTokens` / `rollingWindowSeconds` / `deviationMultiplier` / `checkIntervalSeconds`（与 §2.4 `economy.rollingWindow` 重复）。
> `loopConsecutiveCount` → `loopConsecutiveRounds`，并裁决为 **5 轮**（原 03 号文档写"连续 3 次"与 06 写"5 次"冲突；3 次那个实际是 fingerprint 维度，已在 `loopConfig.json` 单列为 `fingerprint.consecutiveDuplicateRounds`）。

**生效状态（2026-09-22 校准 · 0 ✅ / 9 ⚠️，整文件未接线）：**

| 键 | 生效状态 | 未接线原因 / 实际生效值 |
|----|:---:|------|
| `audit.loopSimilarityThreshold` / `loopConsecutiveRounds` | ⚠️ | `main.ts` 不读 `audit` 段；运行期取 `Src/Services/Audit/anomalyDetector.ts:47-48` 的字面量（同值 0.85 / 5），唯一注入口 `initResourceAuditBureau()`（`resourceAuditBureau.ts:52`）在 `Src/` 内 **0 生产调用者**（只有 `Tests/` 调） |
| `audit.patrolIntervalSec` / `patrolTopPercentile` / `patrolFailureRateThreshold` / `patrolWindowHours` | ⚠️ | `Src/` 内 0 字面量引用；巡检参数取 `Src/Services/Audit/patrolScheduler.ts` 自带的 `PatrolConfig` 默认值，与本文件无绑定 |
| `audit.freezeAutoUnfreezeSec` | ⚠️ | setter `setAutoUnfreezeSec()`（`freezeManager.ts:36`）存在但 **0 调用者**，解冻时长用 `freezeManager` 内部默认 |
| `audit.auditRecentOpsCount` / `auditRecentTxCount` | ⚠️ | `Src/` 内 0 字面量引用（审计取证取数条数写死在代码内） |

### 2.7 监管配置（supervision.json）

```json
{
  "supervision": {
    "qualityThreshold": 0.60,
    "agentConsecutiveFailuresToExpel": 3,
    "patrolEnabled": true,
    "broadcastAckTimeoutSec": 300,
    "triggerDedupWindowSec": 300,
    "triggerCooldownSec": 30,
    "triggerIdempotencyTtlHour": 24,
    "maxConcurrentLoopsGlobal": 20,
    "maxConcurrentLoopsPerUser": 5,
    "stormBreakerThresholdPerMin": 100,
    "stormBreakerOpenSec": 60,
    "lawyerLetterDedupWindowSec": 300,
    "approvalTimeoutSec": 60,
    "approvalDefaultOnTimeout": "reject",
    "criticalApprovalRoles": 2
  }
}
```

> **标度修正**：`qualityThreshold` 原为 `60`（隐含百分制），按 §1.2.1 强制 0.0–1.0 改为 `0.60`。
> **改名**：`maxConsecutiveFailures` → `agentConsecutiveFailuresToExpel`（与 `StopRuleSet.limits.maxConsecutiveErrors` 区分：后者是**单 Loop 内连续工具/模型错误**退出阀，前者是**跨任务累计失败**开除阀）。

**生效状态（2026-09-22 校准 · 0 ✅ / 15 ⚠️，整文件未接线）：**

| 键 | 生效状态 | 未接线原因 / 实际生效值 |
|----|:---:|------|
| `supervision.approvalTimeoutSec` | ⚠️ | `main.ts` 不读 `supervision` 段；唯一代码侧同名字段是 `ToolSafetyGateConfig.approvalTimeoutSec`（`toolSafetyGate.ts:24/32`），但 `runIteration.ts:577-581` 创建该中间件时**不传 config**，恒取组件自带的 `60`；`approvalDefaultOnTimeout` 同理（代码写死 `'reject'`） |
| `supervision.stormBreakerThresholdPerMin` / `stormBreakerOpenSec` | ⚠️ | `Src/` 内 0 字面量引用；熔断器由 `main.ts:249` 的 `initCircuitBreaker({ threshold: 100, windowMs: 60_000, cooldownMs: 120_000 })` **硬编码**（阈值 100 与本键同值，冷却 120s 与文件 `stormBreakerOpenSec: 60` **已漂移**） |
| `supervision.triggerDedupWindowSec` / `triggerCooldownSec` / `triggerIdempotencyTtlHour` | ⚠️ | `Src/` 内 0 字面量引用；去重窗口由 `main.ts:248` 的 `initDedupStore({ windowMs: 60_000 })` 硬编码（与 `triggerDedupWindowSec: 300` **漂移**） |
| `supervision.qualityThreshold` / `agentConsecutiveFailuresToExpel` / `patrolEnabled` / `broadcastAckTimeoutSec` / `maxConcurrentLoopsGlobal` / `maxConcurrentLoopsPerUser` / `lawyerLetterDedupWindowSec` / `criticalApprovalRoles` | ⚠️ | `Src/` 内 0 字面量引用；其中"并发 Loop 上限"与"律师函去重"无对应实现，`criticalApprovalRoles: 2` 的等价约束写在 `approvalGate.ts:53-58`（CRITICAL 需 ≥2 不同角色）为**代码常量而非配置** |

### 2.8 循环控制配置（loopConfig.json）

> 本节收编原散落在 `Docs/02 §12`、`Docs/03 §7B`、`Docs/04 §5.2`、`Docs/12` 各章的循环参数，**成为唯一事实源**；`StopRuleSet` 的字段名以 `Docs/12 §2.1` 为准，本节为其默认值。
>
> **⚠️ "唯一事实源"目前是目标态（2026-09-22 校准）**：两个层面的失配同时存在，逐条见本节末《生效状态》表——
> 1. **整段未接线**：`main.ts:195` 用 `getConfigValueOr(config, 'loopConfig', {})` 取段，但 `configLoader.ts:101-111` 是把 `loopConfig.json` 的**顶层键**（`loopDefaults` / `stopRules` / `verifier` / …）**直挂** `merged`，`merged.loopConfig` 根本不存在 → 取回 `{}` → 该调用点下游全部落 `main.ts` 内的字面量兜底。
> 2. **双源漂移**：`Src/Core/Loop/loopConfig.ts:63-72` 硬编码了一份 `ROLE_OVERRIDES`，与本文件 `roleOverrides` **数值不一致**（如 `prime_director` 代码 `50 / 200_000 / 0.2` vs 文件 `100 / 500_000 / 0.3`）；
>    `Src/Core/Loop/loopConfig.ts:29-34` 的 `LOOP_LIMITS` 又与本文件 `hardLimits` 部分漂移（`maxIterationsCeiling` 200 同值；`tokenBudgetCeiling` 2_000_000 vs 代码 `maxTokenBudget: 1_000_000`；`timeoutMsCeiling` 600_000 vs 代码 `maxTimeoutMs: 300_000`）。
>    **运行期以哪份为准（核实结论）**：**两份都不为准**——`ROLE_OVERRIDES` 的唯一读者 `createRoleLoopConfig()` 在 `Src/` 内 0 生产调用者（`wsHandler.ts:245` 走的是不带角色覆盖的 `createLoopConfig({ model, stream: true })`），
>    而本文件的 `roleOverrides` 只在 `configValidator.ts:85-118` 被做**键集/键名合法性校验**，值从未被读取；实际生效的是 `DEFAULT_LOOP_CONFIG`（`loopConfig.ts:20-27`，值与本文件 `loopDefaults` 相同）。
>
> **大小写混合声明**（避开与 §1.2.1 的误读）：`loopDefaults` 与 `roleOverrides[*]` 内层键为 **snake_case**（豁免 X2，与 TS `LoopConfig` 逐字对齐）；
> `hardLimits` / `stopRules` / `verifier` / `fingerprint` / `goalReanchor` / `failureFeedback` / `context` / `efficiency` 为本项目新增段，一律 **camelCase**。

```json
{
  "loopDefaults": {
    "model": "workerModel",
    "max_iterations": 30,
    "timeout_ms": 60000,
    "temperature": 0.2,
    "token_budget": 100000,
    "stream": true
  },
  "hardLimits": {
    "maxIterationsCeiling": 200,
    "timeoutMsCeiling": 600000,
    "maxWallClockMsCeiling": 3600000,
    "tokenBudgetCeiling": 2000000
  },
  "roleOverrides": {
    "prime_director": { "max_iterations": 100, "token_budget": 500000, "temperature": 0.3 },
    "partner":         { "max_iterations": 50,  "token_budget": 200000, "temperature": 0.3 },
    "worker":          { "max_iterations": 30,  "token_budget": 100000, "temperature": 0.2 },
    "reviewer":        { "max_iterations": 5,   "token_budget": 50000,  "temperature": 0.0 },
    "assembly_node":   { "max_iterations": 10,  "token_budget": 60000,  "temperature": 0.1 },
    "arbitrator":      { "max_iterations": 5,   "token_budget": 50000,  "temperature": 0.0 },
    "auditor":         { "max_iterations": 20,  "token_budget": 80000,  "temperature": 0.0 },
    "regulator":       { "max_iterations": 15,  "token_budget": 80000,  "temperature": 0.1 }
  },
  "stopRules": {
    "limits": {
      "maxIterations": 30,
      "maxWallClockMs": 900000,
      "maxToolCalls": 100,
      "maxConsecutiveErrors": 3
    },
    "budget": {
      "softRatio": 0.6,
      "warmRatio": 0.36,
      "expandRequestRatio": 0.8,
      "hardRatio": 1.0
    },
    "noProgress": {
      "metric": "goal_distance",
      "stagnationWindowRounds": 3,
      "minDeltaRatio": 0.05,
      "action": "switch_strategy"
    },
    "evaluationOrder": ["risk", "limits", "budget", "noProgress", "success"]
  },
  "verifier": {
    "minLevelsRequired": 2,
    "l3MinAgreementWithHuman": 0.85,
    "l3CalibrationDataset": "Tests/GoldenSets/review-50",
    "l3RecalibrateIntervalDay": 90,
    "antiGamingMinRules": 1,
    "antiGamingMaxChangedFiles": 8
  },
  "fingerprint": {
    "singleIterationWarnThreshold": 2,
    "consecutiveDuplicateRounds": 3,
    "windowRounds": 5,
    "windowHitThreshold": 3,
    "crossAgentDuplicateThreshold": 2
  },
  "goalReanchor": {
    "reanchorEveryRounds": 5,
    "alwaysOnAfterCompression": true,
    "injectRecentFailedStrategiesCount": 5
  },
  "failureFeedback": {
    "evidenceExcerptMaxChars": 500,
    "strategyTextMaxChars": 30,
    "sameDefectCategoryEscalateRounds": 3,
    "noImprovementAbortRounds": 2,
    "improvementMinRatio": 0.05,
    "unreasonableGoalEscalateCount": 5
  },
  "context": {
    "partitionCeilingRatio": { "static": 0.15, "lowFreq": 0.15, "midFreq": 0.15, "highFreq": 0.45 },
    "outputReserveTokens": 4096,
    "tokenCoefficient": { "static": 0.05, "lowFreq": 0.5, "midFreq": 0.3, "highFreq": 1.0 },
    "truncationUsageRatio": 0.92,
    "summaryTriggerUsageRatio": 0.80,
    "scoreCompressThreshold": 0.30
  },
  "efficiency": {
    "invalidLoopRepeatRatio": 0.85,
    "invalidLoopGoalDelta": 0.02
  }
}
```

> **预算比例裁决**（原 03 写 50%/80%/100%、04 写 `taskSoft=0.6`，两处不兼容）：统一以 `budget.*Ratio` 为**相对 `token_budget`（= 硬预算）** 的比例 ——
> `warmRatio 0.36`（预热）→ `softRatio 0.6`（软预算，触发降级）→ `expandRequestRatio 0.8`（请求扩预算审批）→ `hardRatio 1.0`（硬预算，中止 + 回滚）。
> 因此 `StopRuleSet.budget.softTokens` 默认值 = `token_budget × softRatio`，`hardTokens` = `token_budget × hardRatio`。

> **超时双键裁决（v2.1）**：原 `Docs/02 §12` 把 `LoopConfig.timeout_ms` 用作"单次循环总超时 900_000"，与 skill `source-code.md` 的定义（**单次模型调用超时**，默认 60_000）矛盾，且与 `stopRules.limits.maxWallClockMs` 形成双事实。
> 现裁定：
> - `loopDefaults.timeout_ms` = **60000**，语义为 **单次模型调用超时**（skill 原义）；
> - 整个 Loop 的墙钟上限**唯一**由 `stopRules.limits.maxWallClockMs`（900000）提供；
> - `hardLimits.timeoutMsCeiling` 卡前者（单次调用），`hardLimits.maxWallClockMsCeiling` 卡后者（整轮），`tokenBudgetCeiling` 卡 `token_budget`；
> - `roleOverrides` 只允许覆盖 `max_iterations` / `token_budget` / `temperature` 三项，出现其他键 → 启动失败。

> **角色键范围**：`roleOverrides` 的键必须与 `agents.role` 取值（`Docs/10 §2.3`：`prime_director`/`partner`/`worker`/`reviewer`/`assembly_node`/`arbitrator`/`auditor`/`regulator`）**逐一对应**；`reviewer` 定义见 `Docs/03 §7.3`。`temperature` 为 0 的角色（`reviewer`/`arbitrator`/`auditor`）不得调高，否则违反 P2 Maker≠Checker 的可重现性假设。

> **上下文分区双系数裁决（v2.1）**：skill `source-code.md` 定义了两个**正交**的量，原 §2.8 将 `partitionRatio` 写为 `30/35/20/15`，与 `Docs/01 §2.4` 的 `S15/L15/M15/H45`（= skill 原值）直接矛盾：
> - `partitionCeilingRatio`（**预算上限百分比**，skill §Token 预算体系：S≤15% / L≤15% / M≤15% / H≤45%）——键名由 `partitionRatio` 改为 `partitionCeilingRatio` 以消除“它是折算因子”的误读；四项和 = 0.90，**剩余 10% 为未分配余量**，不得填满；
> - `tokenCoefficient`（**压缩成本折算因子**，skill §评分延迟压缩：S 0.05 / L 0.5 / M 0.3 / H 1.0）——仅用于压缩评分，**不是百分比**；
> - `outputReserveTokens` 4096：先从 `context_window` 扣除，**剩下的才是分区预算基数**（skill 默认 4K，可配）；
> - **截断红线**（skill 强制）：S 区永不截断；M 区的**当前轮次用户消息**永不截断；超预算时仅从 H 区按评分从高到低反向裁剪；
> - **按角色差异化分区上限尚未实现**：`Docs/01 §2.4` 提的“Director L 大 / Worker H 大”在 Phase 0–2 **不生效**（统一用 `partitionCeilingRatio`）；如后续需要，必须先在本节登记新键 `context.partitionCeilingByRole[role]` 并保留上述默认值作为其缺省。

**生效状态（2026-09-22 校准 · 10 段 / 约 60 个叶子键，运行期 0 消费）**

根因：`main.ts:195` 的读路径 `'loopConfig'` 与文件顶层键不匹配（详见本节首注），`loopConfigRaw` 恒为 `{}`，
于是 `stopRules.*` / `loopDefaults.*` 全部落到 `main.ts:201-219` 与 `:238-244` 的**字面量默认值**上（这些字面量与文件同值，所以行为"看起来正确"）。

| 段 | 生效状态 | 说明 |
|----|:---:|------|
| `loopDefaults.*`（6 键） | ⚠️ | 取不到值；生效者为 `DEFAULT_LOOP_CONFIG`（`loopConfig.ts:20-27`）与 `main.ts:202/239` 的 `100000` 字面量 |
| `stopRules.limits.*` / `stopRules.budget.*` / `stopRules.noProgress.*`（11 键） | ⚠️ | `main.ts:203-218` 逐键 `?? 字面量` 兜底，全部走兜底分支；`stopRules.evaluationOrder` 无消费者 |
| `hardLimits.*`（4 键） | ⚠️ | `Src/` 内 0 字面量引用；启动校验用的是 `LOOP_LIMITS`（`loopConfig.ts:29-34`），且与文件数值漂移（见本节首注） |
| `roleOverrides.*`（8 角色 × 3 键） | ⚠️ | 仅被 `configValidator.ts:85-118` 做键集校验，值无读者；另有一份硬编码 `ROLE_OVERRIDES` 双源 |
| `verifier.*`（6 键） | ⚠️ | **即使修好读路径仍无消费者**：`Src/Services/LoopControl/verifier/*` 用函数入参默认值（如 `verifier/index.ts:46` 的 `minLevelsRequired = 2`），无任何配置装配点 |
| `fingerprint.*`（5 键） | ⚠️ | 同上：`actionFingerprint.ts:55-57` 的阈值由调用方入参提供，`Src/main.ts` 未装配（`consecutiveDuplicateRounds` / `windowHitThreshold` 等仅作为形参名出现） |
| `goalReanchor.*`（3 键） | ⚠️ | 同上：`goalReanchorControl.ts:23-25` 自带 `DEFAULT` 字面量（同值 5 / true / 5），无配置装配 |
| `failureFeedback.*`（6 键） | ⚠️ | 同上：`Services/LoopControl/failureFeedback.ts` 用内部默认，`Src/` 内无配置读取 |
| `context.*`（10 键） | ⚠️ | 同上：`tokenCoefficient` 只作为 `Services/Context/scoring.ts` 的形参名出现；`partitionCeilingRatio` / `outputReserveTokens` / 三个 `*Ratio` 在 `Src/` 内 0 字面量引用 |
| `efficiency.*`（2 键） | ⚠️ | `Src/` 内 0 字面量引用，无效循环判定未实现该口径 |

> **修复待办（按优先级）**：
> 1. 把 `main.ts:195` 的读路径改为直取段名（`getConfigValueOr(config, 'stopRules', {})` / `'loopDefaults'`），或让 `configLoader` 按文件名加一层命名空间；**在此之前的任何"改 loopConfig.json 调参"都是无效操作**；
> 2. 装配 `verifier` / `fingerprint` / `goalReanchor` / `context` / `efficiency` 五段的消费者（当前五段**双缺**：既读不到，也没有读取方）；
> 3. 删除 `Src/Core/Loop/loopConfig.ts` 的 `ROLE_OVERRIDES`，或改为启动时用配置段覆盖，**消双源**；
> 4. 统一 `hardLimits` 与 `LOOP_LIMITS` 的键名和数值（`tokenBudgetCeiling` 2_000_000 vs `maxTokenBudget` 1_000_000、`timeoutMsCeiling` 600_000 vs `maxTimeoutMs` 300_000 必须先裁定谁对）。

### 2.9 持久执行配置（durable.json）

```json
{
  "durable": {
    "checkpoint": {
      "everyIteration": true,
      "afterEachEffect": true,
      "beforeCompression": true,
      "onExplicitPause": true,
      "fallbackIntervalSec": 60,
      "keepLastCount": 20,
      "milestoneKeep": true,
      "compactIntervalSec": 86400
    },
    "effect": {
      "defaultTimeoutMs": 30000,
      "journalRetentionDays": 180,
      "pendingAlertThreshold": 100,
      "unknownAlertThreshold": 1,
      "writeSynchronous": "FULL"
    },
    "idempotency": {
      "cacheTtlHour": 24,
      "store": "sqlite"
    },
    "recovery": {
      "autoResumeMaxUnknownEffects": 0,
      "autoResumeMaxBudgetUsedRatio": 0.8,
      "requireHumanOnArtifactDrift": true,
      "startScanTimeoutSec": 10
    },
    "shutdown": {
      "flushTimeoutSec": 30,
      "journalFsyncTimeoutSec": 5
    },
    "metrics": {
      "checkpointWriteDurationP99Ms": 500,
      "recoveryHumanRequiredAlertPerHour": 5,
      "idempotencyHitRatioMin": 0.3
    }
  }
}
```

> `recovery.autoResumeMaxUnknownEffects = 0` 对应 `Docs/13 §6.2` 的硬规则：**存在任何 UNKNOWN 副作用即不得自动恢复**。

**生效状态（2026-09-22 校准 · 5 ✅ / 20 ⚠️，唯一读者 `main.ts:117-127`）：**

| 键 | 生效状态 | 消费位置 / 未接线原因 |
|----|:---:|------|
| `durable.effect.defaultTimeoutMs` | ✅ | `main.ts:121` → `initEffectJournal`（`?? 30000`） |
| `durable.idempotency.cacheTtlHour` | ✅ | `main.ts:122` → `initIdempotencyStore`（`?? 24`） |
| `durable.recovery.autoResumeMaxUnknownEffects` / `autoResumeMaxBudgetUsedRatio` / `requireHumanOnArtifactDrift` | ✅ | `main.ts:123-127` → `initRecoveryScanner`；上方"UNKNOWN 即不得自动恢复"的硬规则**已落地** |
| `durable.checkpoint.*`（`everyIteration` / `afterEachEffect` / `beforeCompression` / `onExplicitPause` / `fallbackIntervalSec` / `keepLastCount` / `milestoneKeep` / `compactIntervalSec`，共 8 键） | ⚠️ | **全段未接线**：`Src/` 内 0 字面量引用；检查点写入由 `Services/LoopControl/middleware/checkpointWriter.ts` 按固定策略触发，不受配置控制。§2.10 里"`loop_checkpoints` 保留期由 `durable.checkpoint.keepLastCount` 提供"的裁定因此**无运行期依据** |
| `durable.effect.journalRetentionDays` / `pendingAlertThreshold` / `unknownAlertThreshold` / `writeSynchronous` | ⚠️ | `Src/` 内 0 字面量引用（§2.10 引用的 `journalRetentionDays` 同样未接线） |
| `durable.idempotency.store` | ⚠️ | 无消费者，存储后端由 `Infra/DurableExecution/idempotencyStore.ts` 固定 |
| `durable.recovery.startScanTimeoutSec` | ⚠️ | 无消费者；`main.ts:128` 的 `scanAndProposeRecovery()` 不传超时 |
| `durable.shutdown.*`（2 键） / `durable.metrics.*`（3 键） | ⚠️ | **两段整体未接线**：0 字面量引用；`main.ts` 的 9 步优雅关闭（`:288-336`）各步只有 `console.log` 占位，无 flush/fsync 超时实现 |

### 2.10 会话与数据保留配置（session.json）

```json
{
  "session": {
    "sessionKeyHexLength": 16,
    "archiveInactiveDays": 30,
    "historyMaxMessagesInMemory": 200
  },
  "retention": {
    "tasksActiveDays": 30,
    "agentsDestroyedDays": 90,
    "operationHistoryDays": 180,
    "eventsDays": 90,
    "loopsCompletedDays": 90,
    "pendingApprovalsDoneDays": 30,
    "verifierResultsDays": 90,
    "failureFeedbacksDays": 90,
    "actionFingerprintsDays": 90,
    "longTermMemoryPermanent": true,
    "tokenTransactionsPermanent": true
  }
}
```

> 原 `Docs/10 §8.4` 遗漏了 `failure_feedbacks` 与 `action_fingerprints` 两张表的保留策略，现补齐（默认 90 天，与 `loops` 对齐）。
>
> **不列入本表的三项（v2.1 去重）**：`effect_journal` 与 `idempotency_cache` 的保留期由 `durable.effect.journalRetentionDays`（180）与 `durable.idempotency.cacheTtlHour`（24）提供，
> `loop_checkpoints` 由 `durable.checkpoint.keepLastCount`（20）提供；**禁止**在 `retention.*` 下再造同名键。

**生效状态（2026-09-22 校准 · 0 ✅ / 14 ⚠️，整文件未接线）：**

| 键 | 生效状态 | 未接线原因 / 实际生效值 |
|----|:---:|------|
| `session.sessionKeyHexLength` | ⚠️ | `Src/` 内 0 字面量引用；会话 key 长度以**形参默认值**硬编码在 `Src/Infra/Workspace/sessionKeyGenerator.ts:46`（`options.hexLength ?? 16`）与 `:82`（`isValidSessionKey(key, hexLength = 16)`），改文件不改变长度 |
| `session.archiveInactiveDays` | ⚠️ | `Src/` 内 0 字面量引用；`Services/Session/archiveManager.ts` 内**无任何天数常量**，归档不由不活跃时长驱动 |
| `session.historyMaxMessagesInMemory` | ⚠️ | `Src/` 内 0 字面量引用；`Services/Session/sessionManager.ts` 无消息数上限裁剪逻辑 |
| `retention.*`（`tasksActiveDays` / `agentsDestroyedDays` / `operationHistoryDays` / `eventsDays` / `loopsCompletedDays` / `pendingApprovalsDoneDays` / `verifierResultsDays` / `failureFeedbacksDays` / `actionFingerprintsDays` / `longTermMemoryPermanent` / `tokenTransactionsPermanent`，共 11 键） | ⚠️ | **全段未接线**：`Src/` 内连 `retention` 这个词都 0 字面量引用，即**不存在任何数据清理/TTL 实现**；本表数值只是**待落地的裁定** |

> **交叉引用失效**：上方"去重三项"依赖 `durable.effect.journalRetentionDays` 与 `durable.checkpoint.keepLastCount`，
> 但这两键在 §2.9 已判为 ⚠️ 未接线 —— 即 `effect_journal` / `idempotency_cache` / `loop_checkpoints` 三张表**当前无任何保留期执行者**。
>
> **待办 + 唯一源建议**：与 `Configs/dataStorage.json`、`Docs/11 §3.7` 形成**三套并存**的保留期口径，
> 裁定建议见 §3.7（指定唯一源后，其余两处降级为引用或标注 ⚠️）。

### 2.11 共享记忆与事件总线配置（memory.json）

> 收编原 `Docs/07 §6` 自带默认值的配置表（违反 SSOT），并修正无单位后缀的键名。

```json
{
  "memory": {
    "conflictSimilarityThreshold": 0.85,
    "capsuleBudgetTokens": 4000,
    "capsuleMemoryTopK": 5,
    "capsuleRecentOpsCount": 5,
    "capsuleMessageBudgetTokens": 2000,
    "longTermConsolidationDelayMs": 5000,
    "privateStateTtlSec": 3600,
    "vectorDim": 1536,
    "supersedeGraceSec": 60
  },
  "eventBus": {
    "maxQueueSize": 10000,
    "consumerTimeoutMs": 30000,
    "retryMaxAttempts": 1,
    "deadLetterKeepDays": 7,
    "store": "in-memory"
  }
}
```

> **归一裁决（v2.1）**：
> - `memory.conflictSimilarityThreshold` 为**写入冲突检测**的唯一阈值，取代原 `Docs/07` 的 `memory.similarityThreshold` 与 `Docs/11 §2.5` 的 `arbitration.conflictSimilarityThreshold`（三者同值 0.85、同一语义）；审计侧 `audit.loopSimilarityThreshold` 语义不同（行为指纹相似度），**保持独立**。
> - `memory.capsuleBudgetTokens` / `memory.capsuleRecentOpsCount` 为胶囊预算与取数条数的**唯一定义**；`Docs/05` 需要时只引用，不再自带 `arbitration.capsule*` 同名键。
> - `eventBus.store` Phase 0–2 **强制 `in-memory`**，`redis` 仅作为 Phase 3 升级选项登记，不得提前依赖（与 `durable.idempotency.store`、`arbitration.snapshotStore` 的禁 Redis 口径一致）。

**生效状态（2026-09-22 校准 · 0 ✅ / 14 ⚠️，整文件未接线）：**

| 键 | 生效状态 | 未接线原因 / 实际生效值 |
|----|:---:|------|
| `memory.conflictSimilarityThreshold` | ⚠️ | `Src/` 内 0 字面量引用；写入冲突检测在 `Services/SharedMemory/writeGuard.ts` 只做**同名 key 禁写**（`FORBIDDEN_KEYS`），无语义相似度判定（`Docs/07 §3.2` 同判 ⚠️） |
| `memory.capsuleBudgetTokens` / `capsuleMemoryTopK` / `capsuleRecentOpsCount` / `capsuleMessageBudgetTokens` | ⚠️ | 四键 `Src/` 内 0 字面量引用；上下文组装取 `Services/Context/partitions/*` 的内部比例常量，无胶囊预算概念 |
| `memory.longTermConsolidationDelayMs` / `privateStateTtlSec` / `supersedeGraceSec` | ⚠️ | 0 字面量引用，无对应定时器/TTL 实现 |
| `memory.vectorDim` | ⚠️ | 0 字面量引用。`1536` 是 OpenAI 系嵌入模型假设值，现 `modelRouter.json` 只登记华为云 MaaS 且**无 embedding 模型**；`embeddingClient.ts:100` 的维度取自响应长度，`Services/` 内不存在向量维度常量 → **待办**：接入嵌入模型后回填真实维度 |
| `eventBus.maxQueueSize` | ⚠️ | `main.ts:247` 直接 `initEventBus({ maxQueueSize: 10000 })` **硬编码同值**，未从 `memory.json` 读取 |
| `eventBus.consumerTimeoutMs` / `retryMaxAttempts` / `deadLetterKeepDays` / `store` | ⚠️ | 0 字面量引用；死信队列与消费重试无实现，`store` 恒为进程内 `Map`（与本键声明的 `in-memory` 恰好一致，但不是配置驱动） |

> **示例中的 `//` 注释仅供文档说明**，实际 `Configs/*.json` 必须是无注释的合法 JSON。

---

## 3. 安全设计

### 3.1 威胁模型

| 威胁 | 风险等级 | 防御措施 |
|------|---------|---------|
| Prompt 注入 | 高 | `preSupervision` 关键字规则匹配（⚠️ 三层防御仅落第 [1] 层且最简版，见 §3.4） |
| Agent 越权访问 | 高 | 沙箱隔离 + 权限矩阵（✅ `requiredRoles` 执行期硬校验；⚠️ `sandboxMode` 无执行侧消费者，见 §3.3） |
| Token 异常消耗 | 中 | Audit Bureau 实时监测 + 自动冻结 |
| 仲裁操纵 | 高 | 3-LLM 独立裁决 + 最小信息原则 |
| 密钥泄露 | 高 | 环境变量引用（`env:` 前缀）+ 日志脱敏（⚠️ 加密存储/PBKDF2 未实现，见 §3.6） |
| 数据篡改 | 中 | 事件溯源 + 账本守恒验证 |

> **⚠️ 2026-09-22 校准**：本表"防御措施"列混写了**已实现**与**目标态**。逐项落地情况详见 §3.3（危险分级执行）、§3.4（注入防御）、§3.6（密钥）三节的现状标注。

### 3.2 信任分级

> **v2.2 修订**：原 L0 包含 Prime Director，但 Prime Director 与 Partner 本质是**入口级对等协作体**（均可接收用户输入、递归派发子 Agent），不应与治理三权（Regulator/Auditor/Arbitrator）共享最高权限。修订后 L0 仅保留治理三权，L1 为入口级，L2 为执行子级。外部输入的内容隔离（`<external trust="L2">`）与角色信任分级正交，不再占用 L2 行。

| 级别 | 名称 | 适用角色 | 权限范围 | 可接收用户输入 | 可递归派发子 Agent |
|------|------|---------|---------|:---:|:---:|
| L0 | 治理级 | Regulator, Auditor, Arbitrator | 全局读写、规则修改、Agent 管理、冲突裁决 | — | — |
| L1 | 入口级 | Prime Director, Partner | 接收用户输入、需求评估、工作方式选择、递归派发子 Agent、搭建工作流 | ✅ | ✅ |
| L2 | 执行子级 | Worker, Reviewer, Assembly Node | 仅执行上级派发任务、不可接收外部输入、不可自行招募下级 | ❌ | ❌ |

**角色分类说明：**

- **治理三权（L0）**：不参与业务执行，仅在冲突、异常、违规时介入。三权独立互相制衡。
  - Regulator：检测冲突行为、强制暂停 Agent
  - Auditor：审计日志回溯、异常操作告警
  - Arbitrator：裁决分歧（如两个 Partner 意见不一）

- **入口级（L1）**：Prime Director 与 Partner 完全对等，均可独立接收用户输入、递归拆分任务给其他 Partner 或委派 Worker。Prime Director 仅是全局视角的"主领导"标识，权限不高于 Partner。

- **执行子级（L2）**：仅执行上级派发任务。Worker 完成工作后必须走 `submitForReview`，不可自行宣告完成。

### 3.3 工具危险分级

| 级别 | 名称 | 示例工具 | 执行策略 |
|------|------|---------|--------|
| 安全 | Safe | 文件读取、网络搜索 | 直接执行 |
| 受控 | Controlled | 文件写入、代码生成 | 记录操作历史，可回溯 |
| 危险 | Dangerous | 代码执行、外部 API 调用 | 沙箱内执行 + 显式确认 |
| 禁止 | Forbidden | 删除系统文件、修改安全配置 | 永不注册 |

> **⚠️ 执行侧现状（2026-09-22 校准）**：
> - **`FORBIDDEN` 只有注册期一道闸**：`Src/Tools/Traits/specValidator.ts:110-113` 在 `registerTool()` 时把 `dangerLevel='FORBIDDEN'` 判为错误 → 拒注（`toolRegistry.ts:32` 调 `validateToolSpec`）。
>   **执行期没有硬拒分支**：`Src/Services/LoopControl/middleware/toolSafetyGate.ts` 只处理 `DANGEROUS+IRREVERSIBLE`（→ 审批）与 `dangerLevel !== 'SAFE'`（→ EffectJournal），
>   全文件无 `FORBIDDEN` 字样；即一旦某 FORBIDDEN 工具经其他路径进入 registry（或将来放宽注册校验），调用链会**静默放行**。
>   **待办**：`toolSafetyGate` 补 `if (dangerLevel === 'FORBIDDEN') return 拒绝`，作为纵深防御的第二道闸。
> - **"沙箱内执行"未落地**：`sandboxMode: 'strict' | 'standard' | 'none'` 只在 11 个工具 spec 里**声明**（`shell.exec`/`code.eval` 为 `strict`），
>   `Src/` 内除 `specValidator.ts:92-96` 的枚举合法性检查外**无任何执行侧消费者**；`Configs/security.json` 的 `sandboxDefaultMode` 亦 0 引用（见 §1.1）。
>   现状是：`shell.exec` 直接在工作进程内执行命令，仅受 `forbiddenCommands` 黑名单约束（`shellRunner.ts:49`）。

### 3.3.1 工具可见性与执行管控双轴模型

> **v2.2 新增**：原设计用 `dangerLevel × trustLevel` 单轴裁剪工具可见性，导致 Worker 无法使用 DANGEROUS 工具（如 `shell.exec`）。修订为两轴正交：

| 轴 | 真相源 | 作用 | 判定层 |
|----|--------|------|--------|
| **可见性 / 可调用性** | `ToolSpec.requiredRoles`（显式白名单） | 决定“谁能看到、谁能调” | 上下文装配（④）+ 执行前硬校验 |
| **执行管控强度** | `dangerLevel` + `reversibility` | 决定“是否审批、是否沙箱、是否记 EffectJournal” | `wrapToolCall` 中间件（ToolSafetyGate） |

**各角色默认可见工具表（2026-09-22 校准：按 `Src/Tools/` 实际注册的 11 个工具名逐条核对）**

| 工具（注册名） | dangerLevel | reversibility | requiredRoles（代码实际值） |
|------|:---:|:---:|------|
| `file.read` / `dir.list` / `file.grep` | SAFE | REVERSIBLE | 7 角色：`prime_director` `partner` `worker` `reviewer` `assembly_node` `auditor` `arbitrator`（**无 `regulator`**） |
| `web.search` / `vector.search` | SAFE | REVERSIBLE | `prime_director` `arbitrator` `partner` `worker` |
| `file.write` / `file.edit` | CONTROLLED | PARTIAL | `prime_director` `arbitrator` `partner` `worker` |
| `shell.exec` / `code.eval` | DANGEROUS | IRREVERSIBLE | `prime_director` `arbitrator` `partner` `worker` |
| `agent.recruit` | DANGEROUS | IRREVERSIBLE | `prime_director` `partner` |
| `agent.submit_review` | CONTROLLED | REVERSIBLE | `worker` |

> **原文档表格的三处名称错误（已更正）**：① 检索工具注册名是 **`file.grep`**，不是 `grep`；
> ② 提交评审的注册名是 **`agent.submit_review`**，不是 `submitForReview`（后者是 `Src/Tools/Custom/submitForReview.ts` 的导出变量名）；
> ③ **`dispatch.task` 与 `review.deliverable` 两个工具不存在**——`Src/Tools/builtinLoader.ts:32-46` 的 `BUILTIN_TOOLS` 只有上表 11 项，
> 任务派发与交付物评审目前走 `Services/ReviewerAgent` / `Services/Pipeline` 的函数调用，**未注册为模型可调用工具**（待办：若裁定必须是工具，需补注册并声明 spec）。
> 另：`arbitrator` 出现在读写/执行类白名单中，与 §3.2 "治理三权不参与业务执行"矛盾，属**现状 + 待裁定**（本条不抹平裁定）。

**治理级操作（仅 L0）：**

| 操作 | 角色 |
|------|------|
| 修改 Configs | Regulator |
| 强制 suspending 任意 Agent | Regulator |
| 审计日志回溯 | Auditor |
| 裁决冲突 | Arbitrator |

> **⚠️ 未实现 / 未接线（2026-09-22 校准）**：上表四项操作**没有任何一项被注册为工具**，因此"治理级仅 Regulator"在工具层无落地形态：
> `regulator` 角色**未出现在 11 个工具的任何 `requiredRoles` 中**（`grep -rn "regulator" Src/Tools/` 命中 0），
> 即 Regulator 当前**看不到也调不动任何工具**（连 §3.3.1 上表的 L0 兜底可见也无法过执行闸——`executeTool` 只认 `requiredRoles`，无兜底）。而 `auditor` 凭白名单**实际可执行且仅可执行** 3 个 SAFE 只读工具（`dir.list`/`file.read`/`file.grep`），其余一律 `ROLE_FORBIDDEN`。
> **待办**：为治理级操作补注册工具（如 `config.propose_change`、`agent.suspend`、`audit.trace_query`）并只声明对应角色；
> 在此之前 §3.2 的 L0 权限范围与 §6.6 的"角色未在权限矩阵声明即拒绝"只能是设计意图。

**运行时硬校验规则：**

1. 主循环 ④ 上下文装配时，仅将 `requiredRoles` 包含当前 Agent 角色的工具传给模型 —— ✅ **已实现，但实为两分支**（2026-09-22 校准）：`Src/Core/Loop/runIteration.ts:290` 调 `getVisibleToolsForRole`（`toolFactory.ts:75-86`），可见集 = `requiredRoles` 命中 **∪ 角色为 L0 治理级**（L0 兜底可见全部非 FORBIDDEN 工具，`:82-84`）——即 L0 兜底实际**覆盖了"白名单外不可见"的严格语义**，`auditor`/`arbitrator` 因此在代码里可见全部 11 个工具，与本节上方"auditor 只有 3 个 SAFE 只读工具"的白名单表形成张力，属现状记录。
2. `executeTool()` 执行前，检查 `context.agentRole ∈ tool.spec.requiredRoles`，否则返回 `ROLE_FORBIDDEN` —— ✅ **已实现**（`Src/Tools/Registry/toolRegistry.ts:98-104`）。⚠️ **与规则 1 不对称（2026-09-22 校准）**：执行侧**无 L0 兜底分支**——凭 L0 兜底获得可见性的角色（如 `regulator`）调用任何工具都会在 `:98` 被 `ROLE_FORBIDDEN` 拒绝，即"看得到、调不动"；两轴的真实可执行集只由 `requiredRoles` 单因决定。
3. `trustLevel` 仅用于治理级门禁（如修改 Configs、强制 suspending Agent），不参与普通工具裁剪 —— ⚠️ **半实现**：`toolFactory.ts:51/97` 仍用 `isToolAllowed(dangerLevel, trustLevel)` 做**第二重裁剪**（与本条"不参与普通工具裁剪"相悖，属现状 + 待裁定）；
   而"治理级门禁"一侧因无任何治理工具（见上表 ⚠️ 注）而**无消费者**。

### 3.4 Prompt 注入防御

> **⚠️ 未实现 / 未接线（2026-09-22 校准）**：下列三层防御中，**只有 [1] 的第一项以最小形态落地**。
> `InjectionGuardMiddleware` 与 `AnomalyScanMiddleware` **两个中间件在仓库中不存在**
> （`grep -rn "InjectionGuard\|AnomalyScan" Src/` 命中 0；`Src/Core/Middleware/builtin/` 只有 `goalReanchor` / `fingerprintDetector` / `budgetSentinel` / `failureInjector`）。
> **实际实现**：`Src/Services/Supervision/preSupervision.ts:44-49` 的 5 条英文关键字正则
> （`ignore (all )?previous instructions` / `you are now a` / `system: you are` / `<|im_start|>system` / `override safety`），
> 在 `:58` 对 `input.userInput` 做 `some(p => p.test(...))` 命中即返回 `PROMPT_INJECTION_DETECTED`；挂接点为 `Src/Core/Loop/runIteration.ts:241`（前置监管钩子，**已挂接**）。
> **缺口**：① 只覆盖用户输入，**工具返回值 / 检索到的外部内容不做检测**；② 只覆盖英文，中文注入变体与同义改写不命中；③ 无 Base64/URL 解码后复检；④ 无输出侧后置检测；
> ⑤ `[2]` 内容隔离的 `<external source="user" trust="L2">` 标记在 `Prompts/` 全库检索中**不存在**，隔离规则未写进任何提示词。
> **待办**：把 `[1]` 抽成可注册的 `wrapModelCall` 中间件并对工具输出复用同一规则集，`[3]` 至少落"系统提示词泄露"一条检测。

```
三层防御：                                    （以下为目标态结构，现状标注见上）

[1] 前置过滤（设计名：InjectionGuardMiddleware —— ⚠️ 无此中间件，实为 preSupervision 关键字检测）
    ├── 关键词检测（常见注入模式匹配）        ✅ 已实现（仅英文 5 条、仅用户输入）
    ├── 编码解码（Base64、URL 编码等还原后检测）⚠️ 未实现
    └── 结构异常检测（嵌套指令、角色劫持）      ⚠️ 未实现

[2] 内容隔离（L2 外部内容处理）                 ⚠️ 未实现
    ├── 所有外部输入用隔离标记包裹
    │   └── <external source="user" trust="L2">...</external>
    ├── 隔离标记内的"指令"不执行
    └── 模型 Prompt 中明确声明隔离规则

[3] 后置检测（设计名：AnomalyScanMiddleware —— ⚠️ 无此中间件）
    ├── 输出异常检测（是否包含系统指令泄露）    ⚠️ 未实现
    └── 行为异常检测（是否执行了非预期操作）      ⚠️ 未实现（`fingerprintDetector` 只检测重复动作，非注入语义）
```

### 3.5 Agent 沙箱隔离

```
每个 Worker/Partner 的隔离边界：

[1] 上下文隔离
    ├── 私有上下文（Scratchpad）其他 Agent 不可访问
    ├── 共享记忆通过 WriteGuard 保护
    └── 私有上下文在 Agent 销毁时清理

[2] 工具隔离
    ├── 工具白名单（最小权限原则）
    ├── Worker 只能使用 TaskAssignment.requiredTools 中列出的工具
    └── 禁止 Worker 自行扩权

[3] 信任隔离
    ├── 每个 Worker 独立信任边界
    ├── 跨 Agent 调用记为操作历史
    └── Agent 间不可直接访问对方的 Token 钱包
```

### 3.6 密钥管理

> **⚠️ 2026-09-22 校准（本节多条描述不实，逐条更正）**：
> - **引用语法只有 `env:XXX` 前缀**，**不支持 `${ENV_VAR}` 占位符插值**：
>   `configLoader.ts:204-217` 的 `resolveEnvReferences()` 仅对 `value.startsWith('env:')` 的字符串做 `process.env[ref.slice(4)]` 替换；
>   `keyStore.ts:132-147` 的 `resolveSingleRef()` 对非 `env:` 前缀直接返回错误 `不支持的密钥引用格式: … （仅支持 env:XXX）`。
>   写成 `"api_key_ref": "${HUAWEI_MAAS_API_KEY}"` 会被当普通字符串存进配置。
> - **环境变量缺失时静默保留原值**：`configLoader.ts:209-212` 只在 `envValue !== undefined` 时替换，否则把 `"env:XXX"` 字面量原样留在 `merged` 里，不报错不告警；
>   唯一的兜底报错发生在构造 Provider 时（`providerBase.ts:163-174` 抛 `环境变量 X 未设置`）。
>   **待办**：`resolveEnvReferences` 应记录 warn（或由 §1.2 的启动校验强制非空），否则非 Provider 路径（如嵌入、通知类密钥）会带着 `env:` 字面量继续运行。
> - **PBKDF2 派生与"加密敏感配置项"无实现**：`Src/` 内无 `pbkdf2` / `createCipheriv` / `createDecipheriv` 任何引用，`node:crypto` 只被用于 SHA-256 摘要与随机数
>   （`sessionKeyGenerator.ts` / `effectJournal.ts` / `idempotencyStore.ts` / `fileWriter.ts` 等），故 §5 的 `crypto → 密钥派生、加密` 亦为**目标态**。
> - **多 Key fallback 只在未接线的模块里**：`keyStore.ts:76-107` 的 `resolveKey()` 确实按 `refs[]` 顺序尝试并标 `isFallback`，
>   但 **整个 `Src/Infra/Security/keyStore.ts` 在 `Src/` 内 0 生产调用者**（只有 `Tests/Infra/security.spec.ts` 引用），
>   因此运行期**不存在轮换/回退能力**；且 `registerKey()` 无任何装配点调用它（`scanAndRegisterRefs()` 也无人调用）。
> - **泄露风险（现状 + 待办）**：`resolveEnvReferences` 在加载阶段就把真实密钥**写回 `merged`**，
>   任何 dump `config.merged` 的调试/日志路径都会带出明文（当前 `main.ts:58` 只打印 `env` 与 `configDir`，未主动泄露）；
>   脱敏函数 `getMaskedKey()` 在未接线的 keyStore 里。

```
密钥存储策略：                              （目标态，现状标注见上）

[1] API Key
    ├── 存储方式：环境变量（推荐）              ✅ 唯一实际方式（.env.example 只登记 HUAWEI_MAAS_API_KEY）
    ├── 配置文件引用：${ENV_VAR} 占位符        ⚠️ 语法不实 → 只支持 env:XXX 前缀
    └── 运行时：ConfigLoader 从环境变量读取并注入 ✅ 但缺失时静默保留原值（configLoader.ts:209-212）

[2] 数据库加密密钥                            ⚠️ 未实现
    ├── 从环境变量派生（PBKDF2）               ⚠️ 无 pbkdf2 / 无任何 KDF 调用
    └── 用于加密敏感配置项                     ⚠️ 无对称加密调用，SQLite 明文存放

[3] 密钥轮换                                  ⚠️ 未接线
    ├── 支持多 Key 配置（fallback 列表）        ⚠️ keyStore.resolveKey 已写，但整模块 0 生产调用者
    └── 轮换时旧 Key 保留一个周期               ⚠️ 无周期/过期概念
```

### 3.7 审计日志

| 审计事件 | 记录内容 | 保留期限（本表裁定） | 生效状态 |
|---------|---------|---------|:---:|
| 安全事件 | 注入检测、越权尝试、冻结操作 | 永久 | ⚠️ 未接线（且与 `retention.eventsDays: 90` 冲突） |
| 权限变更 | Agent 招募/开除、信任级别变更 | 180 天 | ⚠️ 未接线（与 `retention.operationHistoryDays: 180`、`dataStorage.operations.retentionDays: 180` 三处同值重复定义） |
| 配置变更 | 热更新配置的操作记录 | 90 天 | ⚠️ 未实现（热重载本身未接线，见 §1.3；无配置变更审计记录） |
| Token 交易 | 所有交易记录 | 永久 | ⚠️ 未接线（`retention.tokenTransactionsPermanent: true` 无执行者） |
| 仲裁记录 | 完整案件记录 | 永久 | ⚠️ 未接线（与 `dataStorage.decisions.retentionDays: 365` **直接冲突**） |

> **⚠️ 保留期"三套并存"（2026-09-22 校准 · 现状 + 裁定建议）**：同一语义的保留期目前分散在三处，且**没有任何一处被运行期执行**：
>
> | 来源 | 状态 | 冲突点 |
> |------|------|--------|
> | 本节表（文档裁定值） | 仅文档 | 「安全事件永久」vs `session.retention.eventsDays = 90`；「仲裁记录永久」vs `dataStorage.decisions.retentionDays = 365` |
> | `Configs/session.json` 的 `retention.*`（11 键） | ⚠️ `configLoader` 加载了文件，但 `Src/` 内 `retention` **0 字面量引用**（见 §2.10） | 覆盖 DB 表级保留期，无文件系统维度 |
> | `Configs/dataStorage.json`（7 类数据 × `retentionDays`/`maxSizeMb`/`cleanupPriority`/`cleanupStrategy` + `cleanupOrder` + `globalQuotaMb` + `autoCleanupEnabled` + `cleanupIntervalHours`） | ⚠️ **未被 `configLoader` 加载**（不在 `extraConfigs` 白名单，见 §1.1），`Src/` 与 `Scripts/` 内 **0 引用** | 唯一带"清理优先级/配额"维度的一套 |
>
> **裁定建议（指定唯一源）**：
> 1. **唯一源 = `Configs/dataStorage.json`**，按**数据目录/存储类别**维度管保留期与清理（它是唯一含 `cleanupPriority` / `cleanupOrder` / `maxSizeMb` / `globalQuotaMb` 的一套，且与 `data-storage.md` 的七类数据口径对齐）；
>    需先把它加入 `configLoader.ts:87-92` 的 `extraConfigs` 白名单，并由清理任务消费；
> 2. `session.json` 的 `retention.*` 收敛为 **DB 表级 TTL 的唯一源**（表内行级清理，如 `tasks`/`loops`/`events`），
>    与 (1) 的边界写死为：**`dataStorage` 管"文件/目录 + 整库配额"，`session.retention` 管"表 + 行"**；两者不得再出现同义键（现有 `operations` 与 `operationHistoryDays` 即重复，待合并）；
> 3. 本节的文档表**降级为引用**：只保留"审计事件 → 记录内容"两列，删除"保留期限"列改为指向 (1)(2) 的键名；
>    在此之前，本节数值与配置文件数值**均无运行期效力**。
> 4. 冲突优先裁定：**安全事件与仲裁/决策记录是否永久**必须先定案（`eventsDays: 90` 与 `decisions: 365` 会让"永久"落空），
>    定案前代码不得实现自动清理，以免误删待裁定的数据。

---

## 4. 错误处理策略

### 4.1 错误分级

| 级别 | 名称 | 处理策略 |
|------|------|---------|
| ERROR | 可恢复错误 | 记录日志 + 通知相关组件 + 尝试恢复 |
| FATAL | 不可恢复错误 | 记录日志 + 保存状态 + 终止当前轮次 + 通知用户 |
| PANIC | 系统级崩溃 | 保存快照 + 紧急终止 + 通知管理员 |

### 4.2 关键错误场景

| 场景 | 级别 | 处理 |
|------|------|------|
| LLM 调用失败 | ERROR | 按 fallback 列表尝试下一个 Provider |
| 数据库写入失败 | ERROR → FATAL | 重试一次，仍失败则 FATAL |
| Token 余额不足 | ERROR | 暂停任务 + 通知 Director |
| Agent 连续失败 | ERROR | 3 次后开除 + 重新招募 |
| 仲裁死锁 | FATAL | 升级至 Regulation |
| 数据库损坏 | PANIC | 保存快照 + 紧急终止 |
| 账本守恒异常 | FATAL | 暂停交易 + 通知 Audit |

---

## 5. 依赖关系

| 依赖组件 | 来源 | 用途 |
|---------|------|------|
| `better-sqlite3` | npm 包 | 数据库操作（✅ `Src/Infra/Db/database.ts:16`） |
| `zod` | npm 包 | 配置 Schema 验证 —— **⚠️ 2026-09-22 校准：已声明依赖（`package.json:98`）但 `Src/`、`Tests/`、`Client/src` 内 0 处 import**；配置校验实为 `Src/Infra/Config/configValidator.ts` 的手写字段检查 |
| `chokidar` | npm 包 | 配置文件热更新监听 —— **⚠️ 2026-09-22 校准：引入未使用**（`package.json:95` 已声明，`Src/` 内 **0 处 import**；`configWatcher.ts` 用的是"轮询 + 注释桩"，且轮询从未启动，见 §1.3） |
| `ws` | npm 包 | WebSocket 服务端（`Interface/WebSocket/wsGateway.ts:9`）—— **✅ 已引入**（`package.json:97`；原标注"F0.2 待引入"已过时，2026-09-22 更正） |
| `crypto` | Node.js 内置 | 密钥派生、加密 —— **⚠️ 目标态**：现状仅用 `createHash`（SHA-256 摘要）与 `randomBytes`/`randomUUID`，**无 KDF、无对称加密**（见 §3.6） |

---

## 6. v2 补丁：工具契约、审批门与 Provider 格式修正

> **修订依据**：`Docs/99-审查记录/设计审查报告-Loop与Harness工程.md` §1 部件④、§4.4、§5.5。

### 6.1 工具注册强制字段（v2 扩展）

旧版只定义 `dangerLevel`，新版新增 `idempotency` / `reversibility` / `sideEffectScope`，缺一不可注册：

```typescript
// Src/Tools/Traits/toolSpec.ts:52  （2026-09-22 校准：原标注 Tools/Traits/types.ts 有误，该文件不存在）
interface ToolSpec {
  name: string;                          // 例：'file.write'
  version: string;                       // 语义化版本
  description: string;                   // 展示给模型的描述（单一职责）
  inputSchema: JsonSchema;               // 严格类型，禁止 additionalProperties:true
  outputSchema: JsonSchema;              // 必须包含 {status:'success'|'error', recoverable:boolean}

  dangerLevel: 'SAFE' | 'CONTROLLED' | 'DANGEROUS' | 'FORBIDDEN';

  // 🔴 v2 新增三项，缺一不可
  idempotency: 'YES' | 'NO' | 'CONDITIONAL';
  idempotencyKeyFields?: string[];       // 非 NO 时必填
  reversibility: 'REVERSIBLE' | 'PARTIAL' | 'IRREVERSIBLE';
  sideEffectScope: 'none' | 'workspace' | 'filesystem' | 'network' | 'process' | 'external';

  // 安全执行参数
  requiredRoles: UserRole[];
  sandboxMode: 'strict' | 'standard' | 'none';
  timeoutMs: number;                     // 默认 30_000
  rateLimitPerAgent?: { max: number; windowMs: number };
}
```

**自动门禁**（代码审查 + CI）：

> **⚠️ 2026-09-22 校准**：以下三条**均无 CI 侧实现**，`.github/workflows/ci.yml` 与 `Scripts/` 内不含任何工具 spec 门禁步骤；
> 唯一强制点是**启动 ⑫ 注册期**（`toolRegistry.ts:32` → `validateToolSpec()` 返回错误即拒注，`main.ts:177-178` 遇错直接抛异常终止启动）。

- 任何 `dangerLevel='DANGEROUS'` 且 `reversibility='IRREVERSIBLE'` 的工具 **必须** 声明 L4 HumanGate（Docs/12 §6）
  —— **⚠️ 未实现（现状 + 待办）**：`Src/Tools/Traits/specValidator.ts:103-108` 命中该组合时**只有一个空注释块，既不阻断也不告警**（源码原话："标记但不阻断——L4 HumanGate 在 S6 的 approvalGate 中强制 / 此处仅记录警告"），而实际审批是执行期由 `toolSafetyGate.ts:90-128` 临时创建，**注册期无法声明 L4**（`ToolSpec` 无 HumanGate 字段）。
  **待办**：要么在 `ToolSpec` 增 `requiresHumanGate: boolean` 并在 `specValidator` 内做成硬错误，要么删除本条"CI 拒收"表述。
  > 现命中该组合的注册工具有 3 个：`shell.exec`、`code.eval`、`agent.recruit`（均为 `DANGEROUS` + `IRREVERSIBLE`）。
- 任何 `idempotency='NO'` 的工具 **必须** 先写 EffectJournal INTENT（Docs/13 §4）
  —— ✅ **语义已落地，但发生在执行期而非注册期**：`toolSafetyGate.ts:131-153` 对所有非 SAFE 工具写 INTENT→EXECUTING→终态；
  ⚠️ 注册期**不做该前置校验**，且 `idempotency='NO'` 时**跳过幂等缓存查询**（`:74`），写库失败还是 fail-open（`:149`）。
- `outputSchema` 缺 `status` 或 `recoverable` 字段 → ~~CI 拒收~~ **✅ 启动期拒注**（`specValidator.ts:52-57` + `validateOutputSchema`）；
  现 11 个内置工具统一用 `contentOutputSchema(...)` 生成，故实际不会出现缺字段，但**CI 不拦**。

### 6.2 工具返回格式（强制）

所有工具必须返回下列形式，与 skill `source-code.md` 一致：

```json
{
  "role": "tool",
  "tool_call_id": "{operation_id}",
  "status": "success",
  "recoverable": false,
  "content": { /* 具体业务数据 */ },
  "effects": ["file.write:/Data/Workspace/.../a.ts"],
  "artifacts": [{"path":"...","hash":"sha256:...","size":1024}]
}
```

失败时：

```json
{
  "role": "tool",
  "tool_call_id": "{operation_id}",
  "status": "error",
  "recoverable": true,
  "error": {
    "code": "FILE_PERMISSION_DENIED",
    "message": "...",
    "details": { "path": "...", "required_perm": "..." }
  }
}
```

`recoverable` 决定 Loop 控制行为：`true` → 写 FailureFeedback 后重试/换策略；`false` → 直接风险退出。

### 6.3 不可逆操作审批（对齐 Docs/12 §6）

| 场景 | 默认 dangerLevel | 需审批 | 默认超时行为 | 生效状态（2026-09-22 校准） |
|------|-----------------|--------|-------------|------|
| 删除 `Data/` 以外文件 | CONTROLLED | 首次 | 60s → 拒绝 | ⚠️ **无实现**：11 个注册工具里**没有删除类工具**（`builtinLoader.ts:32-46`），"首次审批"无任何触发点；`pathGuard` 的 `checkPath(..., 'delete')` 形参存在但无人以 `'delete'` 调用 |
| 执行 shell 命令命中白名单外 | DANGEROUS | 每次 | 60s → 拒绝 | ⚠️ **口径不符**：`shellRunner.ts:49` 走的是 `isCommandForbidden()` **黑名单**（`security.json.forbiddenCommands` 2 条），**不存在白名单**，命中黑名单直接拒绝而非转审批 |
| 向外部网络发送含个人信息的请求 | DANGEROUS | 每次 | 60s → 拒绝 | ⚠️ **无实现**：无 PII 识别逻辑；`networkWhitelist` 的判定函数 `isNetworkAllowed()` 0 调用者（见 §1.1） |
| 开除另一个 Agent | DANGEROUS | 监管局 | 无超时 | ⚠️ **无实现**：无 `agent.expel` 工具；监管局介入走 `Services/Regulation` 内部函数，不产生 `PendingApproval` |
| Token 罚没/冻结 | DANGEROUS | 监管局 | 无超时 | ⚠️ **无实现（审批侧）**：`walletManager.freezeWallet()` / `resourceAuditBureau.freeze()` 存在，但**不创建审批记录**，即"监管局审批"这一环缺失 |
| 支付/退款/发布 | FORBIDDEN 或 DANGEROUS | 用户 | 60s → 拒绝 | ⚠️ **无实现**：无对应工具注册 |

> **⚠️ 现状：实际只有 1 条审批路径生效**（2026-09-22 校准）：
> `toolSafetyGate.ts:90-128` —— 当被调用工具的 `dangerLevel='DANGEROUS'` **且** `reversibility='IRREVERSIBLE'` 时创建 `kind='irreversible_action'` 审批。
> 命中工具为 `shell.exec` / `code.eval` / `agent.recruit`（见 §6.1）。其余 5 行场景**均无代码路径**。
> 三个必须记录的现状问题：
> 1. **该唯一路径必然失败**：审批以 `riskLevel: 'CRITICAL'` 创建，而 `approvalGate.ts:53-58` 要求 CRITICAL 至少 2 个**不同角色**的审批人，
>    `ToolSafetyGate` 默认 `approverRoles` 只有 `{role:'user'}` 一个（`toolSafetyGate.ts:26/33`，`runIteration.ts:577-581` 不覆盖）→
>    `createApproval` 返回错误 → 工具以 `审批创建失败: CRITICAL 级审批至少需要 2 个不同角色的审批人` 被拒。
>    **净效果**：三个危险工具当前**等价于硬拒**，不是"等人工审批"。**待办**：默认 deciders 补第二角色，或把该分支的 `riskLevel` 降为 `HIGH`。
> 2. **不等待审批**：`toolSafetyGate.ts:114-120` 在状态非 `APPROVED` 时**立刻**返回 `recoverable: true` 的错误，无挂起/回灌机制；
>    人经由 REST（`Src/Interface/RestApi/approvalApi.ts`，`routes.ts:24` 已注册）批准后，需**模型自行重发同一次工具调用**才可能通过。
>    审批队列本身是**进程内 `Map`**（`approvalGate.ts:94`），重启即清空。
> 3. **超时扫描从不运行**：`checkTimeoutApprovals()`（`approvalGate.ts:137`）在 `Src/` 内 **0 调用者**（只有 `Tests/` 调），
>    因此上表所有"60s → 拒绝"的超时语义**不会发生**，PENDING 记录会一直留在内存队列里；
>    `supervision.approvalTimeoutSec` / `approvalDefaultOnTimeout` 也因此无效果（见 §2.7）。**待办**：`main.ts` 启动一个定时任务调用它。

**硬禁**（不允许审批开启）：
- 读写 `Data/Auth/` `~/.ssh/` 目录； —— ✅ 已落地（`security.json.forbiddenPaths` → `initPathGuard` → 文件类工具的 `checkPath`，见 §1.1）
- `rm -rf /` `:(){ :|:& };:` 类破坏性命令； —— ✅ 已落地（`security.json.forbiddenCommands` → `shellRunner.ts:49`）
- 修改 `Configs/` 下带 L3 不可变标记的项； —— ⚠️ **未实现**：不存在"L3 不可变标记"这一数据结构，`mutationLevels.ts` 的分级判定 0 调用者（见 §1.3），也无任何配置写入 API
- 向未声明的外部域名发送密钥/PII。 —— ⚠️ **未实现**：`isNetworkAllowed()` 0 调用者，出站域名不做校验

### 6.4 Provider 配置格式修正（对齐 skill）

旧版 `modelRouter.json` 存在以下违规：
- ❌ 字段名自创（如 `api_key`）→ ✅ 必须为 `api_key_ref`，不直接写密钥；
- ❌ 缺 `context_window` 必填 → ✅ 新增（**⚠️ 2026-09-22 校准："启动 ⑫ 会拒注"不实**：`configValidator.ts` 全文不校验 `providers[]`，
  而启动 ⑫ 是**工具**注册；Provider 侧 `main.ts:150-160` 对构造异常是 `catch {}` **静默跳过**，
  缺字段只会在调用时经 `resolveModel` 报错。现状是"少一个可用 provider"而非"启动失败"，**待办**：`configValidator` 补 `providers[]` 必填校验并去掉静默 catch）；
- ❌ 无 `verifierModel` → ✅ 新增，保障 Maker-Checker 不同模型（⚠️ 现值与 `directorModel` 同为 `huawei-maas/GLM-5.1`，见 §2.2）；
- ❌ 无 `reasoningSandwich` → ✅ 新增，阶段化模型路由（**⚠️ 字段已登记但运行期 0 消费者**，见 §2.2 生效状态表）。

**完整示例参考 Docs/02 §10.4**（本文件不重复）。

### 6.5 安全事件分级与上报（v2 补充）

| 级别 | 定义 | 上报 | 响应时间 | 生效状态（2026-09-22 校准） |
|------|------|------|---------|:---:|
| INFO | 无危险但可审计（审批拒绝） | 审计日志 | — | ⚠️ 无此分级：`Src/` 内不存在安全事件 severity 枚举（`RiskLevel` 仅用于审批门，`LoopControl/types.ts:33`）；审批拒绝只写 `logger` |
| WARN | 疑似扫描/探测，无实际危害 | 审计日志 + 监控面板 | 24h 回顾 | ⚠️ 同上，且无"回顾"任务（见 §1.3 定时器全部未启动） |
| ERROR | 拦截了一次实际危害（注入命中 / FORBIDDEN 工具调用） | + 监管局广播 + 用户通知 | 5 min | ⚠️ 部分：注入命中会返回 `PROMPT_INJECTION_DETECTED`（`preSupervision.ts:58-64`）但**不产生审计事件、不广播、不通知**；FORBIDDEN 执行期无拒分支（§3.3） |
| CRITICAL | 确认危害发生（密钥泄露 / 硬禁路径被访问） | + 立即全局冻结 + 人工升级 | 30s | ⚠️ 无实现：`security.json.criticalSecurityEvents`（`KEY_LEAKAGE`/`FORBIDDEN_PATH_ACCESS`/`INJECTION_DETECTED`）在 `Src/` 内 **0 字面量引用** |

**硬约束**：CRITICAL 级别 → 系统自动切至 “restricted mode”，仅允许白名单内只读工具运行，直到人工解除。

> **⚠️ 未实现（2026-09-22 校准）**：`Src/` 内 **不存在 `restrictedMode` / restricted mode 的任何标识**（大小写不敏感检索 0 命中），
> 也没有"系统级降级闸"可承载它。现有最近似的两块拼图都不等价：
> ① `Services/Audit/freezeManager.freezeAgent(agentId, reason)` 是**单 Agent 级**冻结，且 `autoUnfreeze` 由调用方驱动（`setAutoUnfreezeSec()` 0 调用者，见 §2.6）；
> ② `Services/Regulation` 的熔断只影响 Token 消耗告警，不裁剪工具可见性。
> **待办**：若要落地本条，需先定义全局降级状态源（建议放 `Infra/Sandbox` 或 `ToolRegistry` 的运行时开关），
> 并让 `getVisibleToolsForRole()` 在降级态只返回 `dangerLevel='SAFE'` 且 `sandboxMode='none'` 之外的只读工具，
> 同时把解除动作纳入 §6.3 的审批模型。在此之前，本表与上句**只是设计意图**。

### 6.6 默认拒绝原则强化

以下任一情况未声明时，一律 **拒绝**：
- 工具未在 Tools/Traits 注册；
- Agent 角色未在权限矩阵声明；
- 目标路径不在 Workspace 边界内；
- 外部域名不在 network 白名单；
- Prompt 变量未声明来源与信任级；
- **新增**：Verifier L4 审批超时；
- **新增**：恢复时存在 UNKNOWN 副作用且无法自动判定。

---

## 7. 修订记录

> 更早的 v2 / v2.1 修订以正文内联标记（`v2 补丁`、`v2.1 补`、`v2.1 修正`）形式保留，未在此重复登记。

| 日期 | 修订内容 |
|------|---------|
| 2026-09-22 | **按代码现状校准**（与 Docs/04/05/06/07 同口径，仅改文档不改代码）：① §1 新增校准基线与判定口径（12 个被加载 JSON 的 204 个叶子键中 104 键＝51% 在 `Src/` 无任何字面量引用；✅＝被运行期读取，而运行期唯一读者是 `Src/main.ts` 的 `getConfigValueOr`）；② §1.1 补 `dataStorage.json` 行与"configLoader 实际加载清单"脚注（`dataStorage`/`coverageBaseline`/`benchBaseline` 均不被加载），`{env}.json`/`local.json` 标注"当前均不存在，属可选层"；③ §1.2 记三层优先级的**落地缺陷**为现状＋代码债（功能 JSON 在 `configLoader.ts:87-113` 强制覆盖 `merged`，`local.json` 对这批键必然失效）；④ §1.3 标注 L0–L3 无运行期含义（`configWatcher` 为注释桩、`startWatching`/`onConfigChange` 0 调用者、`mutationLevels` 无生产调用者）；⑤ **§2.1–§2.11 十一张参数表全部新增"生效状态"列**（本组核心交付）；⑥ §2.2 以真实 `modelRouter.json`（单一 `huawei-maas` + 3 模型 + 单元素 `fallbackOrder`）替换 v2.1 示例，并按原则 2 记与"≥3 provider 且跨厂商"裁定的冲突为现状＋待办，§4.2"无次级 fallback"同步修正；⑦ §2.8 记"唯一事实源"两处硬缺陷——读取路径错位（`main.ts:195` 读 `loopConfig` 顶层 vs `configLoader.ts:101-111` 直接挂顶层键 ⇒ 恒为 `{}`）与 `Core/Loop/loopConfig.ts` 的 `ROLE_OVERRIDES`/`LOOP_LIMITS` 双源漂移，裁定"两份都不为准"；⑧ §2.3/§2.5/§2.6/§2.7 记录字段名与数值漂移及 0 调用者的初始化函数（`minArbitrators` 1 vs 0、`scaleUpFactor` vs `scalingFactor`、`monitoringIntervalSec` vs `Ms`、熔断阈值 100／冷却 120s、去重窗口 60s vs 300s、`approvalTimeoutSec` 恒为 60）；⑨ §2.9/§2.10/§2.11 标注热路径全未接线（checkpoint/metrics/shutdown 占位、`retention` 关键字整库 0 引用、`capsule*`/writeGuard 仅 FORBIDDEN_KEYS、`vectorDim 1536` 过期、`maxQueueSize` 硬编码）；⑩ §3.1 威胁模型与 §3.4 注入防御按代码改写（实际为 `preSupervision.ts:44-49` 五条英文正则关键字匹配，`InjectionGuardMiddleware`/`AnomalyScanMiddleware` 不存在）；⑪ §3.3/§3.3.1 更正工具清单为实际注册的 11 个（修正 `file.grep`、`agent.submit_review` 等 3 处名称错误，`dispatch.task`/`review.deliverable` 不存在），并记 FORBIDDEN 只在注册期拒绝、执行侧无分支、`sandboxMode` 无消费者、`arbitrator` 出现在读/写/执行白名单为现状＋待裁定、治理操作因 `regulator` 缺席所有 `requiredRoles` 而全部不可见；⑫ §3.6 密钥管理重写（仅支持 `env:` 前缀、缺变量静默保留字面量、无 PBKDF2/加密、`keyStore` 多 Key fallback 有实现但整模块 0 生产调用者、明文密钥回填 `merged` 为泄露面）；⑬ §3.7 加生效状态列并新增"保留期三套并存"对照表与"指定唯一源"裁定建议；⑭ §5 依赖表更正（chokidar 引入未使用、zod 0 引用故校验为手写、`ws` 已引入而非"F0.2 待引入"、`crypto` 标目标态）；⑮ §6.1 记无任何 CI 强制（仅启动期注册闸），DANGEROUS+IRREVERSIBLE⇒L4 规则是 `specValidator.ts:103-108` 的空注释块（标记不阻断，现命中 3 个工具），NO_INTENT 检查发生在执行期且 `toolSafetyGate.ts:149` fail-open；⑯ §6.3 审批矩阵加生效状态列（CRITICAL 需 ≥2 个不同角色审批人 vs 默认单一 `user` 审批人 ⇒ 三个危险工具实质永久拒绝；无等待/滞留、队列仅内存、`checkTimeoutApprovals` 0 调用者故"60s 超时拒绝"永不触发）；§6.4 更正"启动 ⑫ 会拒注"不实（`configValidator` 不校验 `providers[]`，`main.ts:150-160` 静默 `catch {}`）；§6.5 加生效状态列并记 `restrictedMode` 在 `Src/` 内无任何标识、系统级降级闸不存在。 |

