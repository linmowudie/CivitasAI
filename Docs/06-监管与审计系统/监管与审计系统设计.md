# 监管与审计系统设计

> 本文档定义 Civitas-AI 的监管局（Regulatory Authority）和资源审计局（Resource Audit Bureau）的完整设计。

---

## 0. 本文覆盖范围：Regulation 与 Supervision 两套"监管"实现的分工

> ⚠️ 2026-09-22 校准：代码里存在**两套彼此独立**的监管实现，本文档此前只描述了第 1 套，易被误读为"监管 = Regulation"。

| # | 子系统 | 代码位置 | 形态 | 职责 | 文档归属 |
|---|--------|---------|------|------|---------|
| 1 | **Regulation（监管局）** | `Src/Services/Regulation/`：`behaviorCode.ts` / `broadcastChannel.ts` / `finalArbiter.ts` / `regulatoryAuthority.ts` | 全局规则层：行为准则、广播、死锁最终裁决、紧急干预 | 面向**跨任务/全局**的裁决与干预 | **本文档 §1** |
| 2 | **Supervision（监督钩子）** | `Src/Services/Supervision/`：`preSupervision.ts` / `compressSupervision.ts` / `summarySupervision.ts` / `reasoningSupervision.ts` / `loopSupervision.ts` / `postSupervision.ts`（出口 `index.ts`） | 六钩子：`runPreSupervision` / `runCompressSupervision` / `runSummarySupervision` / `runReasoningSupervision` / `runLoopSupervision`（另有 `detectDeadlock`）/ `runPostSupervision` | 面向**单次迭代**的读侧监督（安全注入检测、频率限制、压缩/摘要/推理/循环质量监控、退出判定） | **Docs/02 §3 步骤②⑤⑨ 与 §4.4**；参数见 **Docs/11 §2.7**（`Configs/supervision.json`） |

**接线现状（2026-09-22 校准）**：

- 六钩子中仅 `runPreSupervision`（`Core/Loop/runIteration.ts:241`）与 `runPostSupervision`（同文件 `:470`）被调用；
  ⑤ 的四项"懒监管"（compress/summary/reasoning/loop）**未接入**，与 Docs/02 §3 的 ⚠️ 标注一致。
- `Configs/supervision.json` 的键（`qualityThreshold` / `agentConsecutiveFailuresToExpel` / `patrolEnabled` /
  `triggerCooldownSec` / `broadcastAckTimeoutSec` / `approvalTimeoutSec` 等）在 `Src/` 中**当前无任何读取方**，
  各模块使用内联默认值 —— 属未接线项，本文 §2.6 与 Docs/11 §2.7 引用这些键时均视为**设计目标**。
- 两套实现**不互相调用**：`Supervision` 不读行为准则，`Regulation` 也不订阅迭代钩子；
  唯一的间接联系是失败开除阀值语义（`behaviorCode.ts` 的 `cooperation-001` 规则 ↔ `supervision.agentConsecutiveFailuresToExpel`）。

---

## 1. 监管局（Regulatory Authority）

### 1.1 职责边界

| 职责 | 说明 |
|------|------|
| 全局规则维护 | 制定、更新、发布《智能体行为准则》 |
| 跨域协调 | 处理跨任务/跨 Agent 的重大事件 |
| 广播通道 | 允许 Agent 提交重大异常报告，全局广播通知 |
| 最终裁决 | 仲裁庭死锁时，作为最终裁决方介入 |
| 紧急干预 | 系统级异常时强制终止/重启 Agent |

### 1.2 核心组件

| 组件 | 实现（函数式模块） | 职责 |
|------|------|------|
| 行为准则管理器 | `initBehaviorCode` / `addRule` / `removeRule` / `updateVersion` / `checkViolation`（`Regulation/behaviorCode.ts:94,148,159,169,125`） | 加载/更新/发布行为准则 |
| 广播通道 | `broadcast(params)` / `acknowledge(broadcastId, agentId)`（`Regulation/broadcastChannel.ts:42,94`） | 管理全局广播消息 |
| 紧急干预器 | `emergencyIntervene({ type, reason, targetAgentIds? })`（`Regulation/regulatoryAuthority.ts:105`） | 强制执行终止/重启操作 |
| 最终裁决器 | `issueFinalVerdict({ case_, reason, overrideVerdict? })`（`Regulation/finalArbiter.ts:37`） | 仲裁死锁时的降级裁决 |
| 监管局主入口 | `initRegulatoryAuthority` + 上述能力代理（`Regulation/regulatoryAuthority.ts:48`） | 聚合四模块对外出口 |

> ⚠️ 2026-09-22 校准：原表所列 `BehaviorCodeManager` / `BroadcastChannel` / `EmergencyIntervener` / `FinalArbiter` **均为类名写法，Src 中不存在这些类**；
> `Services/Regulation/` 与仲裁一样采用**函数式模块**（且无 `index.ts` 汇总出口，调用方直接 import 单文件）。

### 1.3 行为准则

```typescript
interface BehaviorCode {
  version: string;                     // 语义化版本号
  rules: BehaviorRule[];
  effectiveAt: number;                        // epoch ms
  publishedAt: number;                        // epoch ms
}

interface BehaviorRule {
  ruleId: string;
  category: 'safety' | 'communication' | 'resource' | 'cooperation';
  description: string;                 // 人类可读规则描述
  condition: string;                   // 触发条件（结构化表达式）
  action: 'warn' | 'restrict' | 'forbid' | 'report';
  severity: 'low' | 'medium' | 'high' | 'critical';
  enforceable: boolean;                // 是否可自动执行
}
```

**默认行为准则（Phase 1 最小集）：**

| 规则 | 类别 | 动作 | 可执行 |
|------|------|------|--------|
| 禁止 Agent 越权访问其他 Agent 私有上下文 | safety | forbid | ✓ |
| 禁止通过 Prompt 注入篡改仲裁/审计逻辑 | safety | forbid | ✓ |
| Worker 连续 3 次交付失败须被开除 | cooperation | report | ✓ |
| 重大异常须上报监管局 | communication | report | ✓ |
| Token 消耗超预算须申请补充 | resource | warn | ✗ |

> **2026-09-22 校准**：第 3 行原写 `enforce`，与代码不符——`behaviorCode.ts:67-70` 的 `cooperation-001` 实际 `action: 'report'`，
> 且 `RuleAction` 联合类型只有 `'warn' | 'restrict' | 'forbid' | 'report'`（`behaviorCode.ts:17`），**无 `enforce` 取值**。
> "连续失败即开除"目前只上报、不自动执行；实际开除动作仍属目标态（Docs/02 §状态机引用 `supervision.agentConsecutiveFailuresToExpel`，该键亦无代码消费）。
> 表内规则与 `DEFAULT_RULES`（`behaviorCode.ts:44`）逐条对应；违规判定目前是 `checkViolation` 的关键词匹配（`:132-138`），非条件表达式求值。

### 1.4 广播机制

```typescript
interface BroadcastMessage {
  broadcastId: string;
  type: 'rule_update' | 'emergency_alert' | 'system_notice' | 'arbitration_deadlock';
  priority: 'normal' | 'high' | 'critical';
  
  title: string;
  content: string;
  
  // 目标范围
  targetScope: 'all' | 'specific_agents' | 'specific_role';
  targetAgentIds?: string[];
  targetRole?: string;
  
  // 要求
  requiresAck: boolean;                // 是否需要确认收到
  ackDeadline?: number;                // 确认截止时间（epoch ms）
  
  publishedAt: number;                        // epoch ms
  publishedBy: 'regulatory_authority';
}
```

### 1.5 最终裁决（仲裁死锁升级）

```
仲裁庭判定死锁（3-LLM 无法达成多数决 或 超时 120s）
    │                                        // ⚠️ 现状：裁决死锁后**无人调用升级**（ARBITRATION_DEADLOCK 无发布方），
    │                                        //    本流程为可调用的目标态（2026-09-22 校准）
    ▼
Arbitration → Regulation: escalateDeadlock(case_, reason)   // regulatoryAuthority.ts:96（原写 escalate(caseId) 不存在）
    │                                        // reason: 'deadlock' | 'timeout' | 'escalation'，入参是**案件对象**而非 caseId
    ▼
issueFinalVerdict({ case_, reason, overrideVerdict? })      // finalArbiter.ts:37（原写 FinalArbiter.arbitrate(case) 不存在）
    ├── 前置校验：case.status 必须为 'deadlocked' 或 'reasoning'，否则返回 err
    ├── 降级为单 LLM 强制裁决（不再要求多数决）
    │   └── ⚠️ Phase 0-2：不接 LLM，直接取 overrideVerdict ?? 'new_wins'，modelUsed = 'rule-based'（:76）
    ├── 使用更高能力模型（如 gpt-4o）        // ⚠️ 未实现
    ├── 裁决标记为 "regulatory_intervention"
    │   └── 代码实际字段：事件 payload.isRegulatoryIntervention = true + interventionId
    └── 广播 ARBITRATION_VERDICT（priority='critical'，source='Regulation/finalArbiter'）
    │
    ▼
同时通知用户：
    └── "仲裁庭出现死锁，已由监管局介入裁决。裁决结果：..."   // ⚠️ 未实现（无人工通知链路）
```

### 1.6 紧急干预

| 干预类型 | 触发条件 | 动作 |
|---------|---------|------|
| 强制终止 Agent | Agent 行为严重违反行为准则 | 立即冻结 + 销毁 Agent |
| 暂停全局任务 | 系统级异常（如 Token 大面积异常消耗） | 暂停所有非核心 Agent |
| 强制重启 | 系统状态不可恢复 | 保存快照 → 重启核心组件 |
| 规则紧急更新 | 发现新的安全漏洞 | 立即发布新规则 + 广播 |

> **2026-09-22 校准**：入口是 `emergencyIntervene({ type, reason, targetAgentIds? })`（`regulatoryAuthority.ts:105`），
> `InterventionType` = `'force_terminate' | 'pause_all' | 'force_restart' | 'rule_emergency'`，与上表四行一一对应。
> 现状**只发布 `EMERGENCY_INTERVENTION`（critical）+ 同步广播 `emergency_alert`**，Src 中无该事件的订阅方，
> 故"立即冻结 + 销毁 Agent""保存快照 → 重启核心组件"等动作**未执行**（`record.executed` 仅表示事件发布成功）。

---

## 2. 资源审计局（Resource Audit Bureau）

### 2.1 职责边界

| 职责 | 说明 |
|------|------|
| Token 异常检测 | 监控 Agent Token 消耗，识别异常模式 |
| 自动冻结 | 对异常 Agent 执行钱包冻结 |
| 稽查流程 | 深入分析异常原因，判定是否为误报 |
| 定期巡检 | 周期性全量审计所有 Agent 的 Token 使用 |
| 处罚执行 | 对确认的异常行为执行 Token 罚没 |

### 2.2 核心组件

| 组件 | 实现（函数式模块） | 职责 |
|------|------|------|
| 异常检测器 | `checkRollingWindow` / `checkDeviation` / `checkLoop`（`Audit/anomalyDetector.ts:73,100,130`） | 实时监测 Token 消耗异常 |
| 冻结管理器 | `freezeAgent` / `unfreezeAgent` / `checkAutoUnfreeze`（`Audit/freezeManager.ts:42,77,108`） | 执行 Agent 冻结/解冻 |
| 稽查器 | `investigate(agentId)`（`Audit/resourceAuditBureau.ts:99`） | 执行深度稽查分析 |
| 巡检调度器 | `executePatrol()` / `startAutoPatrol()`（`Audit/patrolScheduler.ts:83,159`） | 定期全量审计 |
| 审计局主入口 | `monitorAgent` / `runPatrol` / `freeze` / `unfreeze`（`Audit/resourceAuditBureau.ts:62,167,177,181`） | 聚合三模块对外出口 |

> ⚠️ 2026-09-22 校准：原表"类名"列的 `AnomalyDetector` / `FreezeManager` / `Auditor` / `PatrolScheduler` 在 Src 中**不是类**；
> 尤其 **`Auditor` 这一名字并不存在**，稽查逻辑是 `resourceAuditBureau.ts` 的 `investigate` 函数。

### 2.3 异常检测规则

#### 规则 1：滚动窗口超限

```
每 5 秒检查一次所有活跃 Agent：

for each agent:
    windowConsumption = sum(consumptions in last 5 minutes)
    budget = agent.tokenBudgetPer5Min  // 默认 10000
    
    if windowConsumption > budget × 0.8:
        trigger ALERT_LEVEL_WARNING
    if windowConsumption > budget:
        trigger ALERT_LEVEL_CRITICAL → 冻结 Agent
```

#### 规则 2：偏离均值

```
每完成一次 LLM 调用后检查：

recentAverage = average(last 10 rounds consumption)
currentConsumption = this round consumption

if currentConsumption > recentAverage × 3:
    trigger ALERT_LEVEL_WARNING
    
if currentConsumption > recentAverage × 5:
    trigger ALERT_LEVEL_CRITICAL → 冻结 Agent
```

#### 规则 3：循环检测

实际实现（`Audit/anomalyDetector.ts:130-155`，调用方为 `monitorAgent(agentId, tokens, outputFingerprint?)`，`resourceAuditBureau.ts:62-74`）：

```
每轮由调用方传入输出指纹 outputFingerprint（未传则跳过本规则）：

fingerprints = 该 Agent 保留的指纹序列（长度上限 loopConsecutiveRounds + 2，默认 7）

if fingerprints.length >= loopConsecutiveRounds:          // 默认 5
    recent = last(fingerprints, loopConsecutiveRounds)
    unique = distinct(recent)

    if unique.size <= 2:
        trigger ALERT_LEVEL_CRITICAL（loop_detected）→ 冻结 Agent
```

> **2026-09-22 校准**：上方原设计的"两两余弦相似度 > 0.85 → warning、连续 5 次 > 0.85 → critical"**未实现**：
> - 判定依据是**指纹集合基数**（`new Set(recent).size <= 2`，阈值硬编码于 `anomalyDetector.ts:147`），Src 中无任何相似度计算；
> - 循环检测**只有 critical 一档**，无 warning；"连续 5 次"在代码里是窗口长度 `loopConsecutiveRounds = 5`，不是计数累加；
> - `audit.loopSimilarityThreshold`（0.85）仅被当作告警对象的展示字段 `threshold` 塞进 payload（`:150`），**不参与判定**；
> - 指纹相等即视为重复（精确匹配），因此"高度相似但不同"的输出不会被识别为循环。
>
> ⚠️ 余弦相似度分档属目标态，未实现（2026-09-22 校准）。

### 2.4 稽查流程

```
异常检测产出任一 critical 告警
    │  monitorAgent() 内自动冻结（resourceAuditBureau.ts:81-88）
    ▼
[1] freezeAgent(agentId, reason)
    ├── 登记 FreezeRecord（内存 Map；freezeId='freeze-N'，autoUnfreezeAt = now + freezeAutoUnfreezeSec）
    ├── ⚠️ 未调用 TokenEconomy.freezeWallet → 钱包状态不变，冻结仅是审计侧内存标记
    ├── ⚠️ 未发 SUSPEND_AND_NOTIFY → Agent 状态不变（无 suspended）
    └── WALLET_FROZEN 实际发布方为 **Audit/freezeManager**（`source='Audit/freezeManager'`，`payload.initiator='AuditBureau'`，freezeManager.ts:61-70）
        └── 与 §3 事件表"发布方 = TokenEconomy"的 v2.1 裁决不一致 → 此处只记录现状，待办见 §3 下方说明（不改代码）
    │
    ▼
[2] investigate(agentId)                                   // resourceAuditBureau.ts:99
    ├── 取该 Agent 的内存告警 getAlerts(agentId)；无告警 → err 返回（:100-103）
    ├── ⚠️ 原设计"拉取最近 50 条操作记录 / 20 条 Token 交易记录"未实现：
    │   Src 中无 operation_history / token_transactions 取数，`audit.auditRecentOpsCount` / `auditRecentTxCount` 为预留未用键（见 §2.6）
    ├── 判定（纯规则，Phase 0-2，:114-130）：
    │   ├── loop_detected 告警数 >= 3 → finding='attack'，recommendation='destroy'
    │   ├── 否则 critical 告警数 >= 2 → finding='confirmed_anomaly'，recommendation='confiscate'
    │   │                             confiscateAmount = Σ(alert.currentValue) × 0.1
    │   └── 否则                     → finding='false_positive'，recommendation='unfreeze'
    │       └── ⚠️ "是否为幻觉？""是否为正常高消耗？"两类分析未实现（Phase 3 接 LLM，见 :97 注释）
    │
    └── 输出稽查报告（:132-141，存内存 Map，不落库）：
        {
          auditId: string,                    // 'audit-N'
          agentId: string,
          finding: 'false_positive' | 'confirmed_anomaly' | 'attack',
          rootCause: string,
          evidence: string[],                 // ⚠️ 不是 EvidenceItem[]；元素形如 '[critical] loop_detected: <detail>'
          recommendation: 'unfreeze' | 'confiscate' | 'destroy' | 'escalate',
          confiscateAmount?: number,
          createdAt: number,
        }
    │
    ▼
[3] 发布 AUDIT_COMPLETED（source='Audit/resourceAuditBureau'，payload={auditId, agentId, finding, recommendation}）
    │
    ▼
[4] 执行稽查建议（:158-160，现状只落地第一条）
    ├── false_positive → unfreezeAgent(agentId, 'audit_investigation')          // ✅ 已实现（顺带发布 WALLET_UNFROZEN）
    ├── confirmed_anomaly → TokenEconomy.confiscateWallet(agentId, amount)      // ⚠️ 未实现：仅算出 confiscateAmount，无罚没调用
    │   └── 罚没后解冻 Agent，通知 Director 重新评估                             // ⚠️ 未实现
    ├── attack → 销毁 Agent + 通知 Regulation                                   // ⚠️ 未实现
    └── escalate → 上报 Regulatory Authority 人工介入                            // ⚠️ 未实现（investigate 当前永不产出 escalate）
```

### 2.5 定期巡检

```
executePatrol()（`Audit/patrolScheduler.ts:83`；定时器入口 `startAutoPatrol()` :159，Src 中无调用方）：

[1] 取巡检数据源 = agentSnapshots（由外部 registerAgentSnapshot() 注入，:77）
    └── 为空 → err('无 Agent 快照数据')，不生成报告
[2] 标记以下 Agent 为"需关注"（两条准则，:99-127）：
    ├── 窗口内消耗 Top `audit.patrolTopPercentile`（按 totalConsumption 降序取前 ceil(N × 比例)）
    └── 窗口内失败率 failureCount/totalRequests > `audit.patrolFailureRateThreshold`
        └── severity：失败率 > 阈值×2 → high，> 阈值 → medium，否则 low
    └── ⚠️ "同窗口无活动但钱包余额异常变化"未实现：Src 不读钱包，快照仅含消耗/请求数
[3] 生成 PatrolReport（内存 Map 保存）→ 发布 PATROL_REPORT
    └── 实际 payload={reportId, patrolId, totalAgents, flaggedCount, summary}，
        `source='Audit/patrolScheduler'`（不是 §3 表写的 'AuditBureau'）
[4] 对"需关注" Agent 执行轻量级分析                        // ⚠️ 未实现：报告中仅列 flagged 清单，无后续分析动作
```

> **2026-09-22 校准**：巡检**不扫描活跃 Agent**，`AgentConsumptionSnapshot` 需调用方自备（当前只有 Tests 通过
> `registerAgentSnapshot` / `addAgentSnapshot` 注入）；`startAutoPatrol()` 与 `audit.patrol*` 四键在 Src 中均无读取点，
> 周期调度属目标态。稽查拉取的条数原由 `audit.auditRecentOpsCount` / `auditRecentTxCount` 提供，两键同为预留未用（见 §2.4、§2.6）。

### 2.6 配置参数

> **本节不维护默认值**（SSOT 裁决，见 Docs/11 文档头）。下表仅列出本子系统消费的键与其定义位置：

| 本文使用的键 | 定义位置 | 说明 | Src 消费状态（2026-09-22 校准） |
|-------------|---------|------|------------------------------|
| `economy.rollingWindow.tokens` / `.windowSec` | Docs/11 §2.4 | 滚动窗口 Token 预算与时长（原误挂在 `audit.rollingWindow*` 下，已去重） | ⚠️ 无读取点：类型 `RollingWindowConfig`（`TokenEconomy/types.ts:128`）仅导出未实例化；实际值取 `anomalyDetector.ts:43-44` 的 `DEFAULT_CONFIG`（10000 / 300 s） |
| `economy.rollingWindow.deviationMultiplierWarn` / `Critical` | Docs/11 §2.4 | 偏离均值倍数（原 `audit.deviationMultiplier` 单一值拆为两档） | ⚠️ 无读取点，且代码字段名为 `deviationWarnMultiplier` / `deviationCriticalMultiplier`（`anomalyDetector.ts:45-46`，默认 3 / 5） |
| `audit.loopSimilarityThreshold` / `audit.loopConsecutiveRounds` | Docs/11 §2.6 | 循环指纹检测（原名 `loopConsecutiveCount` → `loopConsecutiveRounds`，单轮单位） | ⚠️ 无读取点；且 `loopSimilarityThreshold` 不参与判定（见 §2.3 规则 3），只有 `loopConsecutiveRounds`（默认 5）真正被消费 |
| `audit.patrolIntervalSec` / `patrolTopPercentile` / `patrolFailureRateThreshold` / `patrolWindowHours` | Docs/11 §2.6 | 巡检（原名 `patrolIntervalSeconds` → `patrolIntervalSec`） | ⚠️ 只能经 `initPatrolScheduler(cfg)` 注入（Src 无调用方），默认见 `patrolScheduler.ts:51-56`；`windowHours` 定义了但 `executePatrol` 未做时间窗过滤 |
| `audit.freezeAutoUnfreezeSec` | Docs/11 §2.6 | 冻结自动解冻（原名 `freezeAutoUnfreezeSeconds` → `freezeAutoUnfreezeSec`） | ⚠️ 需 `setAutoUnfreezeSec()`（`freezeManager.ts:36`）注入，Src 无调用方；默认 300 秒 |
| `audit.auditRecentOpsCount` / `auditRecentTxCount` | Docs/11 §2.6 | 稽查取数条数 | ⚠️ 预留未用：`investigate` 只读内存告警（见 §2.4 [2]） |
| `supervision.agentConsecutiveFailuresToExpel` | Docs/11 §2.7 | 开除阀值（属监管层，不属审计） | ⚠️ 预留未用：Src 中无 `supervision.*` 读取点（见 §0） |

> **口径说明**：本子系统当前**不接 ConfigLoader**——检测阈值全部内置于三个模块的 `DEFAULT_CONFIG`，
> 唯一注入路径是 `initResourceAuditBureau({ detectorConfig, patrolConfig })`（`resourceAuditBureau.ts:52`），而该入口在 Src 生产链路中无调用方（仅 Tests）。
> `Configs/audit.json` 的相应键因此属"已声明未接线"，改配置当前**不会**改变运行时行为。

---

## 3. 事件清单

> 本表只列**本子系统参与**的事件；事件名的唯一定义处为 `Docs/07 §4.3 EventType` 注册表，本表出现的事件必须能在该表找到同名成员。
> **发布方口径（v2.1 统一）**：`发布方` = **执行状态变更的组件**（写入 `DomainEvent.source`）；触发方记入 `payload.initiator`。审计只自发布 `ANOMALY_DETECTED` / `AUDIT_COMPLETED` / `PATROL_REPORT` 三类。

| 事件 | 发布方（source） | 触发方（payload.initiator） | 订阅者 |
|------|----------------|---------------------------|--------|
| `RULE_UPDATED` | Regulation | — | 所有 Agent（更新行为准则） |
| `BROADCAST_MESSAGE` | Regulation | — | 目标 Agent 范围 |
| `EMERGENCY_INTERVENTION` | Regulation | Regulation / Audit | Core（执行紧急操作） |
| `ARBITRATION_DEADLOCK` | Arbitration | — | Regulation（最终裁决） |
| `ANOMALY_DETECTED` | Audit | AnomalyDetector | Dashboard, Regulation |
| `WALLET_FROZEN` | **TokenEconomy** | Audit | Core(中间件), Dashboard |
| `WALLET_UNFROZEN` | **TokenEconomy** | Audit（自动解冻时 initiator='system'） | Core(中间件), Dashboard |
| `TOKEN_CONFISCATED` | **TokenEconomy** | Audit | Dashboard, Director |
| `AUDIT_COMPLETED` | Audit | — | Dashboard, Director |
| `PATROL_REPORT` | Audit | PatrolScheduler | Dashboard |

> **2026-09-22 校准（现状 + 待办，不改代码）**：
> - `WALLET_FROZEN` / `WALLET_UNFROZEN` 实际由 **`Audit/freezeManager`** 发布（`freezeManager.ts:61-70`、`:94-101`，`source='Audit/freezeManager'`），
>   并非上表裁定的"执行状态变更的组件 = TokenEconomy"；TokenEconomy 侧确有 `freezeWallet` / `unfreezeWallet` / `confiscate`
>   （`walletManager.ts:164,189,214`），但它们只写 `token_transactions`（`FREEZE` / `UNFREEZE` / `CONFISCATION`）、**不发布任何事件**，
>   且在 Src 生产链路中**无调用方**（仅 Tests 引用），故 **`TOKEN_CONFISCATED` 从未被发布**、审计冻结对钱包余额实际无影响。
>   待办：在 TokenEconomy 落地钱包冻结/罚没并把事件收回该模块，或由中间件统一改由钱包侧发布（另见 Docs/04 §5 事件口径、本文 §4"TokenEconomy｜冻结/罚没 Token"依赖尚未落地）。
> - 上表"订阅者"列（Core / Dashboard / Regulation / Director）在 Src 中**均无 `subscribe` 注册点**：本子系统当前"只发不收"，
>   事件仅作为可观测记录；真正执行动作的是模块内的直接函数调用（如 `monitorAgent` 内联冻结）。

---

## 4. 依赖关系

| 依赖组件 | 来源 | 用途 |
|---------|------|------|
| `EventBus` | Services | 事件发布/订阅 |
| `TokenEconomy` | Services | 冻结/罚没 Token |
| `Arbitration` | Services | 接收死锁升级 |
| `AgentRuntime` | Core | 冻结/终止 Agent |
| `Database` | Infra | 稽查报告持久化 |
| `LlmCaller` | Tools | 最终裁决 LLM 调用 |

---

## 5. v2 补丁：触发防风暴与人工审批（本修订新增）

> **修订依据**：`Docs/99-审查记录/设计审查报告-Loop与Harness工程.md` §3 失效五、§4.6。

### 5.1 Trigger 防风暴规范（新增）

监管局与审计局作为**高频事件消费者**，必须实现业界标准的 5 道防护：

| # | 机制 | 默认 | 实现位置 |
|---|------|------|---------|
| 1 | **去重键 dedupKey** = `hash(source+intent+target)` | 5 min TTL | Interface/InputDeduplication |
| 2 | **冷却期 cooldown** | 同 Agent 30s | Services/Audit/AnomalyDetector |
| 3 | **幂等 ID idempotencyId** | 24 h 保留 | Infra/DurableExecution/IdempotencyStore |
| 4 | **并发上限 + 优先级队列** | 全局 20 / 单用户 5 | Services/LoopScheduler（新增） |
| 5 | **任务风暴熔断** | 1 min 内 >100 次触发 → 断开 60s | Interface/WebServer |

**与律师函挂起的关系**：`SUSPEND_AND_NOTIFY` 事件本身去重，同 (loopId, conflictId) 5 分钟内只挂起一次，避免雪崩式挂起。

### 5.2 人工审批面板（Human Gate UI）

监管局新增 `ApprovalQueueView`，展示 `pending_approvals` 表，提供：

| 列 | 内容 |
|----|------|
| 风险等级 | LOW/MEDIUM/HIGH/CRITICAL，颜色分级 |
| 请求人 | agentId + role |
| 预览 | payload 摘要（展开可看完整 JSON） |
| 证据链 | 关联 trace_id 链接 → LoopDebugger |
| 倒计时 | expires_at - now；默认 60s |
| 动作 | 同意 / 拒绝 / 转升级到上级 |

**约束**：
- 默认超时为 **拒绝**（禁止默认通过）；
- CRITICAL 级别需要 2 个不同角色审批（多数决）；
- 审批结果回写到 `verifier_results`，作为 Judge 校准集的一部分。

### 5.3 无进展与风险退出上报

对齐 Docs/12 §2.1 五类退出，监管局需订阅以下新事件：

| 事件 | 来源 | 监管动作 |
|------|------|---------|
| `NO_PROGRESS_STAGNATED` | LoopControl | 自动升级人工，写入审计日志 |
| `BUDGET_SOFT_REACHED` | LoopControl | 仅记录（不阻断） |
| `BUDGET_HARD_REACHED` | LoopControl | 强制冻结 Agent 钱包 5 min |
| `APPROVAL_TIMEOUT_REJECTED` | ApprovalGate | 上报监管局；同 Agent 1h 内 ≥ 3 次 → 临时禁用相关工具 |
| `LOOP_RESUMED` | DurableExecution | 写入审计事件，需人工确认 |
| `ACTION_FINGERPRINT_DUPLICATE` | LoopControl | 同 fingerprint 5 轮 ≥ 3 次 → 监管局下发换策略指令 |

